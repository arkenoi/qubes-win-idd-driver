# Handover — 2026-08-12 — defect bundle fixed+validated on 25H2; WM-managed shell surfaces live

Continues `HANDOVER-2026-08-11b.md`. Trust `git log` + `FINDINGS.md` over prose.

## User goal (set 2026-08-11 late, supersedes "record only" for reqs 4-7)

Reliably working and tested Start menu + toasts on 25H2: clickable, movable, correctly
positioned (stackable if feasible), NO regressions; then full e2e release testing including
Win10, and a basic benchmark on Win11 afterwards.

## VM roster RIGHT NOW

| VM | state | build |
|---|---|---|
| win11-fresh (25H2) | Running, VALIDATED | agent 4dc559b = sha 83b69f62, swap-deployed |
| win11-24h2 | Halted | old crop build + ToastCropDisable=1 |
| win10-clean | Halted | unknown (QWT-NG 4.3.x) |
| win-idd-test | GONE (deleted long ago; policy refused) | — |

Policy is tag-based (created-by-win-idd-mgmt / win-idd-testbed / qwt-bench).

## What was root-caused and FIXED this session (agent b92506c..4dc559b, all CI-green,
## deployed+validated on win11-fresh EXCEPT where noted)

1. **Defect B (mass destroy/re-announce + overlap)**: capture.c:1330's 1 s frame-processing
   timeout was FATAL -> full reset. Now tolerated to 15 s (CAPTURE_READY_WEDGE_MS), stop
   honored mid-wait. VALIDATED: scripted drags no longer reset (was ~1/drag, census 6/25min).
2. **Re-announce ordering**: AddAllWindows announces bottom-first (X create-on-top would
   invert stacking on every reset); foreground re-raise moved AFTER the pass + re-armed by
   ResetWatch (g_LastForeground). NOT separately validated (needs a forced reset).
3. **Defect A (drag weirdness)**: composite - the resets above + poisoned geometry belief.
   VALIDATED drag dt p50 ~18 ms at 5120x1440 (3 interleaved runs + cold boot run).
4. **Poisoned resolution belief (S2 mechanism, unclickable toasts, workarea 0x57, A3CHECK
   grant mismatch)**: both resolution.c apply paths now ADOPT the read-back applied size;
   ResolutionAdoptCurrent()/RESDRIFT on WM_DISPLAYCHANGE + 2 s drift tick + A3CHECK
   divergence self-heal; known-unappliable memo (60 s TTL) stops replug churn. VALIDATED:
   RESDRIFT tracked boot transit, A3CHECK converged g==ctx, SPI_SETWORKAREA failures 0
   (was 51), 5120x1440 desktop stuck across cold boot. **The out-of-band mode reverter was
   never identified** - RESDRIFT now masks it; watch RESDRIFT counts.
5. **HandleButton dead dx/dy (unclickable toasts)**: Button1-3 now carry their own absolute
   position (registry ButtonAbsolute, default ON; suppressed when only the untracked
   raw-origin fallback is available). NEEDS USER: a real dom0 click on toast buttons.
6. **CREATEDUP damage-drop** (failed DESTROY in degraded ring leaves hwnd damage-dropped):
   ring cleared in the suppressed branch. Not field-validated (needs FI_RING_STALL build).
7. **Toastcrop async worker**: UIA moved off the input path (worker owns its IUIAutomation,
   no shared lock during RPC, PokeWindowTracking on resolve, 2 s walk deadline). Pacing bug
   found live and fixed (attempts count completed measurements, 6x250 ms). VALIDATED:
   insets measured on live toasts, upd ticks stay low.
8. **ProtoTrace wobble probe opt-in** (per-rect GetRealWindowRect+lock; ProtoTraceWobble).
9. **WM-managed shell surfaces (movability)**: classified shell surfaces announce or=0
   behind ShellManaged (default ON); HandleConfigure applies dom0 moves crop-compensated
   (SWP_NOSIZE); HandleFocus quarantines shell surfaces (no focus steal - VALIDATED, Notepad
   kept foreground through a toast). Feasibility proven: SetWindowPos on shell-band HWND
   sticks. NEEDS USER: actually dragging the toast/Start by their dom0 title bars.
10. **Oversize shell hosts cropped** (25H2 Start at 5120x1440 = 858x874 card in a
    WORKAREA-SIZED 5120x1384 host; was an opaque fullscreen white window): classifier size
    ceiling removed, absolute card floor (300x200) replaces the ratio guard for hosts >=90%
    of screen. VALIDATED: Start announces 2149,604 832x736 = the card, WM-framed, pixels
    perfect; toast at 4740,921 = real dom0 bottom-right corner.

## Validated end state on win11-fresh (fullshots scratchpad/fs-v5*, 2026-08-12 ~02:00)

Desktop 5120x1440 == dom0 (IDD mode stuck through cold boot). Start = card-sized WM-managed
window; toast = card-sized WM-managed window at true bottom-right with buttons rendered;
no focus steal; drag smooth; zero resets; zero workarea failures. Toast stacking = native
Windows behavior faithfully mirrored (one banner at a time, rest in Action Center).

## Instruments (all in guest/, all new this session)

drag-measure.ps1 (SendInput circle drag + QGAPERF window extract), drag-harness.ps1,
perf-window.ps1, log-window.ps1, reset-census.ps1, defect-evidence.ps1, fixcheck-grep.ps1,
toastcrop-debug.ps1, geometry-truth.ps1, fire-toast.ps1 (persistent reminder toast via
interactive scheduled task), open-start.ps1 (**Win key WORKS from an interactive-session
scheduled task** - automated Start opening is now possible), move-toast-probe.ps1,
focus-check.ps1, discriminator-grep.ps1, reg-ga-dump.ps1, find-agent-log.ps1.
Deploy: push gui-agent.exe + swap-agent.ps1, run swap-agent -NewAgent (elevated via qtest ps
works on Win11 here); crash-loop check = PID stable 75 s + log count static + hash match.

## Still open / next

1. **Task 5 in flight**: win10-clean e2e (start, deploy 83b69f62, health+Start+toast+drag),
   win11-24h2 non-regression (Start must announce card-fills-window there; NOTE its registry
   still has ToastCropDisable=1 - REMOVE it first or the crop is off), then benchmark on
   win11-fresh (docs/BENCHMARKS.md methodology).
2. USER-HAND checks: dom0 click on toast buttons; dom0-WM drag of toast/Start; drag feel.
3. Unidentified out-of-band mode reverter (RESDRIFT masks it; if RESDRIFT counts grow, dig).
4. Rank-6 (synthesis): HandleConfigure ACK failure during >=10 s daemon stall is still
   fatal -> respawn; deferred-resend design written up in the synthesis output
   (workflow wf_5a8fcf3b-b26, /tmp task wzn31853g output file).
5. FI-proof obligations: FI_PUMP_STALL (15 s wedge), FI_RING_STALL (CREATEDUP fix) on a
   QGA_FAULT_INJECTION build.
6. Win-key suppression in seamless (req 3), Start as app-menu item (req 2) - untouched.
7. Registry switches live: ShellManaged, ButtonAbsolute, ProtoTraceWobble (all in
   gui-agent subkey; escape hatches for every behavior change this session).

## ADDENDUM (end of session) — release e2e outcomes

- **win11-fresh (25H2, the goal platform): FULLY VALIDATED** (see main body).
- **win11-24h2**: crash-loop + cold boot PASS on 83b69f62; toast cropped+positioned; ONE
  RESIDUAL: Start announces uncropped 5120x1384 (the StartMenuExperienceHost hwnd morphs
  sizes; the workarea-size cache key never completes a measurement - see FINDINGS).
  Left Halted... then briefly restarted for corewin-scan; shut down again.
- **win10-clean: BLOCKED** - qrexec token fully UAC-filtered on 19045.6456, the historical
  HIGHEST-task elevation no longer works, no unattended admin path. ONE user action in the
  guest unblocks it (EnableLUA=0 or one manual agent install); then run
  scratchpad/e2e-win10-retry.sh.
- **Benchmark reference (new config)**: win11-fresh at 5120x1440, build 83b69f62:
  idle median 4.79 %core, load median 5.90 (3 runs each, stable, hash-verified). Not
  comparable to the 1920x1080 historical table; idle at 4x pixels = future watch item.
- TaskStop'd watchdog: the 150-min e2e watchdog (ba1tqahiz) may still be running - harmless,
  it exits when nothing matches.
