# Handover — 2026-08-11 (QWT-NG)

Context got polluted (long session, several wrong assumptions about dom0 state). This is the clean
state to resume from. Trust `git log` + `FINDINGS.md` + the docs named below over any prose memory.

## VM state (verified this session)
- **win11-fresh**: StandaloneVM, Win11 **24H2 build 26100.8875**, Running. tags: `win-idd-testbed`,
  `created-by-win-idd-mgmt`, guivm/audiovm-dom0. Volumes in `vm-pool` (lvm_thin, ~220 GB free).
  The gui-agent was wedged mid-session by a repro and RECOVERED by killing `gui-agent.exe` (the
  `QubesGuiWatchdog` service relaunches it). It is producing frames again.
- **win10-clean**: StandaloneVM, Halted, also `created-by-win-idd-mgmt`.
- Driven only via `tools/qtest` (`export QTEST_VM=win11-fresh`). `qtest run`/`shot` WORK right now.
- This mgmt qube (win-idd-mgmt) has PARTIAL Admin API: `admin.vm.List/Create/Start/CurrentState/
  property.Get/pool.List` ALLOWED; **all `admin.vm.volume.*` DENIED** (`no matching rule found`) —
  this is why cloning is currently blocked. I CANNOT read installed dom0 policy (no `policy.Get`);
  do not infer dom0 policy from `dom0/*.sh` — some of it was deleted 2026-08-10.

## DONE + committed this session (all validated on the guest unless noted)
1. `9fac0d2` **NotifyUpdates fixed** — root cause was ARG QUOTING, not payload: `qrexec-client-vm`
   splits its raw command line on `|` and never strips quotes, so wrapping the pipe-arg in `"..."`
   leaked a quote into the target (`"dom0`) → daemon refused. Pass fields UNQUOTED (PowerShell split
   tokens). User confirmed Qube Manager lit up. See memory `qrexec-client-vm-arg-quoting`.
2. `175969b`,`c6bee7d` **Updater = Linux model**: guest never auto-installs. Scheduled scan-only
   task reports availability via `qubes.NotifyUpdates` (= Linux's update-check timer). dom0-DRIVEN
   install via rpc `qubes.WindowsUpdate` (guest/wu-update.ps1) speaking the real qubes-vm-update
   protocol (float progress on stderr, exit 100 = no updates); dom0 wrapper
   `dom0/14-install-qvm-windows-update.sh` → `qvm-windows-update`. Validated: exit 100 + 0.0/3.0/100.0
   on the patched guest; exit 1 when the run-task is deleted. NOT validated: a real install pass
   (guest fully patched) — needs an out-of-date guest.
3. `6ef7f07` **Proxy-flood fix** — the updater set a SYSTEM-WIDE WinHTTP proxy and never removed it,
   so every Windows bg HTTP client phoned home through the relay (147 qubes.UpdatesProxy calls in an
   afternoon = "the dom0 request flood"). `Remove-Proxy` now runs in a `finally`; proxy lives ONLY
   during a pass. Verified: post-scan = Direct access, no relay proc.
4. `f862b6d` **Relay `_@default` fix** — same quoting bug in the relay's spawn; the `_` was qrexec
   sanitizing the leaked `"`. Now `domain '@default'` clean. (A short-lived poll service was added
   then removed in favor of the streaming `qubes.WindowsUpdate`.)
5. `f6e3173`,`ae7450d`,`b97d0a5` **GWeck S1 reproduced (severe) on 24H2** — see below.

## GWeck v4.3.1 field report (thread t/42717 posts 44-45) — investigation COMPLETE, fixes NOT written
Full synthesis: `docs/GWECK-post44-investigation.md`. Evidence (his winenum.log, the crash-dialog
screenshot) in `instrumentation/gweck-post44/`. Symptoms + status:
- **S1a garbled Start / S1b dom0 "invalid/suspicious GUI request" dialog** (highest severity): the
  daemon dialog is PROVEN (by elimination) to be `MSG_CREATE` with negative-as-int width/height
  (`xside.c:2937`); the SOURCE of the negative value is UNEXPLAINED (the g_StartWindow-inversion
  theory was REFUTED — g_StartWindow is NULL on 25H2). **On 24H2 the div0/EnumDisplaySettings
  candidate was FALSIFIED live (scale=1, all rects positive, no dialog).** So the DIALOG is
  25H2-only, still unreproduced. **What I DID reproduce on 24H2** (live gui-agent log, `FINDINGS`
  2026-08-11): pressing Win makes the agent thrash the Start CoreWindow size (5120x139x ↔ 858x87x)
  with a grant-rebuild + UNMAP/MAP per flip (the clip region blinks during the open animation);
  under rapid toggling this floods the vchan (`VchanSendBuffer: buffer full`) and the CAPTURE THREAD
  DIES (`error/timeout`) and does NOT recover (RecreateDuplication never fires; the watchdog only
  restarts a DEAD process, not a stuck one) → guest display frozen. A real guest-display DoS.
- **S2 mouse ~1cm-low + S3 caret**: agent stores the REQUESTED (not read-back APPLIED) resolution on
  `RESAPPLIED-MISMATCH` (resolution.c:1207) + missing SDC_FORCE_MODE_ENUMERATION poke → injects
  pointer against a wrong believed height → vertical-only skew. **Reproducible on 24H2** via a forced
  A4CLAMP request (not yet done).
- **S4 `/idd` file-not-found**: user error (`Start-Process -FilePath 'D:\idd'`) ON TOP OF our gaps —
  `/iddonly`+`activate-idd.ps1` (commit 80f8d97) were authored ~4h AFTER the v4.3.1 assets uploaded,
  so they NEVER shipped ("Unknown option: /iddonly"); README is stale. FIX: re-release from a tag
  containing 80f8d97 + fix README. Draft forum reply is in the GWeck doc (needs user approval to post).

## Toast overhaul (task #14) — design COMPLETE, not implemented
Judged patch plan saved to `docs/TOAST-fix-plan.md`. Agent-only fix (no daemon/protocol change):
crop the visible toast card (new CropL/T/R/B in WINDOW_DATA, applied inside GetWindowData so all
consumers stay consistent), keep override_redirect (align IsPopup's oversize demotion with the
daemon's 90%-of-work-area rule so the existing ToggleMap path recovers OR), unify HandleButton click
math on tracked X/Y, suppress dom0 SetWindowPos for cropped windows. Real toast geometry measurement
was flaky (the toast-probe fires but the WinRT call under the throwaway AppID needs fixing).

## BLOCKED / awaiting: 25H2 target
Goal (user): reproduce GWeck's EXACT env (25H2) to nail S1's dialog, then confirm the fix there.
- Clone is BLOCKED: `admin.vm.volume.Clone` denied. I wrote the COMPLETE admin grant needed:
  **`dom0/30-win-idd-mgmt-admin.policy`** (43 rules, scoped to `@tag:created-by-win-idd-mgmt` — this
  qube can manage only what it created, nothing else). Install in dom0:
  `qvm-run --pass-io win-idd-mgmt 'cat .../dom0/30-win-idd-mgmt-admin.policy' | sudo tee /etc/qubes/policy.d/30-win-idd-mgmt-admin.policy`
- The 25H2 enablement package **KB5054156** direct URL is known (catalog.sf.dl CDN) and was
  DOWNLOADED to `C:\ProgramData\Qubes\wu\kb5054156-25h2-ekb.msu` on win11-fresh (170 KB) but is
  **NOT installed** (eKB is removable → 24H2 reversible, but a clone is the safer insurance).
- PLAN (do in order, don't skip step 1): install the policy → `qvm-clone win11-fresh win11-24h2`
  (verify it keeps `win-idd-testbed`+`created-by-win-idd-mgmt`) → DISM the eKB on win11-fresh →
  reboot → confirm build 26200 + gui-agent alive → reproduce S1a/S1b → read dom0 guid log for the
  exact failed VERIFY → then fix.

## NEXT (suggested order)
1. (needs the policy) clone 24H2, then in-place 25H2 on win11-fresh; reproduce S1b's dialog.
2. Reproduce S2 (mouse offset) on 24H2 via A4CLAMP — no 25H2 needed; do it anytime.
3. Implement + build (CI) + validate: the toast plan; the S1b structural guard (SendWindowCreate
   `(int)w/h>0`, mirroring send.c:600) proven by INJECTING an inverted rect (see it fail w/o guard);
   the S2 resolution.c "adopt applied size" fix; then S1a Wnd_StartFeed handling.
4. Re-release with 80f8d97 (ship /iddonly + activate-idd.ps1) + README fix; post GWeck reply after
   user approves the text in the GWeck doc.

## Retractions / do-not-repeat
- Do NOT infer live dom0 policy from repo `dom0/*.sh` — some were deleted 2026-08-10.
- The div0→INT_MIN→dialog theory is FALSE on 24H2 (measured). The dialog needs 25H2.
- Nothing irreversible was done to win11-fresh (24H2, unchanged, running). eKB downloaded ≠ installed.
- Untracked scratch in the tree (release-artifacts/, release-final/, guest/*probe*, guest/stage6*)
  is throwaway; ignore unless a task references it.
