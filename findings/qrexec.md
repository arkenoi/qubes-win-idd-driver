# qrexec — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- Retraction guard: claims retracted during the erased 2026-08-30/31 window stay retracted — in particular, any claim that stock QWT 4.2.2 qrexec cannot interoperate (with our packages, or on this 4.3 host) was retracted as hallucination and must not be resurrected; the 2026-08-29 WIN11 stock cell drove a genuine stock-4.2.2 guest over qrexec end to end (install PASS, hotplug PASS, final health-check ok:true). [verified 2026-09-01]
- How qrexec runs on the testbed TODAY: dom0 policy imposes `user=SYSTEM` for `qubes.VMShell` on testbed-tagged guests, so `qtest run` answers with NO interactive session at all (measured on a logged-off win10-app: `whoami` = nt authority\system while `query session` showed no user); the caller can never request this, only dom0 policy can impose it; it supersedes the 2026-08-12 "Medium-IL filtered token, no unattended admin path, blocked on the user" conclusion for elevation. [verified 2026-09-01]
- Stock/end-user session dependency (mechanism read in upstream/ro/qubes-core-agent-windows): `qubes.VMShell` targeting user `user` sets wrapper flag 0x04 "run in the interactive session (REQUIRES THAT A USER IS LOGGED ON)" (qrexec-agent.c:704), and `qubes.WaitForSession` only accepts WTSActive sessions (wait-for-logon.c:73) — so on stock QWT no session = no user RPC, clipboard, or file-copy; whether a merely LOCKED (still-WTSActive) session also kills RPC is UNPROVEN (the LockWorkStation probe was never run); testbed answer files disable idle-lock/monitor timeouts/screensaver so guests never idle-lock mid-run. [verified 2026-08-07]
- Filecopy/pushrun trap: `qtest push` is qubes.Filecopy into the logged-on user's Documents (`QubesIncoming`); with no session `MyDocuments` is empty for SYSTEM and the copy produces NO OUTPUT at all — `pushrun` on a session-less guest reads exactly like "probe ran and found nothing" — so any guest that may lack a session must be probed with inline `qtest run` (long payloads via `powershell -EncodedCommand`); source memory `pushrun-needs-a-session` dates to the voided 2026-08-31 window, so re-verify live before betting a verdict on it. UNVERIFIED
- Quoting trap: `qrexec-client-vm.exe` splits the RAW command line on `|` and never strips quotes — pass `domain|service|user|prog` UNQUOTED (cmd: escape pipes `dom0^|svc^|user^|prog`; PowerShell: split tokens so the pipe token contains no spaces) or the quote leaks into the target field and dom0 refuses the call; and exit 0 only means the trigger reached the local agent pipe — exit 0 ≠ delivered/allowed, read `Q:\Qubes Logs\qrexec-agent-*.log` for the actual parse and any HandleServiceRefused. [verified 2026-08-11]
- Timeout/queued-call handling: a qrexec call to a Halted qube AUTO-STARTS it and hangs for the full `qrexec_timeout` (standing value 6000 s here), and queued calls outlive the shell that made them — "phantom restarts" after qvm-kill are this queue, and `admin.vm.Start` never returning on a dead-agent guest is the same pattern; drain with a TEMPORARY `qvm-prefs <vm> qrexec_timeout 15` + `qvm-kill`, then RESTORE 6000 (15 is never left in place); always set `QTEST_VM` explicitly — a bare `tools/qtest` silently starts `win-idd-test`. [verified 2026-08-29]
- Task #14 wrapper byte loss CLOSED and SHIPPED: root cause was qrexec-wrapper.c's receive loop treating a closed vchan as an empty one, discarding whatever was still in the ring (Linux names this exact race in libqrexec/process_io.c); fix e5e94b8 proven by effect (unfixed control: 9 short bodies in 60 fetches; fixed: 0 in 90, wrapper hash asserted inside each run); ac33bc9 (1 s I/O-thread wait before sending the exit code) is a second real defect on the OUTBOUND direction only; the release path initially did not build core-agent (fix proven-but-unshipped until 2026-08-28), closed in the 4.3.15 candidate (463c176) whose win11 e2e verified our qrexec-wrapper.exe live and differing from the stock copy on installed guests. [verified 2026-08-28]
- STANDING RULE (from the missed-fix audit): committed and CI-green is NOT shipped — the test is whether the ARTIFACT A USER INSTALLS carries the change, not whether the repo or a CI job does; mechanised in packaging/check-ours-wins.ps1's CompiledSources check, which fails the build when a core-agent source diverges from its 4.2.2 base without its binary shipping. [verified 2026-08-28]
- Self-overwrite trap: qrexec-wrapper.exe cannot be replaced from a qrexec call — every `qtest run` IS that binary, so its image is locked and Copy-Item fails (silently under -EA SilentlyContinue); rename the running image aside first (Windows allows renaming a running exe), then copy — Install-QwtImproved handles this (*.qwt-prev). [verified 2026-08-28]
- Sign-in screen: the owner decision requested on 2026-08-28 was resolved the same day by the owner's secure-desktop revision (recorded in CLAUDE.md): NON-SEAMLESS shows the guest desktop secure-or-not inside the bounded window; SEAMLESS never grants it (frame path freezes; QGADESKSTUCK warns at 30 s then every 120 s — a threshold chosen from measurement, healthy boots clear in 15-38 s); autologon is ENFORCED as the lockout answer (guest/set-autologon.ps1, LSA DefaultPassword). [verified 2026-08-28]
- Shutdown-race artifact: "N quick agent deaths" in the watchdog log at machine shutdown are the SCM notification race, not a crashing agent — in the ~2 s before Windows signals preshutdown every AgentRespawnPointless() signal says "keep running" (wtsstate=0 is WTSActive; the WTS enum starts at 0), so even with the 284bda4 latch the FIRST death of a shutdown still gets a harmless respawn; suppression is PARTIAL by design, no signal exists to close the gap, and this artifact has repeatedly been misread as a crash loop. [verified 2026-08-28]
- Uninstall-removes-channel trap: QWT IS the qrexec agent — `msiexec /x` on a running guest removes the only control channel (win10-u10 was left booting but permanently unreachable this way); a stock/clean precondition can only be built at PROVISIONING time (build-answer-stick.sh STOCK_SETUP path), never by uninstalling from a driveable guest. [verified 2026-08-29]
- Streaming trap: `Get-Content -Wait` over qrexec dies SILENTLY (observed stopping at 28 lines while the guest-side log grew to 104) — poll logs with bounded reads and compare counts; never trust a long-lived stream over this channel. [verified 2026-08-28]
- Wedge signature: guest Running + qrexec dead + CPU pegged (~3.5 vCPUs) + ZERO guest-log events matches the proven Xen HVM IPI/TLB-shootdown wedge class (out of QWT scope, upstream-reportable); the one post-netvm-attach occurrence (win11-24h2, 19+ min, self-recovered) is PLAUSIBLE not proven, n=1; the same boot's separate real finding is the IDD's UMDF driver failing to load (WUDFRd, 0xC0000365), which alone explains "no windows mapped". [verified 2026-08-29]
- PV console as a command channel (`tools/qcon`, validated both ways before use): distinguishes BUSY (flock exit 200) / REFUSED (policy exit 126) / NOCONSOLE — a silent console is usually another attacher, not a dead console; LIMIT: `qcon <cmd>` only works if someone has logged into the console since the last boot (post-reboot it sits at `login:`; `--raw` proves liveness only), and this rig stores no guest credentials — so it is NOT an unattended introspection channel for an overnight wedge. [verified 2026-09-01]
- Console vs qrexec, honestly scoped: on THIS rig the console adds no session advantage (SYSTEM qrexec already answers session-less; the earlier "qrexec needs a logged-on session" edge is RETRACTED for the testbed and holds only for end-user stock QWT); both channels share xenbus.sys, Xen event channels, and guest scheduling, so the IPI/TLB wedge likely silences both — for wedge RESCUE the thing to build is emulated serial + EMS/SAC, not more console; the console's unique value is the retroactive transcript `/var/log/xen/console/guest-<vm>.log` plus the dom0-side `sudo xl console -t pv` path (note `admin.vm.Console` from this qube is itself a qrexec call to dom0 — only the guest agent is bypassed). [verified 2026-09-01]
- Unauthenticated-console option is an OWNER gate: repointing `HKLM\...\Services\xencons_monitor\Parameters\default\Executable` away from xencons_tty would hand an unauthenticated SYSTEM console to anyone holding admin.vm.Console policy — a security tradeoff, not a technical one; do not do it without an explicit owner decision. [verified 2026-09-01]
- Grant substrate (context for agent/qrexec restart churn): the shipped xeniface release build NEVER revokes a grant — its only RevokeForeignAccess call sits inside a compiled-out ASSERT, confirmed by disassembly of the MSI's own binary — so every agent start / qrexec invocation strands its refs (staging ~7200 pages at 5120x1440, vchan ring ~33 pages) until domain destruction; MECHANISM established, live exhaustion NOT reproduced (103+ precondition-asserted restarts, zero 0x5aa, ~145 predicted), the earlier per-kill-leak claim and its retraction are both superseded by this; fix is upstream XenProject's, reportable under the CLAUDE.md exception with owner-approved text. [verified 2026-08-16]
- gui-daemon disconnect handling (already on CLAUDE.md's upstream-reportable list): `handle_vchan_error` never consults `vchan_at_eof`, so a disconnect noticed on the WRITE path exits instead of restarting — force-killing the agent loses the GUI probabilistically (depends on what the daemon had queued), a graceful stop drives the recovering poll path; there is NO reconnect race (`libvchan_client_init` waits indefinitely); guest-side hole: the agent removes its xenstore node only if a client ever connected (main.c:3749), so a never-connected agent exit strands a stale node that kills the next guid permanently. [verified 2026-08-04]
