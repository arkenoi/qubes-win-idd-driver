# windowing — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- Toasts render on screen (owner-observed, SG7); any claim that the toast checks were vacuous was retracted. Campaign record: docs/ACCEPTANCE-4.3.16.md. [verified 2026-08-30]
- TOAST BRIDGE (docs/DESIGN-toast-bridge.md Proposal C phase A0) is RUNNING C++ CODE. A0 was first implemented as a PowerShell spike (Opus 4.8, commit 3ef9468, guest/toast-bridge.ps1) that PROVED the mechanism on win10-app (both split directions; UserNotificationListener works from an unpackaged user-session once HKCU ConsentStore userNotificationListener=Allow is seeded; listener exposes TEXT ONLY, so per-app split needs nothing undocumented while per-toast would need wpndatabase.db). That spike + the listener probe were RETIRED this session (deleted) once the shippable resident landed: `tools/notifhost.exe --bridge` (commit c1ce4f0) — resident user-session forwarder, allowlist = HKLM gui-agent `NotifyBridgeAllow` REG_MULTI_SZ of AUMIDs (THE classifier: per-app, content-blind by design), ONE long-lived qubes.Notifications connection via a `--relay` child (stdio=vchan) spliced to a named pipe, wire codec ported from NotifyClient.cs, Dismissed(reason=2 only)→RemoveNotification dismiss-sync. Launched+supervised by the SYSTEM agent via Task Scheduler; gate = registry `NotifyBridge` / qubesdb `/qubes-service/notify-bridge` (dom0 wins), default OFF; **explicit opt-out `service.legacy-toasts` forces the bridge off (o-r toasts as before) and WINS over notify-bridge**. FAIL-OPEN is the invariant: LAZY suppression (an app's banner is ShowBanner=0 only AFTER its first successful forward, so a bridge that never forwards never suppresses; first toast per app double-shows), a toast is not marked seen until forwarded, consent seeded only when unset (never over an explicit Deny). **CLASSIFIER: per-app allowlist `NotifyBridgeAllow` (REG_MULTI_SZ); when unset a conservative compiled DEFAULT_ALLOW seed is used (Snipping Tool, Camera, Photos, Windows.SystemToast.SecurityAndMaintenance, BackupReminder — all reliably-informational; real-choice senders like WindowsUpdate.Notification / Phone Link / calendar-reminders / Explorer are DELIBERATELY EXCLUDED in the source comment). Exact case-insensitive AUMID match; extend per guest via `--dump-aumids`. The earlier empty-default gap is resolved.** Acceptance harness `mgmt/harness/a0-toast-bridge.sh` (P1-P7) — Opus-5 run, NOT run on Fable. NOT-YET: allowlist seeding, overlay installer deploy (dev path), Phase-2 synthetic "Open" action. The 2026-09-04 PS demo residue on win10-app was cleaned this session (guest/a0-clean-banner.ps1, verified SHOWBANNER-NOW=absent). [verified 2026-09-04]
- ETW PROXY GRANT — exit-5 was TWO stacked defects, and the 09-05 "root cause" claim is RETRACTED as the whole story: (1) ordering — the realtime logger snapshots its SD from WMI\Security[Wnode.Guid] at StartTraceW, so the grant must precede session start (fixed in agent ff1c787, still load-bearing); (2) MASK — with ordering fixed and build identity proven, the never-PLU consumer STILL died ProcessTrace ERROR_ACCESS_DENIED (2026-09-06 rig) because the granted mask was TRACELOG_ACCESS_REALTIME only, while the consumer-side realtime path implicitly issues EVENT_TRACE_CONTROL_QUERY on the session (sechost!EtwpQueryRealTimeTraceProperties, inside OpenTrace/ProcessTrace) and query is gated by WMIGUID_QUERY on the session SD (MSDN EventAccessControl: "Allows the user to query information about the trace session. Set this permission on the session's GUID"; same page documents the per-session grant as the non-PLU realtime-consume route, and PLU's own default-SD ACE carries WMIGUID_QUERY+TRACELOG_ACCESS_REALTIME — a superset). Fix: grant mask = TRACELOG_ACCESS_REALTIME|WMIGUID_QUERY (deliberately NOT PLU's controller/provider bits), harness T2 grep updated; rig confirmation pending. UNVERIFIED
- PACKAGING GAP found while wiring the bridge (P1, findings/issues.md): the user-session helpers `wgcbroker.exe` (de-slice broker) and `notifhost.exe` (this bridge) were BUILT in CI but never staged into the installable package — proven ABSENT on win11-app (base64 Test-Path; gui-agent/gui-watchdog PRESENT). So the shipped 4.3.18 de-slice-broker-default-on was INERT on every clean install (WgcBrokerActive()=false → DDA-slice fallback; win11 still renders, so it was invisible). FIXED for the full-install release: qwt-full builds+signs+exports them in helper-bins/, make-setup stages them into the setup-tree bin/ (THROWs if absent), Install-QwtImproved's bin-overlay installs them. The agent now HARD-FAILS loudly on an eligible system when the broker is expected-but-absent: `QGADESLICEDOWN` at WARNING (30s then 120s) naming packaging-gap vs launch-failure, + machine-readable `DesliceBrokerDown` registry flag (0/1/2). Owner mandate: "hard fail on eligible system", "silent fallbacks with no diag" was the worry. [verified 2026-09-04]
- Reference for every "in source" claim below: agent HEAD 45e788d = release 4.3.17 (cut 2026-09-01); CLAUDE.md's "TWO INDEPENDENT FULLSCREEN MODES" + secure-desktop sections are the owner-approved spec and win over anything older in this file. [verified 2026-09-01]
- Mode 1 (boot/shutdown/logon SCREEN) is denied UNCONDITIONALLY by PHASE + class in ShouldAcceptWindow (main.c ~3380): any fullscreen-sized window (>=99% of guest screen) is rejected when override-redirect, OR class contains "LogonUI", OR no shell window exists, OR the input desktop is secure, OR within FS_BOOT_SETTLE_MS of leaving it. No feature affects this. [verified 2026-09-01]
- Mode 2: a borderless fullscreen-sized window (no WS_CAPTION) maps only with service.gui-fullscreen on (guest registry base ShowFullscreenScreen, qubesdb wins); a WINDOWED fullscreen (WS_CAPTION — maximized normal app) is ALWAYS allowed; override-redirect fullscreen never maps, even feature-on. [verified 2026-09-01]
- Non-seamless / window 0: SetSeamlessMode refuses a switch out of seamless unless service.gui-fullscreen is on (coerces back, per-window mapping stays alive, no black screen); HARD GEOMETRY GUARD (owner 2026-08-27): guest-originated host-sized window 0 is NEVER mapped — the agent shrinks to the windowed default and defers the switch (g_NonSeamlessPending); dom0-requested sizes (g_ResolutionFromDom0) are exempt and never second-guessed; the agent echoes dom0's own w0 position back (g_ScreenWinX/Y) so the daemon resizes in place, never repositions. [verified 2026-09-01]
- Secure desktop is MODE-DEPENDENT (CLAUDE.md 2026-08-28 revision — read that version, not older ones): seamless never grants it (AddAllWindows suppressed while secure, frame path frozen, QGADESKSTUCK logged after 30 s then every 120 s); non-seamless shows the desktop secure or not; autologon is the enforced lockout remedy (LSA DefaultPassword, boot-time re-assert); UAC is an ordinary window via PromptOnSecureDesktop=0. [verified 2026-09-01]
- An already-announced window that BECOMES ineligible (e.g. animates into borderless fullscreen with the feature off) is unmapped immediately in the tracking pass, not on the next RemoveWindow — no map-then-unmap flash. [verified 2026-09-01]
- Start is NOT presented in seamless mode — user decision 2026-08-13 (agent d3deac3), SeamlessStart=0 default, deny line "Start surface not presented in seamless mode": on 25H2 it parks off-screen, morphs card<->workarea size, and its DirectComposition content cannot be captured faithfully. This SUPERSEDES the 2026-08-12 "Start clickable/movable ACHIEVED" state as the shipped default; the installer deliberately removes the Start Menu appmenu shortcut; stock Start stays not-chased (GWECK #6/#7, Open-Shell is the supported answer). Toasts are NOT affected. Re-enable: SeamlessStart=1 + docs/PLAN-start-menu.md. [verified 2026-09-01]
- Genuine-open gate: a shell-host surface (StartMenuExperienceHost/ShellExperienceHost/SearchHost) whose card measurement FINISHED and found no card is not mapped — kills the phantom wallpaper-slice windows; in-flight measurements are never rejected, so an opening menu maps as soon as its card resolves. [verified 2026-09-01]
- Toast pipeline: toasts map as override-redirect popups at their true position, cropped to the visible card by toastcrop; the card is found GEOMETRICALLY as the UNION of every descendant fully inside the raw rect AND strictly smaller in both dims (changed 2026-09-03 from "single largest such element": a WinUI menu BODY's MenuFlyoutPresenter spans the full window height so no single element is the card, and the largest strictly-smaller one is a single menu row → absurd inset the guard rejected; union = the item extent, and for a toast/Start/command-bar the content nests in one card so union == that card, unchanged) — the FlexibleToastView/ToastView class-name dependence was removed; escape hatch ToastCropDisable=1, forced insets ToastCropL/T/R/B. The 2026-08-11 "overcrops the header" claim was RETRACTED (rect was right; render judged mid-recomposition). ShellManaged (0 none / 1 all / 2 start-only WM-managed) is a registry policy; code default when unset is NONE (the perf.c comment calling start-only "default" is stale) and the installer does not seed it. [verified 2026-09-01]
- Chrome filter as shipped, all in ShouldAcceptWindow: GetShellWindow() identity reject; ATTRIBUTE-based shell-furniture reject NOREDIRECTIONBITMAP+TOOLWINDOW+!TOPMOST (agent 8ee3390, 2026-08-26 — fixes field post 85, Progman mapped as a black fullscreen window where shell identity did not match; attribute-based on purpose because GetShellWindow()==NULL at agent start on 25H2); click-through uncapturable shell overlays TRANSPARENT+NOREDIRECTIONBITMAP+TOOLWINDOW (Win11 snap/drag XAML overlays); Office shadow strips by style (LAYERED+TRANSPARENT, owned, !WS_CAPTION, !APPWINDOW, NOACTIVATE+TOOLWINDOW) AND by class (SCENIC_DROPSHADOW_WINDOW_CLASS — load-bearing, since NetUI shadows are orphaned ~117 ms after birth and strips can be unowned); DWM-cloaked folds into IsVisible. Toasts are structurally unmatchable by the furniture rule (they are TOPMOST). [verified 2026-09-01]
- Chrome acceptance caveat: real Office validated once in seamless (2026-08-07: 8 Word HWNDs -> 3 frames + 1 genuine dialog, all 4 shadow strips dropped) and RND-7/SG8 pass on chromerepro (5 HWNDs -> exactly 1 mapped), but per SG11 the safeguard is PASS-UNPROVEN — the naive-cloak fail-proof diag build is still owed, and the style rule stays deliberately narrow because a false positive silently deletes real UI ("a missing window is far worse than a spurious border"). [verified 2026-08-30]
- Popups: a popup rendering OUTSIDE its owner is CORRECT as its own override-redirect window (owner decision 2026-08-16; retracts "relax containment" — SYNTH_OVERHANG_MAX stays 12); synthesis is only for popups contained in the owner; menus are SYNTHESIZED onto the owner, not mapped as o-r popups (RND-3 falsified the older docs claim); the idle menu-dismissal bug is fixed (gated on coordsChanged: 18 -> 0 dismissals in 15 s idle, owner accepted the ~1 s settle). [verified 2026-08-30]
- Materialized WinUI menu crop (non-synthesized menus): a Win11 context menu is TWO o-r PopupWindowSiteBridge windows (command bar + body); the contained one synthesizes, the overhanging body MATERIALIZES and is captured PER-WINDOW via the WGC broker (BROKERFRAME, slice-absent) — its transparent shadow margin renders as a BLACK band. toastcrop now squares it. TWO bugs fixed 2026-09-03 to make it actually run: (a) IsMenuPopupWindow keyed on data->IsOverrideRedirect, but the crop gate lives in GetWindowData() which ZeroMemory()s the entry and only sets IsOverrideRedirect AFTER the gate (post-crop by design) → the flag was ALWAYS FALSE at the gate → menu crop NEVER ran on a single materialized menu since it was written; now keyed on CLASS+IsVisible (both set pre-gate; those classes are inherently o-r popup hosts). (b) the union card-finder above. VERIFIED win11-p2: black BAND gone. THEN made PIXEL-EXACT (2026-09-03): UIA's control-view card is only +/-1-2px accurate (left a thin edge strip + ~2-3px top gap), so the BROKER now measures the menu's true OPAQUE bbox on its full PrintWindow render (transparent margin = black) and reports it in WGCBRK_SLOT.Opaque* (init -1); the agent crops to that via BrokerOpaqueInsets (hard-validated >=50%/dim, guest shmem untrusted) instead of UIA. Menu-only L/R back-off (TOAST_CROP_MENU_BORDER) aligns the frame's outer edge to the menu's EXTERIOR edge (vertical left as measured, owner: "vertically fine"). CROP-BEFORE-SHOW = MAP-DEFER (MapDeferred/MapDeferSince/CropReadyForMap, CROP_BEFORE_SHOW_TIMEOUT_MS=400): CREATE+attach immediately, hold MAP until the crop resolves or timeout — NOT the reverted reject-in-ShouldAcceptWindow attempt (which dropped the window from the eligible set and hid the menu). Result: no shift, no strips, aligned to the exterior edge; RESIDUAL only the rounded-corner black triangles — fundamental (dom0 windows are rectangular, DWMWA_DONOTROUND no-op on WinUI), owner declined the bg-fill. [verified 2026-09-03]
- NO COMPOSITE FALLBACK ON A DIRECT-CAPABLE GUEST (owner 2026-09-06: "this tiny toast bringing fucking full desktop map is absolutely unacceptable when normal path is available, and we gated it insufficiently" / "no fallback on system that supports direct path. just no. fail hard and investigate"). The escalation ladder a per-window (slice-fed) window could take was: broker frame -> whole-desktop DDA composite slice -> legacy (dom0 composites from the window-0 grant). Rung 2 was gated on `WgcBrokerActive()`, i.e. the composite served exactly the case that most needs a hard failure - broker DOWN - so a toast rendered fine out of the desktop map and nothing said so; rung 3 was a `(void)` cast plus a LogDebug. FIXED in agent bd9f410: `DirectRequired()` (WgcBroker gate && build>=26100 && PwEnabled - ELIGIBILITY, not availability) is the single verdict; the frame-loop hold arm is gated on it (broker down -> HOLD last content + `BROKERHOLD ... brokerActive=0 ... (direct required: composite fallback refused)` at WARNING); attach/rebuild failure logs `QGADIRECTLEGACY` at WARNING; `QGADESLICEDOWN` no longer advertises the DDA fallback; the synth composite rung moved from g_DeSlice to DirectRequired() so the deliberate `WgcBroker=0` opt-out actually opts out (it is the ONE sanctioned quiet path, alongside win10/below-floor). MEASURED three-arm, same toast, corrected instrument, package agent bd9f410 on win11 26100: pre-fix 4.3.17 with NO broker present -> toast RENDERS (delta 98-100%, mean 163.6, card visually confirmed) = the fallback, and the proof the check can FAIL; fix + broker alive -> renders correctly (delta 66.7%, card visually confirmed); fix + broker killed (binary moved aside, killed BY PID - tasklist lists it as `WGCBRO~1.EXE`, so `/im wgcbroker.exe` matches nothing) -> toast maps BLACK (mean 1.3, nonblack 1.8%), stable over 6 captures, composite refused. Deliberate: the window is still MAPPED (an invisible window is the worse failure) so the hard fail is visible, black + QGADESLICEDOWN + DesliceBrokerDown. [verified 2026-09-06]
- BROKERDIMS is now self-repairing, and the WGC path can finally crop. A frame/slab dimension mismatch outliving BROKER_DIMS_STUCK_MS (750 ms) is a desync, not a resize in flight -> PwDimsStuck + PokeWindowTracking + `BROKERREREG` re-registers the slot; attached slice-fed windows never had the slot reconciliation the synth path has had since de-slice step 1, so a slot registered before a toast/menu crop resolved kept requesting the pre-crop rect and BrokerFreshFrame rejected every frame for ever. Separately, wgcbroker's WGC path published the whole texture and FrameArrived DROPPED any frame whose ContentSize != the agent's requested size - equal only for an UNCROPPED window - so a WGC-capturable window with a nonzero crop could never publish (recreate pool, drop, repeat); the pool now follows CONTENT size and PublishFrame lifts the card rect at (ReqCropX,ReqCropY), as the PrintWindow path always did. Both were survivable only because the composite quietly covered them; with the fallback gone they freeze a window, which is why they are fixed in the same change. [verified 2026-09-06]
- Slice retirement (P2, branch p2-noscreengrant, SliceRetire=1): a materialized o-r window is captured per-window via the broker (main.c:5994 BROKERFRAME) → slice-absent. The whole-desktop MONITOR composite (broker CreateForMonitor, only launchable via schtasks /ru user /it) is STILL load-bearing in two paths: synthesis copies a contained popup's pixels OUT of it (main.c:5135 BrokerMonitorFrame), and it is the MONSLICE fallback (main.c:6015) for o-r windows with no per-window frame. ALL 3 STEPS DONE 2026-09-03, shipped as DEFAULT under registry DeSlice (on when SliceRetire; DeSlice=0 = safety back to the composite): (1) synthesis sources the child's OWN per-window broker frame (PwPatchSynthChildClipped srcOrigin; the synth child is broker-registered in the UpdateWindowData synth branch, unregistered on SynthDeactivate/owner-removal) — verified SYNTHPERWIN "composite not used"; (2) every o-r surface got a BROKERFRAME (MONSLICE transitional-only, none composite-dependent); (3) DeSlice retires the composite — BrokerRegisterMonitor skipped, BrokerMonitorFrame returns FALSE (no CreateForMonitor), regular slice-fed windows also map-deferred until their first per-window frame, DDA-slice fallback skipped. VERIFIED DeSlice=1 on win11-p2: MONSLICE monAvail=0, full context menu + explorer render per-window with NO composite. The now-dead composite code (BrokerMonitorFrame/BrokerRegisterMonitor/MONSLICE) is GATED not yet deleted — delete after full acceptance confirms the de-sliced default. REMAINING GATE: full acceptance protocol (protocol/run.py, fixture install) + canonical benchmark not yet passed on the de-sliced build. [verified 2026-09-03]
- Windows/Super key: BLOCKED in seamless by default (g_BlockMenuKey=TRUE, applied as block && seamless — fullscreen mode always passes the key); dom0 unblocks via qvm-features <vm> service.enableWinKey 1, which WINS over the guest registry — required for third-party shells (Open-Shell). GWECK-STATUS #23's "now opt-in" describes a superseded intermediate state. [verified 2026-09-01]
- Guest title-bar hiding: DEFAULT ON since 2026-09-07 (owner call). The 2026-08-17 regression that disabled it was reproduced and root-caused, and BOTH halves of the old attribution were wrong: the re-map does not come from the style change (it comes from the raise-on-foreground corrective in AddAllWindows, which fired on every focus change for every window), and dom0 did not send WINDOW_FLAG_MINIMIZE at all — measured on win11-pres with debug logging confirmed active (3012 debug lines), the owner watched both restyled windows vanish while the guest reported IsIconic=False for both and there was ZERO HandleWindowFlags AND ZERO SendWindowFlags traffic. The log showed the corrective feeding itself: our map raises the window in dom0, dom0 activates it in the guest, the guest's foreground changes, the next pass re-maps — ping-ponging between exactly the two caption-less windows while the untouched one never joined. FIX (agent 5471834/a852190): the corrective is slice-era (a composite-sliced window receives the pixels of whatever covers it), so a window with its own pixels (!PwSliceFed, or slice-fed under DirectRequired) skips it entirely and composite-fed windows — win10, or broker off — keep it debounced at 750 ms; QGARAISE sent/skipped/debounced counters make it provable from a log. RE-VERIFIED on the original scenario (two caption-stripped Notepads + Explorer, 24 focus flips): QGARAISE sent=0 debounced=0 skipped=2, no repeated re-map, no window-flags traffic, all three windows still present. Still kept from before: the inset-0 own-frame discriminator (Edge/Explorer/UWP untouched), the owner-token restyle helper (per-thread impersonation does NOT work — USER32 checks the process), and the keep-managed invariant (add+verify WS_EX_APPWINDOW before stripping WS_CAPTION). [verified 2026-09-07]
- Guest drop shadows are disabled in seamless (ApplyGuestShadows; restored on the fullscreen direction). It does NOT shrink the ~7 px DWM invisible-border inset (that predicted bonus was retracted), and NetUI SCENIC shadows are the app's own — handled by the class drop, not this. TWO fixes 2026-09-02: (a) it silently NO-OPPED on cold boot — the helper borrowed the SHELL window's token (SpawnHelperAsUser), and the shell lags logon, so GetShellWindow()==NULL at seamless entry skipped it for the whole session; now spawned via the SESSION token (SpawnHelperInSession → WTSQueryUserToken, available before the shell), with a DaemonSettleSweep fallback retry (flag g_SeamlessShadowsDone) for the narrow boot window before the session token is ready (measured ~12 s on one cold boot). (b) it set only UserPreferencesMask (DWM window shadows); the CLASSIC per-window drop shadow (CS_DROPSHADOW — what MENUS/tooltips draw, surfacing to dom0 as a separate o-r shadow COMPANION window) is governed by the per-user SPI_SETDROPSHADOW, now also set in the user-token helper (SetShadowsMain). VERIFIED cold boot win11-p2: user-session UserPreferencesMask byte1 bit 0x04 cleared, and a context menu maps as ONE o-r window (was two: content + shadow). [verified 2026-09-02]
- Maximized windows are clamped to the APPLIED work area (WorkAreaGetApplied; fixes the 2026-08-07 "maximized beyond dom0 workspace" report). Known residuals from 2026-08-07: the work area is inferred/under-margined for the first ~3.5 min of a boot until dom0's feed arrives, and Explorer permanently fights the agent's work-area value (re-asserted every 2 s; converges, churns). [verified 2026-09-01]
- PARKED BY THE OWNER — GWeck's 25H2 persistent black window (decision restored 2026-09-01 after being lost in the voided window): (a) not reproducible on any rig we control (plain 25H2, OpenShell+ClassicExplorer, Win11 AppVM all render correctly) and (b) owner: tied to AppVM private-volume state. Do NOT chase; revisit only if his pwdiag.ps1 output / dom0 guid log or a reproducer arrives. Context kept: the Progman-as-black-fullscreen leg IS fixed by the furniture rule; the 2026-08-27 boot's leading inference (File Explorer PrintWindow rendering black, or a silently dead capture channel — his log era was structurally blind) stays unproven; telemetry + pwdiag.ps1 shipped since. [verified 2026-09-01]
- OPEN — field "exit status 46" on the seamless-mode switch: localized to the guest-side qrexec LAUNCH path (qrexec-wrapper reports Win32 setup failures as the exit code; measured rc=2 for a renamed-away service file); set-gui-mode.exe returns only 0/2/87 on a healthy guest; the "no interactive session" explanation is FALSIFIED (owner: an unreachable guest hangs, it does not return 46); WHICH Win32 error 46 is remains unknown — the ERROR_SHARING_PAUSED gloss is wrong (that is 70). Evidence to request: Q:\Qubes Logs\qrexec-wrapper-*.log from the failing minute. [verified 2026-08-29]
- OPEN — window tracking while capture is down (g_LocalScreenDestroyed): a window IS still announced in that state (2026-08-15 injector; the "no new window can ever appear" reading is retracted), but a lost daemon confirm still causes a >=90 s session outage; needs a bounded wait for the confirm plus a sweep. Not fixed. [verified 2026-08-15]
- The 24H2 "ghost windows" defect is RETRACTED WITH POSITIVE EVIDENCE: dom0 was right — the dismissed-looking Start/toast were really on the guest screen. Guest EnumWindows (and ANY qrexec-launched probe) is BLIND to shell surfaces (input-desktop attach fails), so never conclude "the guest doesn't have it" from a guest window list; guest truth for "is it displayed" = PIXELS; rendercheck's ghosts_in_dom0 is a hint only. Companion trap: two same-resolution guests place toasts at identical spots — check the OTHER VM's window list before calling any dom0 window a ghost. [verified 2026-08-11]
- Caption/managed-state facts: WS_CAPTION or (WS_SYSMENU + WS_EX_APPWINDOW) keeps a window dom0-managed — "removing the caption unmanages it" was too strong; dom0-initiated close survives WS_SYSMENU stripping; Edge/Chromium draw their controls as client content, so no global caption rule can remove them; "caption strip kills Edge" is a single unreproduced observation — do not repeat as fact. [verified 2026-08-16]
- Fullscreen-flash history is SUPERSEDED by the Mode 1 clause: the flash was per-window LogonUI (not window 0 / SetSeamlessMode-at-shutdown, not Progman-by-class, not gui-emulated, not g_SeamlessMode defaults — all earlier mechanisms dead); the later "splash is back" report was a stale deployed agent on an AppVM plus a genuinely stuck AppVM autologon (I3), not a regression. [verified 2026-08-20]
- RULE: never set service.gui-fullscreen on any guest and never signal the mode-switch events — feature-ON SG arms and any windowed-desktop exercise are owner-attended, ask first; the once-claimed service.gui-windowed-desktop split flag was never written, ONE feature gates both the windowed desktop and borderless fullscreen ("do not complicate the controls"; README table is the spec and wins over code). [verified 2026-09-01]
- RULE: never weaken daemon-side bordering — the fix is always to stop presenting chrome fragments as windows, never to let the guest opt out of borders. [verified 2026-09-01]
- TRAP — configuration errors fake product failures: compound-window/chrome/per-window checks are meaningful ONLY in seamless mode (fullscreen = one dom0 window, nothing to count); resize batteries only in the fullscreen/T2 config (seamless forces host resolution, every resize "fails"); shell-UI checks must run as the INTERACTIVE user (qrexec/session-0 launches see no shell surfaces and silently prove nothing). Establish the configuration before reporting any FAIL. [verified 2026-08-07]
- Fault-injection bits exist for every windowing gate (FI_GATE_MODE1/MODE2/START/NOCARD/SHELLOVERLAY, plus FI_DROP_*) so acceptance checks can be seen to fail on one binary; FiGateOff compiles to constant FALSE in release builds. MSG_CROSSING now has a real handler (HandleCrossing) — the old "unknown msg type 127, benign" note is obsolete. [verified 2026-09-01]
- Acceptance state per docs/ACCEPTANCE-4.3.16.md (retained campaign record): SG1 Mode-1 hold across a proven cold boot with a real vacuity guard; SG2/SG4 exercised at 1024x768 containment with their deny discriminators; SG3 windowed-fullscreen maps 6/6; SG9 Start denied x25 with the documented line — all PASS-UNPROVEN per SG11 (feature-ON and owner-attended arms owed; SG10 shell identity/furniture not run). [verified 2026-08-30]
- Parked by the owner, not open work: distinguishing a genuine lock/UAC LogonUI (persists, awaits input) to log/report it as a bug; suppressing the residual small normal boot window ("probably not now"). [verified 2026-08-19]

## MENU MAP-HOLD (crop-before-show) - measured and materially reduced, 2026-09-11

THE INSTRUMENT CAME FIRST, and it changed the diagnosis twice. Menus emitted NO timing line at
all (PwNoteSliceFedMap only fires for PwSliceFed windows), so the hold was real but unmeasurable;
QGAHELDMAP now logs held_ms + reason for EVERY deferred window. Two harness defects had to be
fixed before any number was trustworthy: the stimulus dismissed each menu the instant its HWND
appeared - i.e. while still deferred - so the window died unmapped (9 opens, 1 map, 0 samples);
and samples cannot be matched by HWND, because a WinUI menu is a PAIR of windows and a stimulus
only sees the site bridge. Attribution is by the log line's own menu= flag.

BASELINE (8 opens, instrumented 4.3.28): held_ms 656/703/703/719/719/719/719/734, median 719,
7 of 8 at or above the 700 ms CROP_BEFORE_SHOW_TIMEOUT_MS ceiling - i.e. released by TIMEOUT and
therefore mapped UNCROPPED, with the shadow strip the hold exists to remove. Worst of both.

TWO ROOT CAUSES, both now fixed:
1. THE CROP CACHE COULD NEVER HIT FOR A MENU. It was keyed on (HWND, size), and a WinUI context
   menu is created fresh on every open, so the key was new every time and every menu paid a full
   UIA measurement. The insets are a property of CLASS+SIZE, not the instance - measured
   identically on every repeat: 287x279 -> 16/6/16/22, 287x68 -> 10/2/10/4 - so menus are keyed
   by class+size. Fresh measurements per 8 opens fell from 10 to 3.
2. A DEFERRED MAP WAS ONLY RE-EXAMINED AT THE CEILING. The release happens on the tracking pass,
   and the wake was armed for MapDeferSince + CROP_BEFORE_SHOW_TIMEOUT_MS, so a crop that was
   ready early was simply not looked at until the bound expired. Re-check is now a 32 ms tick;
   the ceiling remains a hard bound instead of the normal case.

AFTER (same harness, same guest, binary verified by hash): held_ms 32/63/312/546/609/609/750/812,
median 577 (was 719), min 32 (was 656), and 7 of 8 released by CROP - menus now map CORRECTLY
CROPPED, which is the visible artefact fixed. The shadow-strip defect is gone.

RESIDUAL FOUND AND FIXED - it was a third bug, not scheduling. The defer-time size was
instrumented against the release-time size (QGAHELDDEFER vs QGAHELDMAP) and settled it at once:
the slow menus were DEFERRED at raw 287x328 and RELEASED at cropped 267x259, while the one fast
window (47 ms) was released at its RAW size. Applying a crop REPLACES data->Width/Height with the
cropped size, and the cache is keyed on the size - so the moment a window had been cropped, every
later CropPending lookup keyed on the CROPPED size, missed the entry stored under the RAW size,
and reported the crop as still pending. The map then held to the ceiling although the measurement
had resolved long before. CropPending now reconstructs the raw size from the stored insets
(Width + CropLeft + CropRight) and finds its entry.

FINAL (same harness, same guest, binary verified by hash):
    held_ms 15/32/32/47/47/47/47/468   median 47 (was 719)   min 15 (was 656)
    0 of 8 at the ceiling (was 7 of 8)
    8 of 8 released BY CROP (was 0 of 8 - every menu used to time out and map UNCROPPED)
Median menu hold is down ~15x and the uncropped-shadow artefact is gone: menus now map cropped,
first time. The single 468 ms sample is the first menu of a new shape paying its one real
measurement, which is expected and not removable - every repeat of that shape is then ~47 ms.

THE THREE BUGS, none of which was the thing that looked obvious at the start:
  1. the crop cache could never hit for a menu (keyed on a per-open HWND);
  2. a deferred map was only re-examined at the 700 ms ceiling, never when the crop became ready;
  3. applying the crop changed the cache key, so a cropped window could never find its own entry.
NOT YET RE-ACCEPTED: these are shipping agent changes and the full campaign has not been re-run
on them.

## MENU RENDER - WHERE THE TIME ACTUALLY GOES (measured 2026-09-11, owner: "still quite slow on render")

The crop work is done; the remaining latency is NOT the crop. Full timeline for one menu, from
the agent's own ticks (QGAHELDDEFER t=, QGABROKERREG t=, QGASLICECONTENT t=):

    defer -> broker register : 531 ms   <-- the dominant cost, and it is AGENT-SIDE
    register -> first PAINTED:  125 ms   <-- the broker is not the problem

So a menu spends half a second between being deferred and being handed to the capture broker.
The broker's idle reconcile is 250 ms, but the agent already SetEvent()s its control handle on
register, so that is not the gate - the gate is when the agent's own tracking pass gets round to
registering the window. THAT is the next thing to attack, not the broker.

AND THE WINDOW RE-REGISTERS, WHICH IS THE VISIBLE "SETTLING". The same hwnd registered THREE
times in 80 ms at oscillating sizes: 267x259, 263x251, 267x259. Two different inset sets are in
play - the UIA measurement and the broker's pixel-exact opaque bounds disagree (20/69 vs 24/77 on
a 287x328 raw window) - so whichever answers first wins that pass and the geometry flips when the
other lands. Each flip re-registers the broker slot. This is pre-existing but was invisible while
every menu sat at the 700 ms ceiling; it is what the owner sees as the window settling slowly.

STATE AFTER THE RENDER-THEN-MAP WORK (same harness/guest, binary hash-verified):
    held_ms 203/203/250/296/422/500/656/1156, median 359 (baseline 719)
    mapped UNPAINTED: 0 of 8   (was 8 of 8 after the first speedup - the black blink)
    released by crop: 8 of 8   (baseline 0 of 8 - every menu used to time out and map uncropped)
Correctness is fixed and the artificial ceiling is gone. Speed is NOT yet good, and the two
named causes above are why.

## MENU LATENCY - VALIDATED NUMBERS (2026-09-11), and a harness defect that invalidated the earlier ones

INSTRUMENT DEFECT FOUND FIRST. The harness grepped the whole agent log, which persists across
runs within one agent instance, so each run counted every menu since the agent started: three
consecutive 8-open runs reported 14, 22 and 30 crop-releases. Every median quoted from this
harness before this point blended runs together - mine included. It now records the count of
timing lines BEFORE the stimulus and reads only what appears after; a run reporting 7-8 lines for
8 opens is the sanity check.

VALIDATED, three runs on one unchanged binary (the project's own rule, which I had been
violating by comparing single runs):
    medians 422 / 453 / 453 ms, spread ~31 ms - so the instrument is stable at this sample size
    min 204-312, max 563-813
    mapped UNPAINTED: 0 every run          released by crop: 100% every run
Against a baseline of ~719 ms median where every menu timed out and mapped UNCROPPED.

WHAT IS FIXED: the black flash (0 unpainted), the shadow-strip artefact (100% cropped), the
700 ms ceiling as the normal path, and the 250 ms retry grid.

WHAT REMAINS, measured and named:
 - [verified 2026-09-11] No broker->agent "frame published" event exists in the ABI at all. The agent notices a
   published frame only on its next desktop-capture pass, and if that frame hashes as redundant
   the walk is skipped entirely - so a painted menu can sit unnoticed on a static desktop. Worth
   ~125 ms and removes a real correctness hole. This is the next thing to build.
 - [verified 2026-09-11] Two inset authorities disagree (broker pixel-exact 20/69 vs UIA 24/77) and
   whichever answers first wins the pass; when the other lands the geometry flips and the broker
   slot is re-registered, costing a fresh first-frame round trip. Pick the pixel-exact one for
   menus and never let the estimate override it.
Realistic floor once those land is the guest's own render plus one frame - roughly 100-150 ms.
