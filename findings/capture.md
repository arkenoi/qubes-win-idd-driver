# capture — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- Architecture as shipped: per-window capture is the Qubes GUI protocol's NATIVE path (the Linux-agent model) — each accepted window gets a page-aligned BGRA buffer granted read-only via `XcGnttabPermitForeignAccess2` + its own `MSG_WINDOW_DUMP`; gui-daemon's dual composition path (own shmseg, else screen-slice) is used UNMODIFIED, byte-identical between master and release4.3; the legacy window-0 screen-slice path is fully retained as per-window fallback (attach failure, capture-thread death, protocol < 0x00010007). No daemon or protocol change was needed. [verified 2026-08-01]
- WGC BROKER (P8) VIABLE ON WIN11 — RAN 2026-09-02, win11-p2 build 26100/24H2, compiled tools/wgcprobe (agent branch p2-noscreengrant). SYSTEM/session-0 (agent context, and NOTE this testbed's SYSTEM is already WinSta0/Default/interactive=1): IsSupported threw 0x80070424 (service-context refusal, NOT the older 0x8007000E) → dead. Interactive user (schtasks /ru user /it): IsSupported=1, frameArrived=1, nonBlank=1 (626×473, 16 colors), brokerViable=TRUE. So WGC is refused by the SYSTEM TOKEN, not the window station — a user-session broker is the fix (matches MS guidance + OBS/Chrome-Remote-Desktop). Both win11-critical gates GREEN on 24H2: border disables cleanly (borderDisableOk=true hr0) and DirtyRegions/DirtyRegionMode APIs present. NOT YET PROVEN: capture of a STRANDED class (probe captured its own normal gradient window, not an NRB toast/o-r menu/ULW — the classes that justify the broker), the broker→agent frame IPC, per-window dirty-region behaviour, capture cost vs 40ms PrintWindow, logoff/secure-desktop lifecycle. This is a win11-ONLY path (border unremovable on win10 19045, no DirtyRegions). [verified 2026-09-02]
- WGC per-HWND capture SPLITS by class (P8 PASS A/B, win11-p2/26100, user session): CreateForWindow captures real app windows incl. NRB — Calculator ApplicationFrameWindow (ex=0x00200100) nonBlank 16 colors — but FAILS on shell CoreWindows with E_INVALIDARG (0x80070057): the TOAST (ShellExperienceHost Windows.UI.Core.CoreWindow 396×369, found via HWND brute-force scan since EnumWindows can't see it), Start (StartMenuExperienceHost), and Search (SearchHost) all refused. So the broker's per-HWND model handles UWP/NRB apps + (expected) o-r menus, but the TOAST — must-keep — is NOT per-HWND capturable and needs WGC CreateForMonitor OR the existing DDA slice. WGC CreateForMonitor from the user session CONFIRMED working (frameArrived, nonBlank, content=5120×1440, border-off ok) — so the broker's toast route is: capture the whole monitor via WGC in the user session, slice the toast rect out. Broker architecture pinned: per-HWND WGC for app/UWP/NRB/o-r-menu windows + ONE monitor capture for shell CoreWindows (toasts/Start/Search). The monitor capture is a user-session WGC replacement for the SYSTEM-side DDA mirror; DDA can be dropped on 24H2+ once the broker supplies both, or kept as fallback in the minimal first cut. PHASE-1 PROBES COMPLETE. [verified 2026-09-02] 
- WGC BROKER IMPLEMENTED + TESTED END-TO-END, incl. occlusion A/B (2026-09-02, win11-p2/26100; code on branch p2-noscreengrant, DESIGN-wgc-broker.md; experimental, gate WgcBroker + build>=26100, default OFF). tools/wgcbroker = user-session WGC capture (SYSTEM agent cannot activate WGC, 0x80070424) -> Global\ shared section (SYSTEM-created, SDDL SY-full/IU-rw/ME-NW) -> agent bounds-checked seqlock consumer -> existing granted per-window buffer -> dom0. Agent 2a (section+spawn+supervise) stable: broker launches ONCE, heartbeats, no churn, rendering unaffected. 2b consumer: Calculator (NRB ApplicationFrameWindow, DirectComposition content PrintWindow CANNOT capture) rendered full LIVE UWP content via the broker ("BROKERFRAME first WGC frame consumed ... 1202x934"). OCCLUSION A/B (value-over-slice, seen-to-fail control): Calculator occluded by Notepad in-guest -> broker ON renders Calculator CLEAN; broker OFF shows Notepad BLEEDING into Calculator's window from the composited slice. Scope = OCCLUDED NRB app windows only (NOT toasts/o-r/CoreWindows: CreateForWindow E_INVALIDARG on shell CoreWindows; toasts go to the interceptor). Security: every broker-written field bounds-checked before use (user-IL process shares the section) so no OOB into the granted slab; broker only sees the user's own windows (NOT an isolation change). Fallback to slice on any miss (never blank). OWED: clean cold-boot acceptance (verified via live restart - win11 flaky-autologon stalls the heavy e2e harness at bootwait; INLINE bounded qtest calls are reliable), broker-death-fallback + secure-desktop-pause live checks, DirtyRegions consume, toast interceptor, o-r-menu WGC probe for FULL slicer retirement (north star). [verified 2026-09-02]
- TOAST INTERCEPTOR WORKS (2026-09-02, win11-p2): tools/notifhost (user-session, UserNotificationListener - access proven Allowed, no consent prompt) renders each incoming toast as a NORMAL bordered GDI window (WS_OVERLAPPED|WS_CAPTION) - dom0 borders it as guest content, the Linux notification-daemon model. Proven: fired "QWT NOTIF TWO" -> a bordered "Notification" window with title+body appeared and was captured via the normal per-window path (no slice, no broker needed for it). This is what lets the TOAST class retire off the slicer. Cosmetic polish owed: narrow card truncates text (widen + word-wrap), notifhost is a console app so it shows a stray terminal window (make it windowed/no-console), test-toast stray-quote fidelity. NOT dom0-forwarding (in-guest render only; the "notification bridge" to dom0 stays future/forbidden). Native-toast suppression (deny the NRB toast when the interceptor is active) + agent spawn are the wiring for full retirement. [verified 2026-09-02]
- Capture engine is PrintWindow(PW_RENDERFULLCONTENT), NOT WGC: WGC cannot activate in the agent's SYSTEM/session-1 context (IsSupported 0x8007000E), had no DirtyRegions API on 19044, and showed intermittent session creation incl. opened=0 — any WGC revisit must survive total absence. PrintWindow is byte-correct on occluded windows (DWM redirection surfaces; Gate-0 proven with a failing negative control). IsHungAppWindow is checked before every PrintWindow (a hung app would park the capture thread inside the engine lock). DESIGN-per-window-capture.md §3 cost numbers measure WGC and do NOT describe the shipped engine; wincapture.cpp/perwindow.h headers still say "WGC" — stale, the engine is PrintWindow polling. [verified 2026-08-09]
- PURE per-window model (owner-pinned goal: window = bare granted bitmap, dom0 owns compositing, damage = whole-window ticks, NO desktop grant in seamless) — feasibility study concluded 2026-09-02, verdict FEASIBLE-WITH-CONCESSIONS, full writeup DESIGN-pure-per-window.md. Confirmed: gui-daemon needs no screen window at all (g->screen_window NULL-guarded everywhere, missing-image damage is a safe no-op — xside.c:733/2352-2367), the Linux agent IS the model (per-window composite pixmaps, no root grant, but FINE damage rects — whole-window ticks are coarser than the reference), and un-granting the desktop is nearly surgical under staging (every in-agent pixel consumer already reads the staging buffer; skip the grant in StagingEnsure + suppress SendScreenGrants in seamless) — which alone closes the 7200-page staging-grant leak AND stops the redundant whole-desktop-into-dom0-grant copy incl. secure-desktop pixels (caveat: grants irrevocable, so first non-seamless entry grants for process lifetime). OWNER RULING 2026-09-02: this is a TIDINESS/correctness win, NOT a security/isolation win — the qube boundary and gui-daemon trust model are unchanged (dom0 already receives guest window content; a hostile guest grants what it likes regardless). Never call P2 a "security win". Literal zero-machinery is UNREACHABLE: no out-of-process per-window paint signal exists on Windows, WGC is dead from SYSTEM/session-1 and border-unremovable on 19045 (Win11-broker upgrade path only, 24H2 adds DirtyRegions), and o-r/NRB/ULW classes incl. toasts have no pixel source but the composited desktop. Adversary-forced minimum shape = "shipped hybrid minus desktop grant": DDA stays as a LOCAL UNGRANTED oracle+mirror, DDA-owned foreground channel stays (≤~10 ms/tick budget to hold disjoint-below-stock scroll is blown at ~0.9-2.9 Mpx by whole-window PrintWindow), drag dirt-attribution stays (else windows beneath a drag get whole-window ticks at oracle rate), a slow bounded repair tick replaces the sweep (hash soundness depends on it), menu synthesis + toast move-refusal stay (owner decisions). Decisive probes serialized in the doc; P2 (ungranted staging) runs FIRST — its payoff survives every refutation and justifies shipping independently. [verified 2026-09-02]
- P2 probe RAN and PASSED 2026-09-02 (doc §6 has the full record): `SeamlessNoScreenGrant` flag build (agent p2/noscreengrant, 6533765) on StandaloneVM `win10-p2` — flag-on ×3 cold boots renders pixel-identical to the flag-off control (2 boots 0-px diff, 1 boot a 1-px hairline, below the granted arms' own 0.15-0.24% boot variation), UNGRANTED/suppression markers present, granted markers return when the flag is removed on the same binary, zero attach failures/fatal markers. Seamless needs NO desktop grant on this rig. Unproven by this run: toast VISUAL rendering (surface absent from per-window capture in every arm incl. baseline — instrument limitation), dom0-side grant-table absence (agent-side evidence only), and performance (P3). Ship-blockers if promoted beyond probe: PwForceLegacy fallback reroute to sliceFed, protocol ≥1.7 gate, grant-on-demand for non-seamless. TRAP burned into the harness: an AppVM cold boot reverts Program Files AND HKLM (root volume = template), so binary/registry cold-boot tests are valid ONLY on a standalone — run 1 on win10-app measured stock 3 arms straight; the rerun hash-gates every boot. [verified 2026-09-02]
- P1 (PrintWindow area scaling) + P3 (canonical bench, grant off vs on) RAN 2026-09-02 on win10-p2 (design doc §6). P1 CPU-ms/whole-window-call: 0.48Mpx→2.7, 1.02→1.6, 2.07→12.9, 3.69→14.5, maximized 7.13Mpx→40.2 (wall p50 88ms; 800×600 wall 16ms validates vs the 15-18ms anchor). The ≤10ms/tick disjoint-below-stock bar is crossed between 1-2 Mpx (any maximized window) → whole-window PrintWindow CANNOT feed the drag/scroll/typing hot path; the DDA-owned foreground sub-rect channel (~121µs) is MANDATORY in the pure model, not the conditional concession the architect first called it. Measured 40ms is ~half the 80-96ms linear extrapolation (extrap was pessimistic, ~5.6ms/Mpx) but still 4× the bar. P3 = PASS/perf-neutral: flag off vs on, one binary, cold-boot+marker per side, 3 interleaved rounds — drag 14.6/15.1, scroll 3.2/3.4, typing 2.8/3.4, idle 0.5/0.4, all ranges overlapping, within-side variance > any off/on gap, both far below stock. P2 grant removal costs no measurable per-frame CPU. Win11 arm (win11-p2) RAN 2026-09-02: P2 PASS identical (ungranted markers on 3 boots, control reverts, no fatal). P1 SUBSTANTIALLY WORSE than win10 — whole-window PrintWindow CPU/call (probe+target) 0.48Mpx 12.5, 1.02 21.9, 2.07 30.9, 3.69 38.7, max7.13 66.8 (win10 had ~0 target cost; win11 pulls the target DWM path in), wall ~2-2.5× win10; the ≤10ms bar is blown at the SMALLEST size → DDA-owned channel even more mandatory on win11, and this is why WGC broker is a win11-specific future play. P3 win11: drag perf-neutral (15.3/14.6 clean), but scroll/type/idle too noisy at N=3 — the apparent scroll off/on non-overlap (1.5 vs 3.4) is a phase-attribution artifact (win11 DDA over-report smears CPU into idle-mid, off-side idle-mid spikes 4.3/5.0/12.8 hold the missing scroll CPU; P2 cannot change scroll cost mechanistically), REJECTED as a real regression. Win11 needs more rounds for a clean scroll/type disjointness verdict. P1 instrument traps (all burned in): run as interactive user via schtasks /ru user /it (session-0 qrun → 0×0 rect), find Notepad via Get-Process MainWindowHandle NOT FindWindowW(cls,$null) (PS marshals $null as empty-title match → hwnd 0), write to SYSTEM-precreated C:\Users\Public (non-admin task can't mkdir under C:\), flush results per-size (large sizes take minutes, single final write loses the poll race). [verified 2026-09-02]
- Path taxonomy (read from source, previously muddled): PwWindowEligible=FALSE (override-redirect, WS_EX_NOREDIRECTIONBITMAP, ULW-layered — GetLayeredWindowAttributes FAILS on ULW and PrintWindow returns premultiplied near-black — or colorkey) → slice-fed from the persistently-granted DDA desktop framebuffer, engine never involved (occlusion caveat: slice content includes whatever covers the window in the guest; acceptable, these are topmost). Eligible → PrintWindow engine channel, but DDA-OWNED while foreground+unoccluded (engine must not touch the buffer; DDA slices feed it); the engine sweep covers guest-occluded windows. Eligibility is re-checked on ExStyle transitions (runtime PwForceLegacy). [verified 2026-08-27]
- RETRACTION GUARD: "the per-window fixes are unreachable at the shipped default because the guest ships `PerWindowCapture=0`" was RETRACTED — the code default in `PwInit()` is 1 (ON), nothing in the install path writes the value, and the test guest's 0 was our own A/B artifact. Per-window capture IS the shipped default path. [verified 2026-08-04]
- DDA facts: `DesktopImageInSystemMemory` is TRUE on this stack (retail 19045 baseline 3/3, pitch tight — 13760==3440*4, 5600==1400*4 — and it stayed TRUE across mode changes), and an IDD-solo desktop (VGA disabled) also kept capture alive with RecreateDuplication recovering keyed-mutex abandons — capture.c's hard-fail constraint is satisfied on both adapters as measured. `GetFrameMoveRects` was EMPTY on every one of 300 drag frames — never build on move-rects. Trap: ddaprobe's process exit code never consumes the sysmem flag (exits 0 whenever duplication succeeds) — judge the JSON summary, never the exit code. [verified 2026-08-15]
- The engine's 250 ms round-robin sweep is DDA-EXEMPT (per-channel `ddaOwned` via `WcSetDdaOwned`, registry `SweepDdaExempt` default 1, marker file for one-binary A/B): pre-fix it PrintWindow-captured the DDA-owned foreground window 4x/s forever — ~2.5 CPU points of standing idle burn (the entire "typing regression", which was an idle floor, not typing) AND a correctness bug (could overwrite DDA-established content and fire spurious damage). Idle-floor recovery is a canonical-suite question (tools/bench-stock-vs-ours.sh), not re-established here. [verified 2026-08-09]
- Per-window grant accounting is SLAB-POOLED (agent 100a9bb): references scale with peak concurrent windows, with a ~2 s quarantine before slab reuse (123 attaches → 5 slabs; the last 80 attaches granted nothing). Pre-fix shape, kept as the seen-to-fail record: xeniface release builds NEVER revoke (the only RevokeForeignAccess call sits inside an ASSERT compiled out — proven by disassembly of our vendored MSI); ~3819 refs leaked per window APPEARANCE; pool exhausted at ~272 attaches; the degraded state is INVISIBLE (silent fallback to screen-slice, everything renders); the NEXT agent restart then kills the GUI outright (ring grant fails 0x5aa, dom0 window list empty, only a qube restart recovers). True Xen grant ceiling was never located (≥64 windows / ~908 MB granted once, guest-side only; dom0 map cost unmeasured). [verified 2026-08-16]
- OPEN DEFECT — staging grant per agent START: the desktop staging grant costs 7200 pages per agent start and is never reclaimed (~144 restarts to exhaustion, measured separately from the per-window leak). Needs an owner that outlives the agent process — a holder service, or the IddCx driver granting its own framebuffer (CLAUDE.md Phase 1B Outcome B territory). [verified 2026-08-16]
- OPEN DESIGN ITEM — leak-free exit: a graceful stop still cannot guarantee revocation while the daemon maps the pages (vchan already closed, no unmap/destroy handshake reaches dom0); the fix is an ack-gated revoke handshake — Phase 3, design writeup + owner approval first. The shipped exit path is settle-then-single-pass (1 s settle, ONE revoke attempt per grant, loud abandonment; PwShutdown same) — revoke RETRY loops are forbidden: the 100 ms retry loop rolled the xenbus revoke-vs-unmap race ~20x per exit and wedged the guest during OS shutdown. NEVEREXIT audit: remaining fatal exits are only dead-vchan and QGA_SHUTDOWN; capture-init exhaustion degrades to A7DEGRADED (retry every 5 s forever, vchan stays serviced); vchan desync stays fatal DELIBERATELY (input-integrity boundary — a desynced stream re-parsed could synthesize input). [verified 2026-08-05]
- OPEN DEFECT — unbounded vchan writes: `VchanSendBuffer` spins forever (Sleep(1), no is_open check, qubes-windows-utils vchan-common.c:100) when the daemon dies with a full write ring, so a graceful stop can stall indefinitely; and a per-window attach/detach flip storm (Start-toggle storm: ~1 grant-rebuild cycle/s) can fill the vchan, block the main pump mid-write, and kill the capture thread with no recovery (the S1b wedge; H1 flood-first vs H2 dialog-first causality never settled). No bounded-write fix is recorded here — re-verify live before treating it as either open or fixed. [verified 2026-08-11]
- Capture gate/restart: a capture error sends DESTROY(0) and discards all window events until the daemon confirms — a gate that never opens used to be a silent dead qube (agent sat forever). FIXED and proven by fault injection (`CaptureGateFaultInject`): 15 s confirm deadline, re-send twice, then exit for watchdog respawn (~19 s to recovery vs never); plus a first-client-ever marker so a respawn after a lost session says "this guest HAS had a daemon before" instead of blaming the gui feature. The other half is upstream and stays on the reportable list (CLAUDE.md exception): gui-daemon `handle_vchan_error` never consults `vchan_at_eof` (write-path disconnect skips its own restart), plus the restart_guid use-after-free — our agent cannot restore a dead daemon, only fail loudly and fast. [verified 2026-08-16]
- 0x887a0026 (keyed mutex abandoned) fires routinely on resolution/seamless transitions and `RecreateDuplication` recovers it in place, windows kept — CLAUDE.md's "capture thread dies" description is pre-fork behavior. Standing rule from a measured failure: any transition-completion hook must live on the FRAME path, never in StartFrameProcessing — recovery via RecreateDuplication never passes through StartFrameProcessing (the first non-seamless shrink hook shipped there and silently never completed). [verified 2026-08-27]
- Secure desktop: MODE-DEPENDENT per CLAUDE.md's 2026-08-28 revision (seamless: frame path freezes in ProcessNewFrame while the input desktop is not Default, QGADESKSTUCK logs a persistent freeze; non-seamless: desktop shown regardless — it is one bounded window). `EnsureOnInputDesktop()` runs at the TOP of ProcessNewFrame, BEFORE the freeze early-return (agent fb4c1cd), so the on-secure-desktop latch always clears itself — never gate the code path that clears `g_OnSecureDesktop` behind that same flag (the v3 iteration did, latched on Winlogon forever, and was strictly worse than the original bug; recovery demonstrated live). `PromptOnSecureDesktop=0` is the shipped installer default: UAC consent arrives as a normal window and the owner's dom0 click drives it (UIPI blocks synthesized guest input, not the daemon-delivered user's). [verified 2026-08-28]
- Black-fullscreen-window class (GWeck, forum 42717): TWO theories were RETRACTED and must not be re-derived — (1) "the ShouldAcceptWindow filter gap lets Progman through" (the predicate never ran on it) and (2) "Winlogon EnumWindows 0x12A means the agent never re-enumerates" (the race is real and logged, 0x12A is spurious/retryable, but the agent recovered and mapped fine; whether the shipped EnsureOnInputDesktop resync fully covers desktop-transition re-enumeration is not recorded). The defect state was reproduced ON DEMAND via the `DiagWindowFilterOff` diagnostic bitmask and closed by the attribute predicate NOREDIRECTIONBITMAP && TOOLWINDOW && !TOPMOST (agent 8ee3390) — rejects Progman-class shell furniture, structurally cannot match a toast (toasts are TOPMOST|NOREDIRECTIONBITMAP CoreWindows, kept per CLAUDE.md 2A-chrome 3c); the field root-cause record ties the class to secure-desktop surfaces mapped while the frame pipeline is frozen (CLAUDE.md: the dimming backdrop WAS GWeck's black window). "Fixes GWeck's bug" stays UNCLAIMED without his field confirmation, and the second filter leg's failure cause on his rig (feature on vs IDD-inflated desktop bounding box vs pre-xconf g_ScreenWidth=0) is still unknown — the chase itself is OWNER-PARKED (not reproducible; owner: tied to AppVM private-volume state). Separate caution: a DWM_CLOAKED_SHELL fullscreen ApplicationFrameWindow was once seen mapped as a blank fullscreen rectangle (IDD-independent), last recorded here as unfixed — blanket cloak filters are banned (cloaked toasts must be kept); check the current predicate before re-deriving. [verified 2026-08-27]
- Engine telemetry ships (agent e58274d) and is proven by fault injection: WCBLACK latches after 3 consecutive ≥99%-near-black successful captures (1/64 sampling, <0x0C/channel; real windows measure ≤4%), WCDEAD on 5 consecutive capture failures (proven via FI_PRINTWINDOW_FAIL); WCBLACK-vs-WCDEAD distinguishes renders-black from capture-dead in a field log. Default logs are QUIET (QGAPERF default OFF, SYNTHPAINT gated on ProtoTrace); `qvm-features <vm> service.gui-agent-debug 1` is the ONE dom0 switch for per-frame perf + proto traces + Debug level, dom0 wins both directions. [verified 2026-08-27]
- STANDING TRAP — user-context probes do NOT predict the agent's SYSTEM/session-1 capture behavior (WGC worked as user, dead as SYSTEM; Chromium bubble PrintWindow perfect as user, BLANK as SYSTEM — and blank row-diffs as "no change", i.e. a healthy-looking channel showing black forever). dom0-delivered pixels are the authoritative verdict; pwdiag carries this caveat in its header. [verified 2026-08-27]
- STANDING TRAP — `entry->X/Y/Width/Height` is not just reported geometry: perwindow.c:305 derives the per-window CAPTURE CROP from it, so clamping it (e.g. to the work area) crops the window's own content — the work-area maximize clamp shipped, cut 64 px of title+menu bar off maximized windows, and was RETRACTED/reverted; only the screen-bounds clamp is safe (it trims exactly the invisible resize border). Maximized-window overflow is a WORK-AREA problem, never a geometry-reporting problem. [verified 2026-08-07]
- Locking/ordering rules bought with daemon kills: damage-vs-DESTROY is linearized on the vchan lock via a 64-entry recently-destroyed-hwnd ring (SHMIMAGE after DESTROY = gui-daemon exit(1)); NEVER hold g_csWatchedWindows across a vchan send — the main thread dispatches inbound messages while holding the vchan CS, so that order is an ABBA deadlock; window-0 damage after the screen-window DESTROY is gated by g_LocalScreenDestroyed; and a MSG_CONFIGURE ack must BYTE-ECHO the daemon's values (anything else re-arms have_queued_configure → configure livelock at vchan speed, ~1000/s observed). [verified 2026-08-01]
- Screenshot tooling: dom0 `local.WinScreenshot` (qtest shot) does NOT capture override-redirect windows; `qtest fullshot` returns the whole dom0 desktop PLUS a geometry table whose `mapped` column is the ground truth for what dom0 shows — it is restricted to o-r surfaces and dom0-compositing checks, never an escalation from an empty per-window tar. An EMPTY capture is ambiguous BOTH ways (zero windows vs failed service/missing/untagged target) — decide on a positive assertion, triage per docs/ACCEPTANCE-PROTOCOL.md H3.2. Check the END STATE first (what is mapped, what is on screen) before reasoning from log fragments — three wrong black-window theories in one day came from skipping this. [verified 2026-08-24]
- CAPTURE-HYGIENE RULE (binding, docs/ACCEPTANCE-PROTOCOL.md RND-0/RND-0b + D10): per-window capture is the accepted class; whole-desktop captures are processed locally, cropped immediately, and DELETED in the same step that reads them — never committed, not even inside a tar (a name rule cannot see into an archive); `tools/pre-commit-no-desktop-captures.sh` + the enabled `.githooks/pre-commit` screen commits. Guest-side surface existence is proven with EnumWindows (CharSet.Unicode), not with the capture. [verified 2026-09-01]
- qrexec runs in session 0, which does not composite: toasts/Start/shell stimuli and window probes fired there prove nothing (FindWindow from the qrexec context is desktop-blind) — drive such cells as the interactive user (`schtasks /ru user /it`); per the acceptance protocol a stimulus fired as SYSTEM makes the cell INVALID-VACUOUS regardless of what the capture shows. [verified 2026-08-27]
- Never benchmark or judge captures against a window whose content is not verified rendering: a wedged white-body Notepad (survived agent restarts AND build swaps — guest/window-side state) faked a frame-delivery regression for an afternoon. If a window goes empty: guest-side screenshot first, winenum on it, and no drag verdicts against it. [verified 2026-08-12]
- The 2026-08-28 "sign-in lockout MEASURED" run is annotated UNPROVEN (audit 2026-08-29: the verdict rested on a 0-byte qtest shot tar read as "no windows", n=1, no positive control — exactly the empty-capture trap above). The lockout itself is treated as real (field reports; CLAUDE.md enforces autologon as the mitigation and the agent logs QGADESKSTUCK), but it has never been measured to this repo's evidence bar. [verified 2026-08-29]
- Perf attribution residue: the scroll/typing per-frame damage-cost elevation vs the b299011 baseline (lead: PwScreenUnchanged's whole-region read + per-window damage machinery) is recorded as CLOSED by an ABAB attributing the cost to the DDA-slice path, but the closing evidence did not survive in this file — treat the closure as unconfirmed and re-derive from the canonical suite (tools/bench-stock-vs-ours.sh) before any regression claim. UNVERIFIED
