# rig — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- `net stop <svc>` SILENTLY DECLINES when the service has dependents: it asks `(J/N)` on a channel with nobody to answer, leaves the service RUNNING, and an `&`-chained sweep then reads as complete (measured 2026-09-21 on win11de-wus, cryptsvc; the next leg's `net start` answered "already started", which reads like success). Use `net stop /y` / `Stop-Service -Force` and ASSERT each service afterwards. [verified 2026-09-21]
- HARNESS TRAPS ON THE UPDATE PATH (from docs/ADR-updater.md, 2026-09-21): (a) `tools/replay-dom0-update.py` exits 0 regardless - grep its `<-- UNEXPECTED` markers, not its rc; (b) the updates proxy in this qube dies unattended - prove egress first or a network failure reads as a guest defect; (c) `$?` after a command substitution is the SUBSTITUTION's rc; (d) a probe must exclude the command `qtest` echoes back, avoid shell metacharacters, and distinguish EMPTY from NEGATIVE; (e) `tools/qtest` has no default target and refuses without `QTEST_VM`. [verified 2026-09-21]
- A STALE `--required` BLOCK ASSIGNMENT MAKES A GUEST UNSTARTABLE and the error names nothing but `libxenlight failed to create new domain`. Loop numbers are recycled on every reboot and every `loop-delete`, so a sealed golden's claim goes stale (four carried one on 2026-09-21). CHECK IT FIRST with `vm.devices['block'].get_assigned_devices()` over every qube; clearing a golden's stick claim is a REPAIR. [verified 2026-09-21]
- THE ONE UNEXPLAINED RESIDUAL of the 09-21/22 media affair: on 2026-09-21 a `--required` assignment made the domain uncreatable 6/6 with the loop present, correct and unclaimed, on a brand-new subject too - while on 2026-09-22 the same shape starts 3/3. A host reboot happened in between and WHAT IT CLEARED WAS NEVER MEASURED (Jev: `what-the-reboot-cleared` 0.97). Jev graded the candidates 2026-09-22 and NONE wins: `dom0-daemon-bad-state` 0.41, `local-blkback-wedged` 0.31, `contaminated-measurements` 0.20 (conf 0.25), `recoverable_now` 0.33 - this qube's system journal is unreadable (root:systemd-journal 0640, ACLs for adm/wheel only; `journalctl -b -1` has no previous boot) and dom0's is out of scope. The transcript shows the 6/6 was measured while a FULL CAMPAIGN launched at 12:06:49 was still running and four hand probes were started against the same guests at 12:13/12:17/12:19/12:20. `cheapest_discriminator=enforce-serial-rig` 0.63 - already in place as the PreToolUse gate. If it recurs, measure it; do not invent a workaround. CONTRADICTED 2026-09-22 by the 3/3 above. [verified 2026-09-22]
- **RETRACTED 2026-09-22 — "provisioning is dead rig-wide" was OUR workaround; the established recipe works.** Interleaved, 3 rounds, one subject, oracle = `qvm-start` rc: `--required` + the stick's `qemu-extra-args` rc=0 3/3; the same with a PLAIN assignment (prime-run's default since 2026-09-21, set "as a workaround") rc=1 3/3 with the bare `libxenlight failed to create new domain`; `--required` alone rc=0 3/3. A second backing qube behaves identically. **Jev: `root_cause=plain-assign-is-a-hotplug` 0.96 (conf 0.95) — a plain assignment is attached AFTER domain creation, so it cannot be in the stubdomain's initial config and qemu cannot open `/dev/xvdi`; `chain_established` 0.17, i.e. the mechanism is INFERRED, not shown. `record_void=all-conclusions-void-measurements-kept` 1.00. `unexplained_residual=what-the-reboot-cleared` 0.97 — the 09-21 session's 6/6 `--required` failures are real and this does not explain them.** **The direct evidence of the mechanism, kept because it is the only copy:** the stubdom console (`/var/log/xen/console/guest-<vm>-dm.log`) carries qemu's own `Could not open '/dev/xvdi': No such file or directory`, and dom0's `libxl-driver.log` for every such failure has exactly two lines - `stubdom_xswait_cb: ... startup timed out` then `domcreate_devmodel_started: device model did not start: -9`. Default reverted to `required`. Rule: A/B a workaround against the process it replaces before it becomes a default, and never build the probe from the workaround's shape. [verified 2026-09-22]
- `tools/fix-qvm-device-py314.py` is NOT applied and cannot stick on this AppVM (`/usr` is template-derived - it must be applied in the TEMPLATE). Bounded effect: `qvm-block`/`qvm-usb`/`qvm-pci` die at parser construction; `qvm-device <class>` is unaffected and nothing in this repo calls the three aliases. [verified 2026-09-22]
- `devtype=cdrom` LIVE ATTACH failed at qubesd (`Got empty response from qubesd`) through BOTH the CLI and the raw python API on 2026-09-22 while `devtype=disk` succeeded, and a cdrom ASSIGN was accepted but delivered nothing (`cdrom_count=0`). All of that was BEFORE the host reboot and has NOT been re-measured since - re-test before relying on, or condemning, the disc path. STALE - not re-measured since the host reboot. [verified 2026-09-22]
- FALLBACK CHANNEL, NOT THE PATH: `mgmt/harness/seed-private-job.sh` writes a job into the subject's OWN private volume (`admin.vm.volume.Import`, granted here) and the resident primer runs it - proven on `win10-pv1`, which found it at D: and ran the selftest as SYSTEM. It returns the media to RAW first, because QWT's private-disk gate correctly refuses a non-RAW disk #1 (NONRAW-AT-1). Jev: `fidelity_cost=install-path-fidelity-lost` 0.92, so never grade install-from-media with it. The stick recipe is the path. [verified 2026-09-22]
- `w_screen` returns `NOWINDOW` for a guest with no QWT while writing a perfectly good cropped PNG of it: with no gui-agent there are no seamless windows to enumerate. On a pre-QWT guest NOWINDOW means "no seamless windows", never "nothing on screen" - read the PNG. [verified 2026-09-22]
- prime-run discarded the rc of its boot `qvm-start`, so for two campaigns it announced "the guest started but the answer stick is NOT attached" when the DOMAIN had never been created (fixed 0edc9af - it now prints libvirt's own text). A diagnosis printed by a step that never checked the thing it names sends the next session after the wrong subsystem. [verified 2026-09-22]
- `win10-base` and `win11-base` carry a `qemu-extra-args` feature naming `/dev/xvdi`. It is INERT for clones (a fresh `clone-guest.sh` clone inherits `{'os':'Windows'}` only) but it should not be on a pristine base, and the seal covers VOLUME CONTENT, not qube properties, so nothing would catch it. [verified 2026-09-22]
- A FIRST update pass on an un-updated template needs HOURS, not one hour: `replay-dom0-update.py` under `timeout 3600` came back rc=124 on win11de-ctld mid-install of a 4.4 GB cumulative, and the harness then ran its oscillation scan against a guest whose updater mutex made the scan exit at once. The guest-side task survives the driver being killed - watch the guest's own status file instead of polling. [verified 2026-09-21]
- dom0's THIN POOL AT 99.3% LOOKS EXACTLY LIKE A GUEST STALL: `clone-guest.sh` died with dom0's own "free space in thin pool ... reached threshold" and a guest sat Running 48 min after its shutdown request (2026-09-21). Read `qvm-pool info vm-pool` first - it is cheap and it silently fills. BUT THE POOL DOES NOT EXPLAIN HUNG SHUTDOWNS: 17.7 GB was freed with nothing sent to the guest and it was still Running 15 min later. [verified 2026-09-21]
- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- `docs/RIG-CONSTRAINTS.md` is authoritative for measured constraints and verdict semantics (pushrun/session traps, volatile AppVM C:, shot blindness + two-witness rule, verdict rules V1–V7, fault-injection usage) — pointer only, do not restate here. [verified 2026-09-01]
- `.claude/skills/rig-capabilities/SKILL.md` is authoritative for capabilities, the environment map (loops/ISOs/pools), operational traps, and the disproven false limitations — read it before declaring anything impossible; volume revert/checkpoint capability status lives there. [verified 2026-09-01]
- `qtest-geom` WAS ~59s/call, and the cause was NOT the capture (that was my wrong first guess). Proven 2026-09-04: a geometry-ONLY service (local.WinGeom, no PNG at all) was ALSO ~58s, and scrot-vs-no-capture differed by ~1s. The real cost was a PER-WINDOW SUDO STORM: the dom0 geometry enumeration ran `sudo -u DOMUSER env ... xprop`/`xwininfo` 3-4 times for EVERY id on the whole dom0 root tree (hundreds), and each sudo spawn is the cost (owner saw the storm in dom0 logs). FIXED (dom0/07 + dom0/16, commit c83517b): run the whole enumeration in ONE `sudo -u DOMUSER ... bash -s`, no per-call sudo. Re-measure after re-install to confirm ~58s -> seconds. LESSON: isolate/measure the cost before building the fix — I shipped a scrot swap + a geometry-only service against an UNMEASURED capture assumption, and neither helped. Harness robustness added anyway (persistent toasts + low geom retries) is harmless and stays. [verified 2026-09-04]
- The prose-rule acceptance campaign is CONCLUDED as futile (owner call; measured self-adherence to hours-old prose rules ~60%). Protocol state is now owned by `findings/protocol.md` — the compiled runner (`protocol/run.py` + steps + defect scenarios) is the executable form; never add protocol prose here or anywhere. [verified 2026-09-01]
- Ledger numbers (PASS 319 / PASS-UNPROVEN 40 / N/A 21 of 429) must NEVER be quoted as an acceptance result: an unknown share of PASS rows rest on checks of the quality the linter keeps finding; any PASS predating repair of a cited check is unverified. [verified 2026-09-01]
- The legacy bash-harness lint backlog is PURGED (owner, 2026-09-01): 40 findings baselined in `tools/lint-baseline.txt` — superseded lineage, not open work; the lint gate fails only on NEW findings (run with `--baseline tools/lint-baseline.txt`), and editing a legacy file re-exposes its findings. Never add baseline entries. [verified 2026-09-01]
- `tools/lint-harness.py` + the pre-commit hook are mechanical and ENFORCING (self-tested against planted violations plus a negative control); they also lint findings CURRENT STATE heads — every bullet must end `[verified <date>]` or UNVERIFIED. [verified 2026-09-01]
- Fault injection is the standing prove-a-check-can-fail instrument: `QGA_FAULT_INJECTION=1` builds compile in `FI_*` bits (`FI_GATE_OFF`, `FI_DROP_*`, `FI_NOSYNTHPAINT`, `FI_NOSCREENCONFIG`), each off until armed by registry, absent from release builds; usage constraints (no grep-collidable log strings; `gate-preflight.sh` before a 28-min prove cycle) are in RIG-CONSTRAINTS §5. [verified 2026-09-01]
- Restores are SECONDS, never reprovisions: `mgmt/harness/checkpoint.sh` — park/unpark (exact + durable, 2–4 s) and undo-session (restores the session's ENTRY state, depth 2, revisions cut on clean shutdown). A full reprovision is warranted only for the Windows-install-plus-QWT-at-first-logon cell. [verified 2026-09-01]
- Goldens: ONLY `win10-base`/`win11-base` are sealed pristine bases (both exist, checked live 2026-09-01); 4.3.14, stock-4.2.2 and nopvdisk fixtures are built on demand and torn down, never sealed (owner call settling D12); never park a mid-campaign image as a golden. [verified 2026-09-01]
- `win10-u10` — the 2026-08-29 "preserve this guest" fixture (stock 4.2.2 registration + STOCK xenbus_monitor Automatic/Running + PV boot disk) — NO LONGER EXISTS (absent from the policied qube list, checked live); that directive is moot, and any stock-monitor-state precondition must be rebuilt on demand. [verified 2026-09-01]
- Canonical benchmark suite is `tools/bench-stock-vs-ours.sh` (enforces the evidence rules — interleaving, running-hash assertion, control runs — in code); quote performance numbers only from its runs, and remember a disjointness verdict is only as good as the variance sampled. [verified 2026-09-01]
- Guest wall clock: the virtual RTC is re-derived from dom0's LOCAL time at every domain start and in-guest RTC/timezone writes are discarded at destroy — no in-guest setting survives a reboot. Run `tools/qtest synctime` (median −0.009 s) after EVERY `qtest start`, before any measurement compared against dom0 timestamps; the permanent fix is dom0-side (`qvm-sync-clock`, needs a running clockvm). [verified 2026-08-11]
- Any claim about what dom0 DISPLAYS must come from `qtest fullshot` (its geometry.txt carries the override_redirect + mapped columns); per-window `shot` is only for pixel-comparing ordinary windows — the two-witness "nothing mapped" rule is RIG-CONSTRAINTS §3.1. [verified 2026-08-11]
- Stop the agent by signalling `Global\QGA_SHUTDOWN` (`scratchpad/graceful-stop.ps1`), never `Stop-Process -Force`; cold boots are for boot-path acceptance only. Even a graceful agent exit still coin-flip kills gui-daemon (6/10 restarts in the 08-05 soak) — the dom0-side EOF bug in `DESIGN-gui-daemon-restart-survival.md` §3 (repo root), upstream text still awaiting owner approval; recovery is a qube restart. [verified 2026-08-05]
- `HKLM\SOFTWARE\QubesIDD\Modes` has exactly ONE writer, the agent: no harness may touch it while the agent runs (the SyncNow instrument raced it and was retired; `resize-sync.ps1` was deleted outright). Machine-driving the resize/obtain path requires real MSG_CONFIGURE traffic — the dom0 resize service or the owner's hands. [verified 2026-08-15]
- `guest/registry-hygiene.ps1` (phantom-monitor sweep) must run as SYSTEM: Enum keys are SYSTEM-ACLed, an admin run reports the phantoms LOCKED and removes nothing. [verified 2026-08-06]
- Answer-disc assignment cannot be asserted via `qvm-device block list` (policy-refused from this qube); the working check is a second `qvm-device block assign` answering "already assigned". Exercise any permission-dependent command once by hand before using it as a gate. [verified 2026-08-07]
- Files under `guest/` cross qrexec as ASCII — keep them ASCII-only (a non-ASCII stem arrived mangled and broke the parse); gate with `tools/ps-parse-gate.sh` before shipping. [verified 2026-08-14]
- CI, qrexec binaries: the gui-agent job builds core-agent's three qrexec projects DIRECTLY (the whole solution drags in the WDK via relocate-dir) and generates + asserts `qwt_version.h` up front — a build whose success depends on project order is not a build. [verified 2026-08-21]
- CI, ours-wins guard: `packaging/ours-wins.psd1` + `packaging/check-ours-wins.ps1` in release-package.yml make the stale-payload class (our file shadowed by stock 4.2.2, or not shipped at all) a build failure — validated against 10 seeded defects; real-data green only after `e92ffde` (cite that commit, not the 08-28 entry). Contract: when CI ships core-agent bins, flip `bin/qrexec-wrapper.exe` to Required and delete its KnownGaps entry in the same commit. [verified 2026-08-28]

## "subject would not halt" was a HARNESS failure, not the guest (fixed 2026-09-12)

- ARITHMETIC, not bad luck: cell subjects get `qrexec_timeout=600` while the park waited 420 s and nothing drained queued qrexec calls - and a queued call RESTARTS a guest after it halts, so `w_halt` (which returns on the FIRST Halted sighting) cannot tell "never shut down" from "shut down and was restarted", and blames the product. FIX: `w_drain_and_shutdown` (drops qrexec_timeout to 15, kills pending calls, always restores), a 660 s budget that OUTLASTS the 600 s timeout, and `w_halt_stable`, which requires Halted to stay Halted and reports a restart as INVALID-INSTRUMENT naming the restarter. THE CHEAPEST CHECK WAS LAST: compare the harness's own two constants before theorising about the product. [verified 2026-09-12]
- THIS DEV QUBE'S GUI DIED 2026-09-18 03:45 until the owner restarted X on 09-20: `tools/context-audit.sh`'s self-test re-invoked `$SELF` without clearing `CONTEXT_AUDIT_SELFTEST=1`, so every child re-entered `selftest()` - 4818 `git init`ed trees in 29 s, 504 MB pinned in the 1 GB RAM-backed `/tmp`, then the OOM killer walked app.slice and session.slice. Fixed by `env -u` plus an exported depth counter, and the guard is seen to hold with the defect re-introduced (`CTXAUDIT_SELFTEST_DEFECT=1`). [verified 2026-09-20]
- RULE for any self-test that re-invokes its own script: clear the knob that selected the self-test (`env -u`) AND carry a depth counter. `unpark-precheck-selftest.sh` (guard var) and `qcon-session-selftest.sh` (`--inner`) already did; `context-audit.sh` was the one that did not, and it cost the owner two days of GUI. [verified 2026-09-20]
- `/tmp` in this qube is a 1 GB tmpfs, i.e. RAM out of only 4 GB, and self-tests leave their `mktemp -d` trees behind: it was at 100% on 2026-09-20 with ~600 MB of leftovers dating to early September (MSBuild temp dirs, extracted 32 MB setup trees, per-test dirs). Removing the 4818 bomb trees returned it to 51%. Any script that mktemps here must `trap ... EXIT` its cleanup, and a full `/tmp` on this qube is a memory emergency, not a disk one. [verified 2026-09-20]
- `qvm-start --cdrom=win-idd-mgmt:<loop>` FAILED FROM THIS QUBE on 2026-09-20 (domain not created, plain start of the same clone fine) AND POISONED THE GUEST: a failed `--cdrom` start leaves an assignment behind that cannot be seen or removed from here, after which even a plain start fails. Recovery is re-clone. Measured under the plain-assign era and NOT re-tested since the host reboot - re-measure before trusting either verdict. STALE - not re-measured since the host reboot. [verified 2026-09-20]
- `qvm-shutdown --wait` IS A HARD KILL ON A TIMER (`--timeout` default 60 s KILLS the domain), and it was in ten harness files, including one that killed the guest it was about to SEAL. CLOSED 2026-09-20: all callers go through `mgmt/harness/shutdown-lib.sh`'s `qwt_shutdown`, which requests and polls and kills nothing; `L9-shutdown-wait-kills` in `tools/lint-harness.py` refuses the pattern and is seen to fire on a fixture. Only `clone-guest.sh` and `release-acceptance.sh` still kill, both after a 600 s clean attempt and both logged. [verified 2026-09-20]
