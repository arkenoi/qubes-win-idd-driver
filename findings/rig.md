# rig — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- `net stop <service>` SILENTLY DECLINES WHEN THE SERVICE HAS DEPENDENTS, AND AN `&`-CHAINED SWEEP THEN READS AS COMPLETE. Measured 2026-09-21 on win11de-wus: `cmd /c net stop wuauserv & net stop UsoSvc & net stop DoSvc & net stop BITS & net stop cryptsvc & net stop WaaSMedicSvc` stopped five services and, at cryptsvc, printed the dependency list and the interactive question `Moechten Sie diesen Vorgang fortsetzen? (J/N) [N]:`. On a qrexec channel there is nobody to type J, so it logged `Es wurde keine gueltige Antwort gegeben`, left cryptsvc RUNNING, and the chain carried on as if the sweep had completed; the next leg's `net start cryptsvc` answered `Der angeforderte Dienst wurde bereits gestartet`, which reads like success. An experiment was recorded as covering six services and covered five. Use `net stop /y <svc>` (or `Stop-Service -Force`) and ASSERT each service's state afterwards - never read a chained sweep's output as the result. [verified 2026-09-21]
- HARNESS TRAPS ON THE UPDATE PATH — moved here from docs/ADR-updater.md 2026-09-21. (a) `tools/replay-dom0-update.py` prints each step's rc but **exits 0 regardless**; grep its own `<-- UNEXPECTED` markers instead of trusting its exit code. (b) The updates proxy in THIS qube dies and nothing restarts it, so prove egress before a run or a network failure will read as a guest defect. (c) `$?` read after a command substitution is the SUBSTITUTION's exit code — one run printed `exit=0` for a leg that had just refused to grade. (d) A probe's output must exclude the command `qtest` echoes back (matching the marker in the echo declares success 30 s after launch), must avoid shell metacharacters (`|` is a PIPE to cmd, so the probe errors and returns empty), and must distinguish EMPTY from NEGATIVE — an empty result is "the check did not run", not "not yet". (e) The mis-targeting trap is now closed in code rather than by discipline: `tools/qtest` has NO default target since 2026-09-21 and refuses when `QTEST_VM` is unset. [verified 2026-09-21]
- **A STALE `--required` BLOCK ASSIGNMENT MAKES A GUEST UNSTARTABLE, AND THE ERROR NAMES NOTHING.** Measured 2026-09-21, after it failed the acceptance campaign three times: `qvm-start` returns only `internal error: libxenlight failed to create new domain '<vm>'`, and prime-run's restart loop read that as the guest rebooting and then as "crash-looping, not installing" - i.e. a product verdict from a toolstack refusal. THE CAUSE: `qvm-device block assign --required` persists, and a loop device in THIS qube is transient - recycled on every reboot and by every `udisksctl loop-delete`. Four sealed goldens were carrying such claims (`win10-base`->loop15, `win11-base`->loop17, `win11-qwt`->loop0, `win11de-base`->loop35), so `win11-qwt` and a fresh `win11-acc` both claimed loop0 and neither could start. Removing the assignments made the guest start immediately, with nothing else changed. CHECK IT FIRST when a guest will not create: `vm.devices['block'].get_assigned_devices()` over every qube - it is one call and the error message will never tell you. Clearing a golden's stale stick claim is a REPAIR, not a modification: nothing that boots needs an answer stick attached. [verified 2026-09-21] **THIS CAUSE DOES NOT COVER EVERY CASE - see the entry immediately below, measured hours later: a guest can be uncreatable with a `--required` assignment whose loop is present, correct and unclaimed.**
- **AND A `--required` ASSIGNMENT CAN MAKE A GUEST UNCREATABLE WITH THE LOOP PERFECTLY VALID.** Measured 2026-09-21 on `win11-acc`, one variable at a time, after five acceptance campaigns died on `internal error: libxenlight failed to create new domain`. The entry above blames a claim on a RECYCLED loop; that does not explain this one. loop0 and loop1 both existed, were backed by the intended files, were not `(deleted)`, and no other qube claimed them or had them attached - all checked BEFORE assuming the documented cause applied. THE MEASUREMENTS, interleaved so nothing drifting in the background can account for them: with NOTHING assigned the domain is created and stays up (3 starts); with a `--required` assignment it is NOT created (6 starts), and that holds for a DIFFERENT loop the guest had never seen (loop35), a DIFFERENT frontend slot (xvdk not xvdi), and a DIFFERENT devtype (cdrom not disk). AND THE ONE THAT SAYS WHERE THE FAULT IS NOT: a LIVE `qvm-device block attach` of the very same device to the very same guest while it is RUNNING is ACCEPTED. So this qube's block export works; what fails is satisfying an assignment AT DOMAIN CREATION. TWO OF MY OWN CONCLUSIONS DIE HERE and must not be repeated. (1) "`qemu-extra-args` is the blocker" - REFUTED: the clean A/B is no-assignment vs assignment-with-NO-feature, and the feature changes nothing. I had drawn it from a test that removed the DEVICE while the feature still pointed at `/dev/xvdi`, i.e. qemu was handed a path that could not exist - a result that confirmed what I already believed, taken without the control that would contradict it. (2) "win11-acc cannot be created" - REFUTED by prime-run's own log: the domain WAS created at 12:07:17 with the full configuration, ran, halted at t+20s, and it was the RESTART that failed. THE "CREATED ONCE, NEVER AGAIN" RULE IS REFUTED, 12:46 the same day: a BRAND-NEW subject `win11-once` (clone of win11-qwt, never started before) was created fine with nothing assigned and then REFUSED TO BE CREATED AT ALL on its FIRST start with the assignment. So it is not per-domain residue and it is not once-only - prime-run's 12:07:17 success with the very same shape needs another explanation, and whatever changed did so rig-wide between 12:07:17 and 12:09:09 and has persisted since. NOT YET TESTED and the obvious next cut: a BRAND-NEW loop device from a fresh backing file - every failing leg so far used a loop that already existed, so "the old loops are stale in a way losetup does not show" and "no assignment can be satisfied at all any more" are still both live. STILL OPEN: WHY. The toolstack's reason is in dom0's `/var/log/libvirt/libxl/libxl-driver.log`, which cannot be read from this qube - `dmesg` is refused here and `/sys/bus/xen-backend/` shows nothing while detached, so there is no local substitute. A WORKAROUND IS INDICATED BUT UNPROVEN: since a live attach works, a harness could assign for the boot that needs the medium and live-attach afterwards instead of holding a `--required` claim across reboots. [verified 2026-09-21]
- **WHAT IS *NOT* THE CAUSE — four hypotheses killed by measurement, 2026-09-22.** Oracle: domain creation succeeds if and only if the stubdomain received the device, so `qvm-start` rc reads it without dom0. (a) **LOOPBACK HANDLING IS NOT IT** — the existing read-write loop, a FRESH loop over a FRESH COPY of the image (new inode, new backing file), a second READ-ONLY loop over that copy, and the existing loop assigned `--port` (identity ignored) ALL fail identically. (b) **THE CD AT START IS NOT IT** — the 2026-08-07 recipe verbatim, `--required` stick plus `qvm-start --cdrom=`, fails too (worth testing because the 09-13 working stubdom had `xvdd` alongside `xvdi`/`xvdj`). (c) **THE ASSIGN MODE IS NOT IT** — plain and `--required` both fail. (d) **NOTHING IN THIS QUBE WAS RE-INITIALISED** — up 384 h since 2026-09-06, so the loops persisted across the entire working-to-failing transition; 17 loops of 45 nodes, `max_loop=0`, and this qube's block export still works (a plain assignment attaches and is confirmed unfiltered). **THE ASYMMETRY THAT SURVIVES: the guest's own volumes, backed by DOM0, still reach the stubdomain; only devices backed by THIS QUBE stopped reaching it.** Jev: `where_is_the_reason=dom0-decides-stubdom-vbds` 0.81 (conf 0.74); `owner_hypotheses_refuted` 0.75. The break window from the transcripts: 12:07:17 UTC was the FIRST start of an acceptance run, 12:09 was `prime-run` RESTARTING the same guest, and by 12:46 a brand-new subject failed on its first start — with nothing in the 12:00-12:20 transcript touching devices, loops or dom0. **NO RECORD of a local fix to dom0's block extension exists anywhere** — repo (all branches, `-S` over content), findings, docs, dom0/, memory, scratchpad and all 16 session transcripts searched; the py3.14 argparse fix is the only local toolstack patch ever recorded. [verified 2026-09-22]
- **CAUSE CLOSED BY qemu ITSELF: `/dev/xvdi` DOES NOT EXIST IN THE STUBDOMAIN.** The stubdom console log (`/var/log/xen/console/guest-win10-acc-dm.log`, 2026-09-22 09:47:42) carries qemu's own stderr: `-drive file=/dev/xvdi,...: Could not open '/dev/xvdi': No such file or directory`. qemu exits, `/tmp/qemu.qmp` is never created, the stubdom script falls through to its shell prompt, the stubdom never signals ready, and libxl aborts the create at ~31 s. **The same logged command line shows what the stubdom DOES have: `/dev/xvda`, `/dev/xvdb`, `/dev/xvdc` as `if=ide` — the guest's own root/private/volatile volumes, which qemu emulates for the HVM.** So the stubdomain still receives block devices; only the EXTERNALLY ASSIGNED stick is missing from it — consistent with the unfiltered read showing an assigned `devtype=disk` device attaching to the GUEST domain, with no stubdomain entry in `q.domains` at all. **Jev: `why_it_broke=assigned-devices-now-go-to-the-guest-not-the-stubdom` 0.88 (conf 0.82); `chain_established` 0.48 — the link from the argument to the failure is now measured end to end, what is still unmeasured is WHY the target domain changed.** The stubdomain is not addressable through the Admin API, so no spelling from this qube puts a device there. **Jev's remedy: `seed-payload-into-the-base-image` 0.73 (conf 0.66)** — the base's own volumes DO reach the stubdomain, so a payload inside them needs no external medium; `give-the-base-pv-drivers` 0.26. [verified 2026-09-22]
- **THE FAILURE IS A STUBDOMAIN STARTUP TIMEOUT — dom0's libxl log, read 2026-09-22.** Every failing start, last night's probes and this morning's campaign alike, is exactly two lines and nothing else: `libxl_dm.c:2856:stubdom_xswait_cb: Domain N:Stubdom M for N startup: startup timed out` then `libxl_create.c:2018:domcreate_devmodel_started: Domain N:device model did not start: -9`. Timestamps match the cells one-to-one (06:44:01 = F3, 06:44:37 = F4, 06:48:12 = the WIN10-clean cell that began booting at 06:47:41 — a ~31 s budget). **There is NO device error, no permission error and no file-open error in that log, and no line naming any guest** — so "qemu could not open /dev/xvdX", which I wrote on 2026-09-21, was never stated by the toolstack and remains an inference. The stubdomain is the domain that runs qemu; a startup timeout means it never wrote its ready state to xenstore, and whether qemu exited, never ran, or was merely slow is NOT in this log. The same subject with no `qemu-extra-args` starts in ~3 s. **Jev: `root_cause=qemu-exits-on-the-drive-argument` 0.61 (conf 0.47), `chain_established` 0.21 — one link still inferred; `next_measurement=stubdom-console-log` 0.87** (`/var/log/xen/console/guest-<vm>-dm.log`, not yet read). [verified 2026-09-22]
- **PROVISIONING IS DEAD RIG-WIDE: ANY qemu `-drive file=/dev/xvdX` NOW FAILS DOMAIN CREATION — Jev 1.00.** Measured 2026-09-21/22 after two live acceptance campaigns lost all four cells. Every `qvm-start` rc CAPTURED, on a FRESH clone of `win10-base` (`win10-stk`) so churn contamination is excluded: no qemu-extra-args + nothing assigned STARTS (F1, F2); the base's own ONE-drive arg + `xvdi` assigned FAILS (F3); the harness's TWO-drive arg + both sticks assigned FAILS (F4) — always the same bare `internal error: libxenlight failed to create new domain`. On the churn guest the same pattern held across nine cells, including an arg naming `/dev/xvdNOPE`, a path that cannot exist, which fails IDENTICALLY — so this signature cannot distinguish "the device is missing from qemu's namespace" from any other reason the device model failed. A bare `-device nec-usb-xhci` and an EMPTY arg string both start, so the extra-args MECHANISM is still honoured; only a `-drive` naming a `/dev/xvdX` path fails. A PLAIN (not `--required`) assignment with NO arg starts and genuinely attaches, and an unfiltered read shows it attaching to the GUEST domain, not to a stubdomain (Jev rated that fact's explanatory power 0.11 — it is a fact, not the mechanism). **Jev: `root_cause=any-xvdX-drive-arg` confidence 1.00, `chain_established` 0.17** — the pattern is certain and the MECHANISM IS NOT SHOWN. It is in dom0's `libxl-driver.log`, unreadable from this qube. Consequence for the rig: the emulated-USB medium is unavailable, and with it every clean-install cell, because a stock Windows has no PV drivers and cannot see the PV disk that a plain assignment gives it. Jev's indicated next route: an emulated CD (`cdrom-attach-to-running-guest` 0.52 / `cdrom-assign-before-start` 0.35) — UNVALIDATED. The earlier entry below refuting "qemu-extra-args is the blocker" was measured under `--required` only, where the assignment alone fails; with a PLAIN assignment the arg is decisive, and both statements hold at once. [verified 2026-09-22]
- **THE LOCAL PYTHON 3.14 PATCH IS GONE, AND ON THIS AppVM IT IS LOST ON EVERY REBOOT.** `tools/fix-qvm-device-py314.py` (ab5c1b7, 2026-08-01) is the repo's only local toolstack patch: qubesadmin registers a fixed device class as a hidden POSITIONAL with `action="store_const"`, which Python 3.14 rejects, so the parser dies at construction. Measured 2026-09-22: no patch marker in `/usr/lib/python3.14/site-packages/qubesadmin/tools/qvm_device.py`, no `.orig-preclaude` backup, `rpm -V qubes-core-admin-client` clean (the file is stock), python 3.14.6, and `qvm-block --help` reproduces `ValueError: action 'store_const' is not valid for positional arguments`. **win-idd-mgmt is an AppVM, so `/usr` is template-derived and a sudo patch to it dies at the next reboot — it must be applied in the TEMPLATE to stick**, which is what the script's own docstring says. WHAT IT ACTUALLY BREAKS: `qvm-block`/`qvm-usb`/`qvm-pci` only; `qvm-device <class>` takes a different path and works, and a repo-wide grep finds NO caller of the three aliases. **It does NOT explain the provisioning failures — Jev 0.10** — and Jev rates restoring it now at 0.38, so it is a known, bounded, separate regression, not a blocker. [verified 2026-09-22]
- **THE CDROM FAULT IS IN qubesd, NOT IN THIS QUBE'S CLI — measured by bypassing the CLI.** Back to back on one running guest: the CLI `qvm-device block attach --ro -o devtype=cdrom` and the PYTHON API (`DeviceAssignment` + `devices['block'].attach`, no argparse involved) BOTH fail with `Got empty response from qubesd`, while the same API with `devtype=disk` succeeds and the device attaches. An empty response is what the client prints when the daemon returns nothing — an unhandled exception on the dom0 side. So the missing local patch and the CLI layer are both excluded, and no spelling reachable from this qube gets past it. **Jev: `next_step=owner-reads-dom0-logs` 0.52, `report-upstream-qubesd-bug` 0.37.** [verified 2026-09-22]
- **THE SECOND MEDIUM ROUTE IS DOWN TOO: `devtype=cdrom` FAILS AT qubesd — Jev places the fault dom0-side at 0.93.** Measured 2026-09-22 on `win11-cdt` (a clone of the `win11-qwt` golden, so it has QWT and can report), with a real ISO on a loop. A `cdrom` ASSIGNMENT before start is accepted and the domain IS created — but it silently delivers nothing: an unfiltered read shows ZERO block attachments rig-wide at that moment, and the guest reports `cdrom_count=0`, `diskdrive_count=3`. A LIVE attach, same guest, back-to-back polarity: `devtype=disk` SUCCEEDS (the unfiltered read then shows the device attached); `devtype=cdrom` FAILS with `Error: Got empty response from qubesd`. This repo's own record has a live attach being ACCEPTED earlier on 2026-09-21, so it is a change in behaviour, not a standing limitation. **With the `-drive /dev/xvdX` route also dead, every EMULATED medium is unavailable and only the PV `devtype=disk` path works — which a pristine base cannot see, that being the whole reason the emulated medium exists.** Both errors' detail is in dom0 logs (`libxl-driver.log`, `journalctl`) that this qube cannot read. **Jev: `fault_location=dom0-side` 0.93 (conf 0.91), `escalate_now` 0.69, `best_workaround=pv-disk-plus-pv-drivers-in-base` 0.72.** ESCALATED TO THE OWNER 2026-09-22 — clean-install acceptance cannot run until this is cleared. [verified 2026-09-22]
- **A FULL HOST REBOOT DID NOT CLEAR THE MEDIA FAULT, and two more candidate causes died with it.** Measured 2026-09-22 07:16Z, minutes after the host came back (this qube up 3 minutes, every guest Halted, every loop device gone and recreated fresh): `scratchpad/after-reboot-check.sh` — fresh loop over `answer-usb.img`, PLAIN `devtype=disk` assignment at `xvdi`, the stock USB `qemu-extra-args` — returned `rc=1 internal error: libxenlight failed to create new domain 'win10-lv'`, the identical signature. **So the fault is neither transient daemon state in dom0 nor stale state in this qube: it survives a reboot of everything.** Two more candidates are dead, both measured the same morning: the DEVICE DENY-LIST (`admin.vm.device.denied.List` returns 0 entries for the backend qube and for every subject) and dom0 POLICY DRIFT (`policy.Get+10-win-idd-all` matches the repo copy except for lines this repo added later). **The owner has closed dom0-side investigation** — 2026-09-22, verbatim: "stop dom0 fuckery for things that are not allowed by policy anyway". The cause therefore stays UNEXPLAINED by design, and the rig works around it. [verified 2026-09-22]
- **THE PROVISIONING CHANNEL THAT NEEDS NO EMULATED MEDIUM: the subject's OWN private volume.** The stubdom's own qemu command line shows the guest's root/private/volatile passed as `if=ide` — dom0-backed volumes still reach the device model and are emulated as ORDINARY IDE DISKS, which a pristine Windows sees with no PV driver. The baked-in primer (`mgmt/primer/qubes-prime.cmd`) scans drive letters `d..z` for `<letter>:\qubes-prime\onboot.cmd` and cares nothing for bus type, removability or media class. So the job image can be written INTO the subject's private volume: `admin.vm.volume.Import` is granted to this qube for `@tag:win-idd-testbed`, and it WORKS — measured 2026-09-22, a 512 MiB MBR+FAT32 image (built with `mkfs.vfat` on a file and `mcopy -i img@@1M`, since a udisks loop partition is not writable here) imported into `win10-pv1:private` with `qvm-volume import --size`, rc=0, the volume reading 512 MiB afterwards. **AND THE GUEST-SIDE HALF IS MEASURED:** `win10-pv1`, a fresh clone of `win10-base` with NOTHING assigned and NO `qemu-extra-args`, started rc=0 at 07:24:32Z; at 07:26:58Z it was Halted (the selftest job ends in `shutdown /r`); restarted, and at 07:30:23Z the screen showed the job's own Notepad reading `PRIMER SELFTEST PASSED`, `ran this job as: nt authority\system`, `Date: 22/09/2026 7:25:19.12` (inside the FIRST boot's window, so the reboot was its own), **`Job media was: D:\qubes-prime\`** — Windows lettered the imported private volume — and the QubesPrime task gone, which is what that job asserts. The owner saw the same window independently. **Jev: `best_route=private-volume-primer` 0.78 (conf 0.72), `first_measurement=windows-letters-the-volume` 0.80, and afterwards `channel_proven` only 0.43 with `biggest_residual=no-readback` 0.78 — this qube has NO `admin.vm.volume.Export` grant and the CLI has no export subcommand, so a run that fails before qrexec exists can only be read as pixels, where the emulated-stick harness had a second writable DIAG stick read back with mtools.** AND THE COST, which is not optional to record: `fidelity_cost=install-path-fidelity-lost` 0.92 — the media-insertion path is itself part of what a release acceptance grades, so this channel is for FUNCTIONAL work on a guest, never for grading how the field's ISO arrives. [verified 2026-09-22]
- **prime-run REPORTED THE WRONG FAILURE FOR TWO WHOLE CAMPAIGNS, because it threw away the rc of the start.** The boot-time `qvm-start "$CHURN" >/dev/null 2>&1` discarded its result (only the RESTART loop captured it, fixed in 0edc9af), so when the domain was never created the script walked on to its post-start assertion and announced "the guest started but the answer stick is NOT attached" — naming the medium, the one thing that was fine. Eight cells across two campaigns were recorded against that sentence. It now captures the rc and says "the DOMAIN WOULD NOT START - the guest never ran, so nothing about the stick, the payload or the product is inferable from this", with libvirt's own text. **A diagnosis printed by a step that never checked the thing it names is worse than no diagnosis: it sends the next session after the wrong subsystem, which is exactly what it did.** [verified 2026-09-22]
- **A SEALED PRISTINE GOLDEN CARRIES HARNESS DEVICE WIRING — `win10-base` and `win11-base` both hold a `qemu-extra-args` feature naming `/dev/xvdi` (Jev: anomaly 0.89).** It is INERT for clones — measured 2026-09-22, a fresh `mgmt/clone-guest.sh` clone inherits `{'os': 'Windows'}` only — so it did not cause the failure above, and it was not the cause of anything measured tonight. It should still not be there: a pristine base is the thing every acceptance run enters from, and harness-specific device wiring on it is state nobody declared. The seal covers VOLUME CONTENT, not qube properties, so nothing would ever have caught it. [verified 2026-09-22]
- **THE 1-HOUR REPLAY BUDGET IS TOO SHORT FOR A FIRST PASS ON AN UN-UPDATED TEMPLATE.** Measured 2026-09-21 on win11de-ctld (GWeck's environment, env-assert OK): `tools/replay-dom0-update.py` is run under `timeout -k 20 3600` and came back **rc=124** - killed, not finished - while the guest was at `phase=install` working through KB5129195, a **4.4 GB** cumulative it had already fully downloaded (`already complete (4424.5 MB)`). Nothing was wrong with the product: KB5007651 and KB4052623 were installed, KB5126052 staged, `remaining=6` and dom0 `1` - all truthful. But the harness then ran its oscillation SCAN against a guest still mid-install, where the updater's own mutex makes a scan exit immediately, so that check can only report 'the scan never ran'. TWO CONSEQUENCES: a first pass on an un-updated 25H2 template needs a budget in HOURS, not one hour; and the guest-side task survives the driver being killed, so the honest way to follow such a pass is to stop polling and watch the guest's own status file. [verified 2026-09-21]
- **dom0's THIN POOL RAN TO 99.3% AND THAT LOOKS EXACTLY LIKE A GUEST STALL.** Measured 2026-09-21: `qvm-pool info vm-pool` read usage 868,998,821,655 of size 875,212,832,768. Two things failed at once and neither was a product defect: `mgmt/clone-guest.sh` died with dom0's own message *"Cannot create new thin volume, free space in thin pool qubes_dom0/vm-pool reached threshold"*, and `win11de-ctlb` - which had been sent a polite ACPI shutdown - went qrexec-dead and then sat `Running` for THIRTY MINUTES without ever delivering S5, which is precisely the signature this project has been calling the applying-boot stall. A thin pool that cannot allocate extents blocks the writes Windows makes on its way to S5. **Before attributing any stall to the guest, to QWT or to a PV driver, read the pool.** The rig runs ~20 Windows guests at 12-20 GB each plus checkpoints, so it reaches this threshold silently and nothing warns. Removing one spent subject freed 17.7 GB. **BUT THE POOL DOES NOT EXPLAIN THE HANG, and that was TESTED, not assumed:** 17.7 GB was freed and NOTHING was sent to the guest; `win11de-ctlb` was still `Running` fifteen minutes later, 48 minutes after its shutdown request. It should have been obvious beforehand that 6.2 GB free is ample for the writes a shutdown makes - the two failures were coincident, not causal, and I coupled them too readily. What stands: the pool DID cause the clone failure, dom0 said so verbatim, and the pool is worth reading first because it is cheap and it silently reaches the threshold. What does not stand: any claim that it causes hung shutdowns. [verified 2026-09-21]
- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- `docs/RIG-CONSTRAINTS.md` is authoritative for measured constraints and verdict semantics (pushrun/session traps, volatile AppVM C:, shot blindness + two-witness rule, verdict rules V1–V7, fault-injection usage) — pointer only, do not restate here. [verified 2026-09-01]
- `.claude/skills/rig-capabilities/SKILL.md` is authoritative for capabilities, the environment map (loops/ISOs/pools), operational traps, and the disproven false limitations — read it before declaring anything impossible; volume revert/checkpoint capability status lives there. [verified 2026-09-01]
- `qtest-geom` WAS ~59s/call, and the cause was NOT the capture (that was my wrong first guess). Proven 2026-09-04: a geometry-ONLY service (local.WinGeom, no PNG at all) was ALSO ~58s, and scrot-vs-no-capture differed by ~1s. The real cost was a PER-WINDOW SUDO STORM: the dom0 geometry enumeration ran `sudo -u DOMUSER env ... xprop`/`xwininfo` 3-4 times for EVERY id on the whole dom0 root tree (hundreds), and each sudo spawn is the cost (owner saw the storm in dom0 logs). FIXED (dom0/07 + dom0/16, commit c83517b): run the whole enumeration in ONE `sudo -u DOMUSER ... bash -s`, no per-call sudo. Re-measure after re-install to confirm ~58s -> seconds. LESSON: isolate/measure the cost before building the fix — I shipped a scrot swap + a geometry-only service against an UNMEASURED capture assumption, and neither helped. Harness robustness added anyway (persistent toasts + low geom retries) is harmless and stays. [verified 2026-09-04]
- The prose-rule acceptance campaign is CONCLUDED as futile (owner call; measured self-adherence to hours-old prose rules ~60%). Protocol state is now owned by `findings/protocol.md` — the compiled runner (`protocol/run.py` + steps + defect scenarios) is the executable form; never add protocol prose here or anywhere. [verified 2026-09-01]
- Ledger numbers (PASS 319 / PASS-UNPROVEN 40 / N/A 21 of 429) must NEVER be quoted as an acceptance result: an unknown share of PASS rows rest on checks of the quality the linter keeps finding; any PASS predating repair of a cited check is unverified. [verified 2026-09-01]
- The legacy bash-harness lint backlog is PURGED (owner, 2026-09-01): 40 findings baselined in `tools/lint-baseline.txt` — superseded lineage, not open work; the lint gate fails only on NEW findings (run with `--baseline tools/lint-baseline.txt`), and editing a legacy file re-exposes its findings. Never add baseline entries. [verified 2026-09-01]
- `tools/lint-harness.py` + the pre-commit hook are mechanical and ENFORCING (self-tested against planted violations plus a negative control); they also lint findings CURRENT STATE heads — every bullet must end `[verified <date>]` or UNVERIFIED. [verified 2026-09-01]
- Fault injection is the standing prove-a-check-can-fail instrument: `QGA_FAULT_INJECTION=1` builds compile in `FI_*` bits (`FI_GATE_OFF`, `FI_DROP_*`, `FI_NOSYNTHPAINT`, `FI_NOSCREENCONFIG`), each off until armed by registry, absent from release builds; usage constraints (no grep-collidable log strings; `gate-preflight.sh` before a 28-min prove cycle) are in RIG-CONSTRAINTS §5. [verified 2026-09-01]
- Restores are SECONDS, never reprovisions: `mgmt/harness/checkpoint.sh` — park/unpark (exact + durable, 2–4 s) and undo-session (restores the session's ENTRY state, depth 2, revisions cut on clean shutdown). A full reprovision is warranted only for the Windows-install-plus-QWT-at-first-logon cell. [verified 2026-09-01]
- Goldens: ONLY `win10-base`/`win11-base` are sealed pristine bases (both exist, checked live 2026-09-01); 4.3.14, stock-4.2.2 and nopvdisk fixtures are built on demand and torn down, never sealed (owner call settling D12); never park a mid-campaign image as a golden. [verified 2026-09-01]
- `win10-u10` — the 2026-08-29 "preserve this guest" fixture (stock 4.2.2 registration + STOCK xenbus_monitor Automatic/Running + PV boot disk) — NO LONGER EXISTS (absent from the policied qube list, checked live); that directive is moot, and any stock-monitor-state precondition must be rebuilt on demand. [verified 2026-09-01]
- Canonical benchmark suite is `tools/bench-stock-vs-ours.sh` (enforces the evidence rules — interleaving, running-hash assertion, control runs — in code); quote performance numbers only from its runs, and remember a disjointness verdict is only as good as the variance sampled. [verified 2026-09-01]
- Guest wall clock: the virtual RTC is re-derived from dom0's LOCAL time at every domain start and in-guest RTC/timezone writes are discarded at destroy — no in-guest setting survives a reboot. Run `tools/qtest synctime` (median −0.009 s) after EVERY `qtest start`, before any measurement compared against dom0 timestamps; the permanent fix is dom0-side (`qvm-sync-clock`, needs a running clockvm). [verified 2026-08-11]
- Any claim about what dom0 DISPLAYS must come from `qtest fullshot` (its geometry.txt carries the override_redirect + mapped columns); per-window `shot` is only for pixel-comparing ordinary windows — the two-witness "nothing mapped" rule is RIG-CONSTRAINTS §3.1. [verified 2026-08-11]
- Stop the agent by signalling `Global\QGA_SHUTDOWN` (`scratchpad/graceful-stop.ps1`), never `Stop-Process -Force`; cold boots are for boot-path acceptance only. Even a graceful agent exit still coin-flip kills gui-daemon (6/10 restarts in the 08-05 soak) — the dom0-side EOF bug in `DESIGN-gui-daemon-restart-survival.md` §3 (repo root), upstream text still awaiting owner approval; recovery is a qube restart. [verified 2026-08-05]
- `HKLM\SOFTWARE\QubesIDD\Modes` has exactly ONE writer, the agent: no harness may touch it while the agent runs (the SyncNow instrument raced it and was retired; `resize-sync.ps1` was deleted outright). Machine-driving the resize/obtain path requires real MSG_CONFIGURE traffic — the dom0 resize service or the owner's hands. [verified 2026-08-15]
- `guest/registry-hygiene.ps1` (phantom-monitor sweep) must run as SYSTEM: Enum keys are SYSTEM-ACLed, an admin run reports the phantoms LOCKED and removes nothing. [verified 2026-08-06]
- Answer-disc assignment cannot be asserted via `qvm-device block list` (policy-refused from this qube); the working check is a second `qvm-device block assign` answering "already assigned". Exercise any permission-dependent command once by hand before using it as a gate. [verified 2026-08-07]
- Files under `guest/` cross qrexec as ASCII — keep them ASCII-only (a non-ASCII stem arrived mangled and broke the parse); gate with `tools/ps-parse-gate.sh` before shipping. [verified 2026-08-14]
- CI, qrexec binaries: the gui-agent job builds core-agent's three qrexec projects DIRECTLY (the whole solution drags in the WDK via relocate-dir) and generates + asserts `qwt_version.h` up front — a build whose success depends on project order is not a build. [verified 2026-08-21]
- CI, ours-wins guard: `packaging/ours-wins.psd1` + `packaging/check-ours-wins.ps1` in release-package.yml make the stale-payload class (our file shadowed by stock 4.2.2, or not shipped at all) a build failure — validated against 10 seeded defects; real-data green only after `e92ffde` (cite that commit, not the 08-28 entry). Contract: when CI ships core-agent bins, flip `bin/qrexec-wrapper.exe` to Required and delete its KnownGaps entry in the same commit. [verified 2026-08-28]

## "subject would not halt" was a HARNESS failure, not the guest (diagnosed + fixed 2026-09-12)

SYMPTOM: a campaign cell fails `subject would not halt for parking` / `for unpark`, the guest sits
Transient at ~0% CPU, and a later boot comes up dark with the agent logging `dom0's gui-daemon for
this qube is gone`. Graded product-FAIL. It cost a campaign, and I compounded it by reverting a
working agent change on the strength of the misattribution.

CAUSE, and it is arithmetic rather than bad luck:
every cell subject is given qrexec_timeout=600 (matrix.sh), the park waited 420 s for Halted, and
NOTHING in the park path drained queued qrexec calls - zero occurrences of the qrexec_timeout-15
drain this project already documented.
A queued qrexec call RESTARTS a guest after it halts and can hold it longer than the wait, so
`w_halt` - which returns on the FIRST sighting of Halted and otherwise only ever observes Transient -
cannot tell "never shut down" from "shut down and was started again", and blames the product.
Everything downstream follows: the state oscillates, the restarted guest boots into a dom0 that has
given up on a qube that kept reappearing, and the agent's gui-daemon complaint is TRUE but is a
consequence, not the cause.

FIX: `w_drain_and_shutdown` (drops qrexec_timeout to 15, kills pending client calls, shuts down,
always restores the previous value), a 660 s budget that OUTLASTS the 600 s timeout instead of
sitting under it, and `w_halt_stable`, which requires Halted to still be Halted a moment later and
reports "halted then came back" as INVALID-INSTRUMENT naming the restarter.

WHAT IT COST, so the lesson is not lost: four candidate causes were eliminated by evidence (agent
init completes; the daemon safely ignores damage for an unmapped window; no broker respawn loop;
6/6 clean reboots on the suspect binary) and a targeted reproduction - clean install then immediate
shutdown - took 22 s against a 420 s budget. Only then did the arithmetic get checked. THE CHEAPEST
CHECK WAS LAST: compare the harness's own two constants before theorising about the product.
[verified 2026-09-12]
- THIS DEV QUBE'S OWN GUI DIED on 2026-09-18 03:45 and stayed dead until the owner restarted X on 09-20 13:23. Cause, established by controlled reproduction: `tools/context-audit.sh`'s self-test re-invoked `$SELF` without clearing `CONTEXT_AUDIT_SELFTEST=1`, and a shell exports every variable it inherits from its environment — so each child re-entered `selftest()` with no depth limit. Measured: 4818 `git init`ed `/tmp/ctxaudit.*` trees in 29 s (504 MB pinned in the 1 GB RAM-backed `/tmp`), forking on for HOURS afterwards (dirs timestamped through 02:51, one at 06:58); kernel OOM killer then walked the user session — 22 kills between 02:22 and 03:45, ending with `app.slice` and `session.slice`, which is X and the gui-agent session. Reconstructed Sep-18 code in a `TasksMax=120` scope hit 123 tasks in 2 s. Fixed by `env -u CONTEXT_AUDIT_SELFTEST` on the child plus an exported depth counter that refuses re-entry past 3; with the defect re-introduced (`CTXAUDIT_SELFTEST_DEFECT=1`) the self-test FAILS and the guard holds it to 5 processes, so the check has been seen to fail. Jev graded the candidate causes: root_cause=ctxaudit-fork-bomb conf 1.00, gui_death_was_collateral 0.93, chain_established 0.76. RESIDUAL, cannot be closed from here: the kernel's own OOM victim list (`dmesg` is refused, `kernel.dmesg_restrict=1`; the system journal needs root, which is out of scope) — Jev puts 0.95 on that being the one fact still missing. [verified 2026-09-20]
- RULE for any self-test that re-invokes its own script: clear the knob that selected the self-test (`env -u`) AND carry a depth counter. `unpark-precheck-selftest.sh` (guard var) and `qcon-session-selftest.sh` (`--inner`) already did; `context-audit.sh` was the one that did not, and it cost the owner two days of GUI. [verified 2026-09-20]
- `/tmp` in this qube is a 1 GB tmpfs, i.e. RAM out of only 4 GB, and self-tests leave their `mktemp -d` trees behind: it was at 100% on 2026-09-20 with ~600 MB of leftovers dating to early September (MSBuild temp dirs, extracted 32 MB setup trees, per-test dirs). Removing the 4818 bomb trees returned it to 51%. Any script that mktemps here must `trap ... EXIT` its cleanup, and a full `/tmp` on this qube is a memory emergency, not a disk one. [verified 2026-09-20]
- `qvm-start --cdrom=win-idd-mgmt:<loop>` IS BROKEN FROM THIS QUBE as of 2026-09-20: the domain fails to be created (`internal error: libxenlight failed to create new domain`) while a PLAIN start of the same guest succeeds. Measured four ways: a fresh clone + a never-used loop number fails; the same clone starts fine with no `--cdrom`; `maxmem` 0 vs 8192 makes no difference; and the backing file is present and not `(deleted)`. WORSE, IT POISONS THE GUEST: a failed `--cdrom` start leaves a persistent assignment behind, after which even a plain start fails with the same opaque libxl message - the only visible proof is that a second `--cdrom` attempt then says `block device win-idd-mgmt:loop33:* already assigned to win11de-fresh`, while `qvm-device block list <vm>` prints NOTHING and `admin.vm.device.block.Assigned` is policy-refused, so the assignment cannot be seen or removed from here. Recovery is to `qvm-remove` the guest and re-clone (5 s with `mgmt/clone-guest.sh`), and then never to pass `--cdrom` to it. DOCUMENTED FALLBACK, not yet tried here: `mgmt/CLAUDE.md` ("Known failure modes") already records `--cdrom` from our own file failing, with the workaround `qvm-block attach --ro <vm> win-idd-mgmt:<loop>` BEFORE the start instead of `qvm-start --cdrom` - worth trying before calling the disc path dead, though note `qvm-device block list` is policy-refused from this qube so the attach may be too. CONSEQUENCE: the release-ISO-as-a-disc delivery path that `mgmt/harness/quick-upgrade.sh` and `matrix.sh` depend on is unavailable until this is fixed, and any harness that calls `qvm-start --cdrom` will report it as a DARK BOOT unless it reads its own `cdboot.out` (fixed in quick-upgrade 2026-09-20). This cost most of an afternoon because the first symptom looked host-wide - I wrongly reported "every Windows HVM domain creation is failing" and asked for dom0 diagnostics; a clone that had never seen a CD started immediately and refuted that. [verified 2026-09-20]
- `qvm-shutdown --wait` IS A HARD KILL ON A TIMER, and the rig is full of it. Its own help says `--timeout TIMEOUT  timeout after which domains are KILLED when using --wait (default: 60)` - so `--wait` does not merely wait, it `qvm-kill`s the domain when the timer expires. Measured 2026-09-20: `timeout 600 qvm-shutdown --wait <vm>` returned after 61 s (the 60 s DEFAULT, the outer `timeout` never getting a say) on a guest that was applying a staged cumulative and whose qrexec still answered - i.e. a healthy guest, mid servicing apply, at the exact moment it is least safe to kill. At least ten files carry the pattern, and the most dangerous is `mgmt/harness/matrix.sh:157` (`--wait --timeout 300`): the release acceptance campaign will hard-kill any guest that needs more than five minutes to shut down, which is ordinary for an applying-updates boot - against the standing rule that a killed subject is CONTAMINATED and must never be reused or regraded. This is a plausible contributor to the contaminated-subject and "stall" history: a guest that was merely slow to apply updates gets killed by the harness, and the next cell grades the corpse. `mgmt/harness/wu-e2e.sh` no longer uses it - it REQUESTS the shutdown and then WATCHES for Halted with its own bound, and a guest that will not halt is reported, never killed. The other callers are NOT fixed and each needs its own judgement about whether a kill is acceptable there. [verified 2026-09-20] **CLOSED 2026-09-20, same day:** all ten callers now go through `mgmt/harness/shutdown-lib.sh`'s `qwt_shutdown <vm> [deadline]`, which REQUESTS the shutdown (bare `qvm-shutdown`) and then POLLS, and which kills nothing ever - a guest that will not stop returns rc=1 and the caller decides. Reading the call sites made the cost concrete: every poll loop written UNDER one of these was DEAD CODE, because the guest was already killed before the loop began - `stability-e2e.sh` promised "8 min covers a guest applying updates" over a 60 s kill, `reprovision-usb.sh` polled 40x5 s after one, and `sg1-u2-coldboot.sh`'s `FATAL unless Halted` guard could never fire because the kill guarantees Halted. Two sites were worse than dead code: `matrix.sh:157` killed while its own comment three lines up said "ACPI only - a killed guest leaves a dirty volume that blocks the next clone", and `seal-qwt-golden.sh` killed the guest it was about to SEAL AS A GOLDEN, which every clone made from it then inherits. A new lint, `L9-shutdown-wait-kills` in `tools/lint-harness.py`, refuses the pattern on any non-comment line of a harness, and is seen to fire on a fixture in `tools/tests/lint-selftest.sh`. The repo is clean of it; only the two explicit last-resort kills (`clone-guest.sh`, `release-acceptance.sh`) still call `qvm-kill`, both after a 600 s clean-shutdown attempt and both logged as the contamination they are. [fixed 2026-09-20]
