# wedge — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- MECHANISM (the live wedge class), NMI-proven on two Windows builds with unrelated workloads: Xen HVM IPI/TLB-shootdown deadlock. The sender CPU spins forever at `nt!KeFlushMultipleRangeTb+0x13e` (a `pause`) on `_KPRCB.PacketBarrier=1 / TargetCount=1` — a single-target shootdown whose target vCPU never ACKs — reached from ORDINARY kernel MM ops (pool allocation, VA teardown, a driver's `MmUnmapIoSpace`) as well as from qrexec bridge-process exit; Windows' HvlLongSpin hypercall rescue does not land under Xen HVM. Captures: win10-tpl 19045 under relay soak; win11-fresh 26100 with no relay, no servicing, no gui-agent. [verified 2026-08-20]
- Third capture (win10-app, owner fired `--nmi`) added the XEN-SIDE half no guest dump can hold: `xl vcpu-list` showed 3 vCPUs pegged `r--` and the fourth `-b-` BLOCKED with flat cputime — favouring IPI-delivery-to-a-halted-vCPU over scheduler starvation. Limits: two snapshots 10 s apart, one instance; the yield was dom0-side state only (kit defects degraded the console/grant capture). [verified 2026-09-01]
- The one OPEN evidentiary link: WHY the IPI never lands (Xen scheduling vs emulated-LAPIC delivery/latch). Invisible from any guest dump by construction; needs Xen-side instrumentation at repro time (`xl debug-keys` vcpu/evtchn state, vlapic, xentrace). UNVERIFIED
- Trigger profile: NOT workload-specific — three guests, three workload classes (relay soak / idle first boot / near-idle AppVM poked with qrexec); concurrent MM ops on a 4-vCPU HVM guest suffice. The proven VOLUME driver and best provocation is qrexec bridge-process EXIT (each qrexec-client-vm/qrexec-wrapper maps a locked+secured grant region into user space; exit → `MiUnmapLockedPagesInUserSpace` → shootdown); 38 concurrent bridges at capture 1. [verified 2026-08-20]
- RETRACTED and stays retracted (2026-08-20, second capture): per-fetch qrexec/relay churn as THE TRIGGER — churn is only a provocation. Also retracted: "flat CPU = spin" (sampled deltas varied ~11%), and "drop the testbed to 2 vCPUs" (masks the bug and destroys the reproducer; the testbed stays at 4 vCPUs). [verified 2026-08-20]
- RETRACTED WHOLESALE (2026-08-29): the 2026-08-28 root-cause cluster — "ROOT CAUSE NAMED BY WINDOWS ITSELF" (event-1074/xenagent attribution), all four seeded "reproductions", the suppressor-race hypothesis, "WIN10 takes the two-stage path", and the seeded-cell "reboot-prompt condition wedged win10-tpl" (the seed fired BEFORE the installer started — those cells measured the injection). None of it may be cited as fact. Surviving from that window: the 19:25 seed-OFF control, the black-with-CPU observation, the harness-defect list, and commits 61cdec9/81d2b79 on their own terms (81d2b79's message overclaims). [verified 2026-08-29]
- Two EARLIER, distinct, closed classes — do not conflate with the IPI wedge: (1) xenbus/xeniface grant-revoke spin on a still-mapped entry (NMI dump 2026-08-05, ~22k pinned grants) — display-path trigger removed by the lifetime staging grant, never recurred, underlying PV-driver defect unfixed upstream; (2) idle/deaf PV-ring-servicing stop after display replug PnP churn (NMI dump 2026-08-06: ALL vCPUs idle, event channels intact) — mitigated by the M7 churn guard (mode LRU + 2.5 s min replug interval; the storm soak that wedged on storm 1 then passed 6/6), never root-caused. [verified 2026-08-06]
- Guest-side recovery of the IPI wedge is infeasible (all CPUs ≥ DISPATCH; the IPI machinery is the dead part; DPC watchdog cannot fire) — prevention only. The relay multiplex redesign (deployed fleet-wide 2026-08-20) removed the biggest churn producer, but every qrexec service call still spawns a grant-mapping bridge process, so the trigger is present system-wide and our own acceptance testing is a heavy churn source. [verified 2026-08-20]
- Operator signature: domain Running with ~2–3.5 vCPUs busy, qrexec dead, no/frozen dom0 windows, TOTAL silence — all three guest event logs gapped, and the dom0 device-model log showed a ~97-min hole spanning the whole failure (rules out qemu/stubdom/emulated-device faults; the ACPI power press never even reaches the device model). The failure is entirely in-guest. [verified 2026-08-29]
- Mimics that are NOT this wedge: a qube stuck `Transient` is a start-time/host-memory-pressure confound (never share the host with another 8 GB install — the 2026-08-07 "second boot also wedged" was the host, retracted same day); and the 2026-08-29 WIN10 "install wedge" was a modal "Windows Security" driver-publisher-trust dialog (class #32770, rundll32, up 1.5 s after InstallDriverPackages) blocking msiexec for 28 min — recoverable by ACPI in 30 s, unlike the real wedge. The repeated hard kills answering that dialog state — not the installer — are the credible (not proven) author of the win10-tpl brick. [verified 2026-08-29]
- The trust-dialog fix is IN THE TREE: Install-QwtImproved.ps1 trusts the payload certs in Root + TrustedPublisher before driver install, explicitly to stop that prompt; the defect-present→absent verification on the same cell has no surviving record (erased window) — re-verify before relying on it. [verified 2026-09-01]
- .13-vs-.15: the build delta contains NO kernel-adjacent change (xenvif source-identical in both; the whole diff is user-mode), and wedge occurrence #2 hit mid-install before any agent ran — build version is probably not the variable. The .13/.15 A/B is a falsification test (prediction: no difference) and is meaningful only after a validated reproducer exists. [verified 2026-08-29]
- Reproducer state: tools/wedge-hunt.sh drives the proven trigger (qrexec bridge-process exit churn) with outside-the-guest detection and a WriteThrough black box; the known-good provocation remains OLD-(pre-multiplex)-relay + 4-soaker churn (wedged win10-tpl in ~2.7 min for capture 1). A hunt launched 2026-08-29 23:45 has no surviving verdict, so the harness's reliability as a reproducer is unestablished — re-validate before any A/B. UNVERIFIED
- Fix-acceptance rule (standing, set 2026-08-20): interleaved A/B under the same aggressive soak at 4 vCPUs, ≥3 runs per side, running-binary hash verified per run; metric = peak concurrent qrexec-client-vm/wrapper count and their exit rate (wedge condition = 38 concurrent). "No wedge lately" is not evidence. [verified 2026-08-20]
- Forensics kit: dom0/11-wedge-forensics.sh runs as a dom0 qrexec service (`--nmi` deliberately owner-only); tools/wedge-guard auto-captures the moment Running+qrexec-dead persists (needs the dom0/13 service installed); guest NMI-dump prerequisites (CrashDumpEnabled=2, NMICrashDump=1, pagefile sized) are per-guest — armed on win10-tpl 2026-08-20, and re-clones since may have discarded it, so re-check before a capture matters. Standing rule: when a harness freezes a wedged state, the dom0 kit runs BEFORE any kill (one capture was destroyed by violating this). [verified 2026-09-01]
- Dump analysis is self-serve on this dev qube: vol3 patched for DumpType-6 bitmap kernel dumps, ISF built from the msdl ntkrnlmp.pdb, per-CPU context via volshell (crashing CPU's true context from the dump header ContextRecord@0x348; canonicalise vol3's 48-bit-truncated kernel base). [verified 2026-08-20]
- Kit traps, fixed IN-REPO but NOT yet deployed to dom0 — the next capture is degraded until the owner pulls: (1) plain `xl console` can NEVER work on a Qubes HVM (no stubdom console target; it core-dumps dom0's xl — three occurrences, drafted as docs/upstream-xl-console-overflow.md) — use `xl console -t pv`; (2) `xl debug-keys g` after a wrapped console ring printed "grant entries: 0" for a table that was never captured (only the stubdom's survived) — now `xl dmesg -c` runs first and a grant-CAPTURE-INCOMPLETE marker replaces the fabricated number. [verified 2026-09-01]
- Emulated serial needs NO dom0 change: `qvm-features <vm> qemu-extra-args '-serial file:/dev/hvc0'` gives the guest a working COM1 whose bytes land in dom0's `guest-<vm>-dm.log` (write-only; interactive SAC would need a dom0 libvirt `<serial>` template change). Set and COM1-write-verified on win10-app; the last link — the SERIALMARK lines actually appearing in the dom0 file — still needs one dom0 grep. [verified 2026-09-01]
- EMS is ARMED (2026-09-02) on win10-tpl AND win11-tpl (`ems Yes` on {current}, {emssettings} COM1@115200; unplug latch re-armed NICS=0x1 on both boots, clean shutdowns) and INHERITED by AppVMs via the root snapshot — verified: win10-app booted `ems Yes`. `qemu-extra-args '-serial file:/dev/hvc0'` is set on win10-tpl/win11-tpl/win10-app/win11-app so COM1 exists everywhere it matters. EMS would NOT diagnose the IPI wedge (no bugcheck fires; nothing schedulable to emit) — its value is bugcheck-headline insurance, spontaneous BSODs, boot failures; the NMI dump stays primary. Payoff UNVERIFIED until a dom0 grep shows EMS/SAC bytes in guest-<vm>-dm.log (in the owner's pending dom0 paste). [verified 2026-09-02]
- PV console (xencons) is vendored, CI-built and ships since 4.3.16 — `sudo xl console -t pv <vm>` / admin.vm.Console gives an out-of-band channel independent of qrexec; a guest deadlocked at IPI level will still be silent there, and the proposed kernel heartbeat writer (would timestamp wedge ONSET in a dom0-captured log) is not built. [verified 2026-09-01]
- XENVIF_REF is PINNED: .github/workflows/pv-xenvif.yml pins xenbits xenvif to 0c61248439c6bdeb862178c95843bba9a27877fc with a checkout-mismatch check — closing the 2026-08-29 finding that the kernel PV NIC driver content was decided by the wall clock (.13/.15 were identical only by luck). [verified 2026-09-01]
- The first-vif netvm-attach wedge (xenvif PdoCreate spinning at DISPATCH under Frontend->Lock against unpatched mirage fw-net: ~2 cores, qrexec never up, ACPI ignored) is FIXED by the patched mirage close/reconnect state machine — qrexec up in 15 s, xenbus handshake completes; the residual downstream MiniportRestart failure belongs to the mirage-gso track, not this wedge class. [verified 2026-08-21]
- Upstream status: docs/upstream-xen-pv-grant-revoke-spin.md is a DRAFT AWAITING OWNER APPROVAL and covers ONLY the 2026-08-05 grant-revoke-spin class (its resize-storm repro has since been removed by our own staging fix; driver versions still TODO in the text). The IPI/TLB-shootdown class — 3 captures, 2 Windows builds, unrelated workloads — qualifies under the out-of-scope reporting exception but has NO draft text yet; the owner approves the exact text before anything is sent. [verified 2026-09-01]
