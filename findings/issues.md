# issues — prioritized register by tag

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The owner-required standing view
(2026-09-01): every open issue, tagged and prioritized, maintained IN PLACE — close by
DELETING the bullet, never by appending. P1 = owner action needed or top product risk;
P2 = next engineering work; P3 = background/watch. Tags: graphics-performance,
guest-stability, user-facing, rendering, test-framework, network, debug-introspection,
updates, installer, architecture, upstream. Session reports lead with the P1/P2 deltas
of this file.

### guest-stability
- P1 [MEASURED 2026-09-10 - the post-install-reboot stall is a MULTI-vCPU DEADLOCK IN THE GUEST, not a shutdown problem] A clean install stalled on round 1 of a repeat hunt and the subject was preserved UNKILLED for the first time. dom0 forensics (dom0/11-wedge-forensics.sh, capture wedge-20260910-100313) show, over 10 s: vCPU0 r-- 2237.9->2247.9 and vCPU1 r-- 2192.5->2202.5 (+10.0 s each = 100% of a core apiece), while vCPU2 --- 47.2->47.2 and vCPU3 --- 50.4->50.4 are FROZEN, never scheduled. Corroborated from this qube: cputime +54.4 s over 30 s wall (181% of a core). The PV console capture taken IN DOM0 is EMPTY, so the guest is stuck below the OS - which also retires the worry that earlier missing consoles were an artefact of my own kills. Grants 60 active (not exhaustion); the stubdomain is alive and blocked (the device model is fine). No S5 ever reaches the toolstack, which is why the domain stays Running and ignores further ACPI requests. THIS RETIRES THREE EXPLANATIONS BUILT EARLIER THE SAME DAY, all mine: a hung service-stop at shutdown (that idles at ~0% CPU, not 181%), Startup Repair (idles at a prompt - and the screen photographed then was on a guest I had killed repeatedly, so it proved nothing), and the dirty-volume/flush story (the flush IS a real hazard mitigation - shutdown under write load is unclean, both arms 3/3 - but it is NOT the cause of this). ORDER-OF-WORK LESSON: CPU consumption should have been measured BEFORE two explanations were built on a screenshot. IT IS THE SIGNATURE of the IPI/TLB-shootdown wedge already in this register - two CPUs spinning, the CPUs they would wait on never running - but the MECHANISM IS NOT CONFIRMED and must not be asserted without a memory image. NMI WAS FIRED (xl trigger, 07:03:50) and produced NO bugcheck and no dump, though the spin fell 181%->100%, consistent with vCPU0 taking the NMI; likely reason, self-consistent with the class: a bugcheck must freeze the other processors via IPIs, so if IPI delivery is what is wedged the crash path deadlocks too - which also means NMICrashDump=1 in the image would NOT help and was deliberately not added. ONLY REMAINING ROUTE to name the spinning code: `xl dump-core <vm> <file>` from dom0 (no guest cooperation; ~8 GB for an 8192 MB guest). Rate: 1 stall in the first round of a 10-round hunt, plus 2 in the preceding day's campaigns. [also: upstream, test-framework] [verified 2026-09-10]
- P1 [ROOT-CAUSED 2026-09-09, fix shipped in 4.3.25, released in 4.3.26, NOT yet shown to close the stalls] SHUTTING A GUEST DOWN WITH WRITES IN FLIGHT PRODUCES AN UNCLEAN SHUTDOWN, AND THE NEXT BOOT NEEDS REPAIR. Two subjects were lost in one day to "guest never came back from its post-install reboot": win10-upgrade (1 of 5 runs) and win10-clean (prime-run, which waited 900 s and gave the guest every chance - so NOT a harness race). Both ended with an identical fingerprint: Running in Xen, no qrexec, NO CONSOLE ATTACH AT ALL (dom0's [ATTACHED] banner never printed, which is a domain-level condition, not busy guest software), no mapped window, no framebuffer; one was later photographed in Startup Repair ("Your PC did not start correctly"), which is exactly a state with no qrexec, no xencons and no mapped window. MEASURED, owner hypothesis, with an instrument first PROVEN TO FAIL (hard kill mid-write -> Kernel-Power 41,6008): idle, `shutdown /s /f` clean 3/3 and host ACPI clean 3/3; under ~2 GB of writes in flight, BOTH UNCLEAN 3/3. THE POWER-OFF IS EXONERATED - the two arms fail together, so this is not a property of choosing /s over /r and reverting the verb fixes nothing; the variable is IN-FLIGHT WRITES, and the installer's inter-stage transition happens straight after an MSI has been writing. FIX (306a94b): flush the system volume and wait for the disk to go quiet before signalling S5, bounded, with a missing cmdlet logging rather than silently no-opping. HONEST LIMIT: the measured load is heavier than an MSI's tail, so this is a demonstrated HAZARD, not a demonstrated CAUSE of those two stalls - do not close this until a clean-install campaign runs repeatedly without one. DETECTION now exists where there was none: prev_shutdown_orderly in health-check.ps1 (41/6008 + the NTFS dirty bit). The injection also corrected that check's design - on the unclean boot fsutil still read "NOT Dirty", so an fsutil-only check could never have failed. Evidence parked: ckpt-win10-acc-wedge0909, ckpt-win10-acc-wedge2clean (both contaminated by later qvm-kills - a killed guest is dirty BECAUSE of the kill, so neither proves anything about the original fault). [also: installer, test-framework] [verified 2026-09-09]
- P2 [FIXED AND PROVEN 2026-09-09] THE STAGE-1 REBOOT WAS A COIN FLIP; IT IS NOW A DEFINED PATH. A pristine guest has no PV drivers bound, so stage 1's reboot went out via the emulated ACPI/CF9 reset. In QEMU that raises `qemu_system_reset_request`, and TWO consumers race for it: the generic main loop (`qemu_system_reset` -> `qemu_devices_reset`, an IN-PLACE reset that never tells Xen) and the Xen accelerator's ioreq handler (`destroy_hvm_domain` -> SHUTDOWN_reboot -> toolstack -> on_reboot=destroy). Domain-destroys is the INTENDED Qubes behaviour; the in-place reset is the leak. FIX (guest-side, stands alone): stage 1 requests an ACPI S5 POWEROFF, which sets `shutdown_requested` - a separate flag with no `qemu_system_reset` path, whose only consumer is the Xen ioreq handler - so the domain goes down every time and the next start is always a fresh domain with the PV bus bound. It costs no capability: under Qubes a guest cannot restart itself either way (on_reboot=destroy), so `/r` never bought a self-restart, only a chance of an invisible in-place reset. Fast Startup is SET AND VERIFIED in the same block, because `shutdown /s` with hiberboot enabled is a hybrid shutdown whose next start RESUMES instead of booting - that would defeat the fix silently, and client Windows ships with it ON. EVIDENCE: 7 clean installs on the fixed path, 7/7 DESTROY, 0 WARM, 0 UNKNOWN, confirmed by a second instrument. CAVEAT ON STRENGTH, because the raw numbers overstate it: the historical control ("8 destroy / 6 warm" over 14 runs) was measured with prime-run's `restarts` counter alone, a 20 s poll that demonstrably misses short halts - three of the seven fixed-path runs recorded restarts=0 and were genuine destroys - so an unknown number of those six historical "warm resets" were not, the control's true rate is unknown and likely below 43%, and the 0.57^7 power calculation resting on 43% claims more than it should. The direction is not in doubt; the multiplier is. The historical runs CANNOT be re-checked: no xid was ever recorded for them. NOT ESTABLISHED: how the guest's vCPUs are reset in the in-place case (under Xen, QEMU models devices, not vCPUs), so the second half of the warm-reset mechanism is UNKNOWN - owner ruling 2026-09-09, not worth chasing, no useful application. Plausible but unverified consequence: an in-place reset reinitialises the device model without cleanly re-establishing PV state (grants, event channels, xenstore), which would explain why warm resets usually worked and occasionally left the PV bus permanently unbound. [also: installer] [verified 2026-09-09]
- P3 [SHIPPED IN 4.3.22, the 7043 risk measured] WATCHDOG LIFETIME FIXES. Merged from `watchdog-lifetime-4322` (agent 550e1da) into 4.3.22: token handle leaks in `StartTargetProcess` (both tokens leaked per launch; the `GetCurrentProcess()` pseudo-handle was closed instead of the real one), the INFINITE `ServiceMain` join bounded to 10 s after the stop event, and PRESHUTDOWN reporting STOP_PENDING + setting the stop event instead of reporting STOPPED with the respawn loop still live. CI has now compiled the file, and the specific risk - that the PRESHUTDOWN change REVERSES b51a09f, which was rig-verified against Event 7043 (a three-minute stall on EVERY clean shutdown) - was measured: `prev_shutdown_clean` passed across real cold shutdowns in the 4.3.22 6/6 acceptance, "no 7043/7031/7000 for our services across the previous shutdown", on both win11-clean and win11-reinstall. b51a09f's premise is also obsolete by inspection: it was written when WatchdogThread was marked "never exits", and the thread now waits on g_StopEvent as waitHandles[0] and breaks out on WAIT_OBJECT_0. REMAINING WEAKNESS, kept honest: `prev_shutdown_clean` has never been seen to FAIL with a synthetic 7043 present, so its PASS is evidence of absence collected by an unproven instrument - drive it with a planted event before treating it as a closed gate. HOW THIS CLASS WAS FOUND, worth keeping: agent commit `a195692`'s message described all three fixes and `git show --stat` says it touched `gui-agent/main.c` ONLY - they were never staged, and sat in the working tree while a release package was built, verified and put through acceptance. A commit message is not evidence that a change shipped; `--stat` is. [also: test-framework] [verified 2026-09-09]
- P2 BOOT-TO-QREXEC VARIES 15-38 s AND THE CAUSE IS UNKNOWN. **RETRACTED 2026-09-09: the "~10 s slower on NON-CLEAN guests, sets are DISJOINT" claim below is WRONG and must not be repeated.** It was n=2 vs n=4 from a single campaign. The 4.3.21 final campaign added samples and the split does not survive: win11-acc CLEAN measured 33 s (the slowest standalone number on record) while win10-acc clean measured 15 s (the fastest), and across all 22 samples the ranges overlap completely - win11-acc (standalone, clean AND upgrade) 20/30/20/29/21/33/24, win10-acc 20/23/15, win11-app 38/28/30/22/23/34, win10-app 20/24/29/18/20/21. **The spread WITHIN one unchanged configuration (win11-acc: 20-33 s) is as large as the between-group difference the finding was built on**, so the grouping explained nothing. This is precisely the CLAUDE.md instrument rule I skipped: a metric must be shown stable on ONE unchanged binary across >=3 runs BEFORE any verdict, and I drew a disjointness verdict without ever establishing stability - the same way a whole bisect was voided once before. What the data now supports, weakly: a possible OS effect (win11 median ~28 s vs win10 ~20 s) that also overlaps and is NOT established. WHAT TO DO NEXT, in order: (1) establish the noise floor - boot ONE unchanged guest >=5 times and get the within-configuration distribution, because until that exists no comparison between configurations means anything; (2) only then compare configurations; (3) attribute with `mgmt/harness/boot-attribution.sh` (added 2026-09-09), which reads what Windows already records - `Diagnostics-Performance/Operational` event 100 carries BootTime/MainPathBootTime/BootPostBootTime per boot and 101/102/103/106/109 NAME the slow application, driver, service, background process or device. Check event 100 FIRST: if two guests report the same MainPathBootTime, the difference is AFTER the boot Windows measures (service start ordering, qubesdb readiness, QrexecAgent's 1 Hz readiness poll) and no 103 line will show it. NOT a release blocker (owner 2026-09-09) and not a regression against any baseline - no prior boot-to-qrexec numbers exist, because the harness previously recorded only "desktop shell up", a DIFFERENT quantity (autologon + profile + explorer) that says nothing about when dom0 can talk to the qube. Needs a guest that SURVIVES a run - cells reap at their start, so measure on a preserved subject or a dedicated boot. [also: graphics-performance] [verified 2026-09-09] --- SUPERSEDED ORIGINAL CLAIM, kept only so it is not re-derived: "~10 s slower on non-clean guests", acceptance 2026-09-08 (matrix.sh `start_vm` now records every cold boot to `$M/boot-to-qrexec.tsv`, and writes `no-qrexec-within-600s` rather than a number when a boot misses the ceiling, so absent guests cannot flatter it): clean installs 20, 20 s; upgrade-over-golden 30 s; AppVM cold boots 28, 30, 38 s. `{20,20}` vs `{28,30,30,38}` are DISJOINT. Not an AppVM-vs-standalone effect - the upgrade guest is a standalone too - so the discriminator is something the clean guests do not carry (installed state? service count? profile creation?). NOT a release blocker (owner 2026-09-09) and not a regression against any measured baseline: no prior boot-to-qrexec numbers exist, because until now the harness only recorded "desktop shell up at t+16-17 s", which is a DIFFERENT quantity (autologon + profile + explorer) and says nothing about when dom0 can talk to the qube. INSTRUMENT TO USE, do not theorise: Windows records the answer itself in `Diagnostics-Performance/Operational` - event 100 carries `BootTime`/`MainPathBootTime`/`BootPostBootTime` per boot, and events 101/102/103/106/109 NAME the slow application, driver, service, background process or device. That turns "~10 s slower" into "this component costs N ms". Two candidates worth testing first: our own service start ordering (`QrexecAgent`'s readiness wait on qubesdb is a 1 Hz poll, so up to ~1 s of slack per boot, and the SCM dependency orders process start rather than readiness), and simply more installed state on non-clean guests. Needs a guest that SURVIVES a run - the cells reap at their start, so this must be measured on a preserved subject or a dedicated boot. [also: graphics-performance] [verified 2026-09-08]
- P1 [ROOT-CAUSED AND FIXED 2026-09-08, awaiting a green retest before closing] BROKER-DEATH WAS NOT DETECTED - the relaunch path reset only the agent's LOCAL heartbeat copy (`g_WgcBrokerHbLast = 0`) while the dead broker's last heartbeat stayed in SHARED memory, non-zero, for ever; the next supervise pass compared stale-nonzero against 0, read it as "the heartbeat advanced", refreshed `g_WgcBrokerHbSeenAt` and re-certified a corpse as alive - repeating every ~8 s, so a dead broker was healthy indefinitely. The recovery machinery was suppressing the failure detection it exists to support. Fix: zero the SHARED field on relaunch so "no heartbeat yet" is representable (safe: the line is reached only after `alive` is already false). ORIGINAL REPORT: with wgcbroker.exe killed AND renamed away (no process, relaunch impossible), the agent still reported `brokerState=2` (READY) 80 s later and emitted NO `QGADESLICEDOWN` and no `DesliceBrokerDown` for 180 s. Measured 2026-09-08 on win11-brk (agent 756d320) with the broker's absence confirmed on the guest (`tasklist` empty, binary present only as `wgcbroker.exe.off`). Detection is supposed to take ~36 s (6 s heartbeat staleness + `DESLICE_FIRST_WARN_MS` 30 s). Ruled out by inspection: heartbeat field aliasing (`AgentHeartbeat`/`BrokerHeartbeat` are distinct in `wgcbroker_ipc.h`); the 1 s main-loop wait cap being gated on liveness (it is `g_WgcBroker && g_WgcBase`, still true); the supervisor not being called (`BrokerSupervise()` is unconditional at main.c:8528 and the loop demonstrably ran, logging a QGADIRECTWAIT at the 80 s mark). NOT introduced by the 2026-09-08 state-machine work — that touched neither `BrokerSupervise` nor the heartbeat. This is a failure path that fails to detect failure, i.e. silent by construction. Next step is targeted logging of `hb`, `g_WgcBrokerHbSeenAt` and `alive` inside `BrokerSupervise`, since static reading has not explained it. WHICH REAL SCENARIO THIS IS (owner correction 2026-09-08): these guests enforce AUTOLOGON with a single user - there is no logoff and no fast-user-switch - so the session-change deaths the relaunch path (`g_WgcSession`) is written for DO NOT OCCUR here, and that path is effectively dead code in this deployment. Excluding them, a CRASH of wgcbroker.exe (WGC/DXGI fault, WDDM reset) is essentially the only way the broker dies after having been ready - i.e. the ready->dead transition is not an artificial test case, it is THE realistic one, and it is the one measured as undetected. The remaining deaths (packaging gap: binary absent; task deleted/Scheduler blocked) are NEVER-STARTED, a different route through the same function (`alive` false from the first pass, `g_WgcBrokerHbSeenAt` never set) which is the case the hard-fail was built and validated against; it is NOT shown broken and must not be described as such until measured. User-visible effect of the undetected case: toasts, menus and WinUI surfaces silently stop appearing (there is deliberately no composite fallback on an eligible guest) with DesliceBrokerDown=0 and nothing in the log naming the cause. MITIGATED, not fixed, by the destruction-time report (a withheld window that dies unpainted now raises QGADIRECTSUPPRESS regardless of what the broker state claims) - which is currently the ONLY signal covering a broker crash. TODO: add a never-started arm to brokerstates.sh so the two routes are measured apart instead of argued. [also: rendering] [verified 2026-09-08]
- P1 [CAUGHT IN THE ACT 2026-09-10 - the wedge is a XEN deadlock on the EMULATED-VGA DIRTY-VRAM PATH, and the stack is now known] `xl debug-keys d` on a live, never-killed wedge gives pCPU21 spinning in HYPERVISOR context with this call trace: flush_area_mask+0xcb <- map_pages_to_xen+0x5ba <- virt_to_xen_l3e+0xe9 <- vunmap+0x29 <- vfree+0xb1 <- hap_track_dirty_vram+0x16a <- dm_op+0x241. Xen 4.19.4. flush_area_mask is the TLB-shootdown broadcast: it IPIs a mask of CPUs and spins until each acknowledges, so a CPU pegged there is waiting for an acknowledgement that never comes - which is precisely the 'why does the IPI never land' question this entry used to hold open, now answered as far as WHERE. dm_op is a hypercall FROM THE DEVICE MODEL, so the wedged vCPU is QEMU's stubdomain (xentop shows both win10-acc and win10-acc-; the dump's context tag is a different domid from the guest's 6735), and the GUEST is unreachable only because its device model is stuck inside a hypercall - not because Windows did anything. hap_track_dirty_vram is the emulated-VGA dirty-region tracking QEMU uses to know what to redraw, which puts this squarely on the display path this project works on (see the open IDD VGA-disable item). REPRODUCER, which this entry also lacked: repeated clean installs - 1 stall in the first round of a 10-round hunt (mgmt/harness/clean-install-repeat.sh, which never kills, so the specimen survives). Capture: wedge-20260910-100313 plus wedge-regs.txt. NOT YET KNOWN: which CPU fails to acknowledge and why; that needs the target mask decoded or a memory image (`xl dump-core`, ~8 GB, or streamed out of dom0 compressed). This is a XEN defect, not a QWT one, so it is reportable upstream under the standing exception once the owner approves the exact text. [also: upstream, graphics-performance] [verified 2026-09-10] TRIGGER NARROWED, AND THE VGA-DISABLE THEORY IS OUT: the wedged guest's own install log, read off the parked volume with ntfscat on the raw device (no mount, no journal replay - a dirty NTFS refuses a normal mount), ends at 'trusted 9 payload certs [stage 2, before msiexec]' 06:23:30, TWO SECONDS into stage 2. A healthy run writes the next line in the SAME second and continues steadily; here nothing follows for the three minutes until the guest stopped answering. The IDD activation and the emulated-VGA disable happen ~65 s into stage 2 and emit their own lines - NONE are present, so the guest never reached them. Caveat stated because absence of log lines is not absolute proof: the deadlock is in the DEVICE MODEL, so guest disk I/O stalls with it and anything in cache was lost - but continuous second-by-second logging in healthy runs makes 'I/O stopped at 06:23:30' much the better reading. CONSEQUENCES: (a) the A/B over /noidd came back 0 stalls vs 0 stalls in 4+4 runs, which is now EXPLICABLE rather than merely inconclusive - both arms are equally exposed because /noidd only changes something that happens after the wedge point; (b) the -DeferVgaDisable switch added the same day targets the WRONG event and stays default-off and unshipped - deferring it would have fixed nothing and cost an extra install reboot; (c) the leading candidate is now EARLY STAGE 2, around msiexec, which installs the PV drivers and forces device re-enumeration - a plausible route to the device model re-evaluating VRAM tracking, and it fits the timing exactly. Log kept at scratchpad/wedge-forensics/wedge3-install.log.
- P2 forensics-kit fixes (console `-t pv`, grant-capture-incomplete marker) are in-repo but NOT deployed to dom0 — dom0 cannot be pushed to; the one-paste pull procedure was handed to the owner 2026-09-02, and after it runs one `qtest wedge` capture verifies the fixed kit end-to-end. [also: debug-introspection] [verified 2026-09-02]
- P2 staging grant per agent start (7200 pages) is never reclaimed — ~144 restarts to exhaustion; needs an owner outliving the agent (holder service or IDD-granted framebuffer). [also: architecture] [verified 2026-08-16]
- P2 `VchanSendBuffer` spins unbounded when the daemon dies with a full ring, and a window flip-storm can fill the vchan and kill the capture thread (S1b) — last recorded open; re-verify live, then fix with bounded/non-blocking writes. [also: architecture] UNVERIFIED
- P3 ~3085 of 3445 agent starts once died before StagingEnsure, unexplained (2026-08-20 record; history erased) — re-derive from live telemetry before investigating. UNVERIFIED
- P3 WUDFRd event 219: recovered-transient today; whether it EVER fails to recover is under observation across acceptance runs (one non-recovery on record, 0xC0000365). [verified 2026-08-29]
- P3 win11-24h2 qrexec-agent death while the guest lived — n=1, plausible wedge-class, unproven. [verified 2026-08-29]
- P3 sporadic first-boot reset attribution (xenvbd reboot-request) — prompt path removed by monitor-disable; resetter never caught in the act. [also: network] UNVERIFIED

### graphics-performance
- P1 PACKAGING GAP: `wgcbroker.exe` + `notifhost.exe` + `wgcprobe.exe` are BUILT in CI (gui-agent-package artifact) but NEVER staged into the installable package — `qwt-full`/stage-qwt-repo `agent-bins` = only gui-agent.exe+gui-watchdog.exe (release-package.yml qwt-full step ~L374); `make-setup.ps1`/`make-package.ps1` copy only those two; `Install-QwtImproved.ps1` `$OurBinaries=@('gui-agent.exe','gui-watchdog.exe')`. PROVEN on win11-app (base64 Test-Path): gui-agent/gui-watchdog PRESENT, wgcbroker/notifhost/wgcprobe ABSENT; Qubes-WgcBroker task armed but NO broker process. CONSEQUENCE: the shipped-4.3.18 "de-slice broker default-on (24H2+)" is INERT on any release install (WgcBrokerActive()=false → DDA-slice fallback; win11 still renders, so it was invisible). Any win11 de-slice/materialized-menu broker verdict from a release install measured the fallback; if it passed, it was validated with a dev-pushed binary (violates the release-only acceptance rule). FULL-INSTALL PATH FIXED (the release): qwt-full builds+signs+exports wgcbroker.exe+notifhost.exe in helper-bins/; make-setup stages them into the setup tree bin/ (THROWs if absent); Install-QwtImproved's existing bin-overlay installs them next to gui-agent.exe. Agent now HARD-FAILS loudly: QGADESLICEDOWN (WARNING, 30s then 120s) + DesliceBrokerDown registry flag (0 ready/opt-out, 1 present-not-running, 2 binary-missing) when broker expected-but-absent on an eligible system (owner 2026-09-04: "hard fail on eligible system", "silent fallbacks with no diag" was the worry). RESOLVED + VALIDATED: win11-a0tb primed from the FIXED release package (agent b8fbe9a / e5da449) 2026-09-04 — wgcbroker.exe + notifhost.exe PRESENT, broker process serving the agent, `DesliceBrokerDown=0` (ready), health-check ok:true, `desktop_on_idd` PASS, pixels render. The de-slice broker now runs end-to-end from a clean release install — the gap is closed and proven, no dev-pushed binary. Measurement note: `Get-Process wgcbroker` false-negatives (the task launches via 8.3 short path `WGCBRO~1.EXE`); assert by Win32_Process path / DesliceBrokerDown / heartbeat, never by process name. RESIDUALS: (a) the OVERLAY installer (packaging/payload/install-qwt-improved.ps1, dev path) still deploys gui-agent only — new-file helpers need plan/backup/default-selection work [task #8, P2]; (b) health-check could assert DesliceBrokerDown directly [P3]. [also: architecture, installer, test-framework] [verified 2026-09-04]
- P2 task #26 resize replug: every novel size = monitor hot-replug (re-enumeration + duplication teardown); IddCxMonitorUpdateModes tried and reverted; next = IddCx research on re-parse without arrival, or fix only the capture death (#23). [also: user-facing, architecture] [verified 2026-08-28]
- P2 win11-24H2 resolution-change capture FREEZE (0x887a0026 keyed-mutex-abandoned) — on a resolution change or seamless↔fullscreen switch the agent "recovers" (RecreateDuplication, thread survives, QGAPERF seq keeps advancing) but re-sends STALE frames: captured pixels frozen. win10 recovers correctly (passing control). Different code path from de-slice; NOT hit in steady-state seamless (resize/mode-switch only), so out-of-de-slice-scope (owner ruling 2026-09-04, tracked separately). Fix (owner-endorsed shape): one-time FULL DDA teardown+rebuild (output duplication + shared surface + keyed mutex + D3D ctx) + re-grant framebuffer + full-screen damage, on the resolution-change event — cheap because rare. This is the CLAUDE.md Phase 2B-resize prerequisite bug, now characterized as recovered-but-stale (not thread-death) on 24H2. win11 25H2: UNTESTED (no 25H2 guest on the rig) but EXPECTED AFFECTED — 25H2 (build 26200) ships as an enablement package on the same 24H2 base (26100, Germanium), so it inherits the identical DWM/DXGI DesktopDuplication resolution-change code; confirm with an rnd8 run once a 25H2 image exists. Detector: P4 rnd8 (win11-24H2 FAIL / win10 PASS, judges pixels not logs). [also: rendering, user-facing] [verified 2026-09-04]
- P3 drag residual: announce-quantisation + dom0 apply-lag tail keeps ~11% direction reversals worst-drag; accepted by design (owner), numbers on record — compare against 11%, not 0. [verified 2026-09-01]
- P3 scroll/typing damage-cost attribution vs b299011 baseline recorded closed but closing evidence lost — re-derive from tools/bench-stock-vs-ours.sh before any regression claim. UNVERIFIED

### user-facing
- P3 [SHIPPED AND VERIFIED IN 4.3.26] THE ERROR-NOTIFY ROUTE HAD NEVER DELIVERED A NOTIFICATION IN ANY RELEASE, while its suite reported 10/10. `Send-QwtError` returns `send` as soon as notifhost is LAUNCHED - it deliberately does not wait - and notifhost, run as SYSTEM, passes `SYSTEM` as qrexec-client-vm's local-user field, so the relay never connects back (`relay never connected` in bridge.log). Every real caller is SYSTEM: gui-agent is a service, activate-idd/deactivate-idd run from the installer. Fixed by re-running in the console user's session via the Task Scheduler; verified by a photographed bubble. KEPT AS A STANDING LESSON rather than deleted: the defect was invisible for as long as the suite graded ACKS, and it was caught the same day a dom0 render witness was added - which then needed THREE corrections of its own (whole-screen density loses to a busy desktop; persistence fails because a bubble auto-dismisses; region-local ambient only works with an ASSERTED notification-free baseline). Any check that grades "we handed it off" instead of "a human could see it" is the same bug waiting. [also: test-framework] [verified 2026-09-10]
- P2 BIND-DIRS AND THE ERROR-NOTIFY ROUTE ARE IN-TREE AND WIRED, BUT HAVE NEVER RUN ON A GUEST (2026-09-09). Both landed with their build/installer wiring in the same change (e255c10), so `ours-wins` can fail on their absence - but the offline suites test LOGIC ONLY. bind-dirs' portable core is 296 checks / 0 failed with 8-of-8 injected defects caught; `main.c`, the ntdll implementation of the 12-function BD_FS interface, is NOT covered by any of it and has never been compiled on Windows (no WDK in the dev qube). It runs at **BootExecute**, so a hang takes the boot with it and a bad junction on a system directory is not something a guest recovers from politely - it gets a single-variable run on a DISPOSABLE subject before it goes near a real qube, never as a rider on another change. notify-errors is C 63/63 + PS 53/53 with five defect builds failing, and still owes on a guest: script-context delivery, the marker-store ACL under ProgramData, `--notify-errors` surviving the schtasks `/tr` string, once-per-boot across a real reboot, dom0 actually rendering it, and the negative control (feature on, healthy guest, ZERO notifications). Do not describe either as working until those exist. [also: test-framework] UNVERIFIED
- P2 THE IDD ACTIVATION REMOVES THE ONLY WAY TO SEE A GUEST THAT HAS JUST GONE WRONG. Stage 2 disables the emulated VGA adapter in the INSTALL boot, before qrexec has ever been confirmed on that guest. When the install then fails to bring the agent up, dom0 has no window and no qrexec - which is exactly the state that cost an hour on 2026-09-09 and left the guest unreachable by every channel we have (no qrexec, and no PV console either, because xencons ships with QWT). The stage-1 poweroff fix removes the specific recorded stranding, so this is no longer acute, but the structure remains: we switch off the fallback display before we know the replacement works. RECOMMENDED SHAPE (from the determinism investigation, deliberately NOT implemented): defer only the emulated-VGA DISABLE - creating the IDD devnode is harmless - to a one-shot step gated on qrexec/gui being healthy on a LATER boot, keeping the emulated VGA as the boot-phase instrument through every install reboot. Not done because it reworks a delicate, heavily-worked display-topology sequence (`activate-idd.ps1` plus a new post-qrexec gate), can introduce a transient dual-display period, and must be validated live on its own single-variable run. [also: rendering, guest-stability] [verified 2026-09-09]
- P1 task #31: autologon arming for managed/domain/Windows-Hello images — no LSA-secret path there; deferred, needs a design. [verified 2026-08-28]
- P2 task #28: `service.uac-disable` cannot work on AppVMs (EnableLUA boot-latched, volatile root) — direction: apply in template + loud log; feature acceptance never run. [also: installer] [verified 2026-08-27]
- P2 a pending UAC elevation is invisible in seamless (by design today); UAC visibility is declared future work. [verified 2026-08-28]
- P2 video-modes-during-update: hide or shrink+decorate the guest desktop during servicing, restore on every exit path — planned, not started. [also: updates] UNVERIFIED
- P2 toast-bridge (docs/DESIGN-toast-bridge.md): A0 per-app forward-bridge DONE + acceptance green; per-toast classifier (Phase 3) OPEN. C++ resident shipped — notifhost `--bridge` + agent NotifyBridge gate/supervision (0c9f5eb/05b5bdd); gate default OFF, allowlist HKLM gui-agent `NotifyBridgeAllow`. Acceptance EXISTS and has run: standalone suite `mgmt/harness/a0-toast-bridge.sh` (P1-P8) + `a0-lib.sh` shared instruments + `a0-selftest.sh` live instrument floor. The floor caught a REAL crash regression: the bridge silently terminated on ~2/3 of forward→dismiss cycles — `AgentGone()`'s 5s `CreateToolhelp32Snapshot` stalled the sole worker loop past the agent supervisor's 15s heartbeat deadline, and the supervisor's delete-task-first relaunch killed the live bridge artifact-free; A/B-bisected (250b007f 8/8 clean vs ~2/3 crashing), fixed with snapshot-free `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` liveness + 30s throttle (71d609c). Instrument overhaul landed after audits (blog_len constant-10, self-matching heartbeat probes, unconfirmed fires graded as bridge behavior — f45a65e/3f6bffa/294d351/4a0ff74); P6c relay-kill RETIRED — no separate relay process exists to kill (the vchan rides a WMI-unidentifiable qrexec-wrapper), reconnect is asserted by P6b's consent Deny→FATAL-exit→Allow→reconnect cycle (c529171/c8012e2); P8 stale pre-boot-heartbeat race fixed (c8012e2). Acceptance CONFIRMED GREEN 2026-09-05 — 11/11 verdict rows PASS, 0 FAIL/INSTRUMENT, exit 0 (HEAD c8012e2, pkg 4.3.18+agent.8840ca9, 3 cold boots, ~22 min); the AgentGone crash fix and the P8 heartbeat-race fix (hbseen absent) both hold. REGISTERED in the canonical protocol as part `p6-toast-bridge` (wrapped suite + floor gate, 6 scenarios, dry fail-proofs harvested, `a0-instrument-floor` live-proofed via the bisect; protocol/selftest.sh ALL GREEN incl. the regenerated whole-campaign walk); harnesses now honor `A0_OUT` and emit INSTRUMENT-class P1c/P1d verdicts. OPEN next: the per-TOAST classifier (Phase 3) — read scenario=reminder/actions per notification (not just per-AUMID) to split window-path vs informational, synthetic "default"/Open action, verdict channel + harness (3a probe -> 3b classifier -> 3c verdict channel -> 3d harness); not started. (Phase 2's synthetic action folds into this.) ETW push tier: proxy exit-5 RE-diagnosed 2026-09-06 — the 09-05 grant-ordering fix (agent ff1c787) was necessary but NOT sufficient (rig-refuted with build identity proven); the remaining denial is the grant MASK: ProcessTrace's realtime setup implicitly issues EVENT_TRACE_CONTROL_QUERY on the session (sechost!EtwpQueryRealTimeTraceProperties) and the grant lacked WMIGUID_QUERY — mask widened to TRACELOG_ACCESS_REALTIME|WMIGUID_QUERY (etwproxy.c EtwCtlGrantConsumer; p3a-etw-gate.sh T2 grep updated to the new log text), awaiting CI+rig. The per-session EventAccessControl grant IS the MSDN-documented non-PLU realtime-consume route, so NO owner decision is pending; if the widened mask still exits 5, ONE diagnostic rung remains (+WMIGUID_NOTIFICATION, PLU's session subset) before any PLU/privileged-broker discussion. [also: architecture, test-framework] UNVERIFIED
- P3 dom0-menu click of the new Terminal / File Explorer app-menu entries after qvm-sync-appmenus — guest half verified, dom0 half not. [verified 2026-09-01]
- P3 Start-in-seamless stays off by owner decision; re-enable path is SeamlessStart=1 + docs/PLAN-start-menu.md when wanted. [verified 2026-09-01]

### rendering
- P3 THE AGENT DEFERS CONSOLE WINDOWS FOR A SHADOW-CROP THEY CANNOT HAVE. Measured 4.3.25 acceptance 2026-09-10: a `CASCADIA_HOSTING_WINDOW_CLASS` window (Windows Terminal / conhost, opened here by the harness's own `qtest run` probes) was held ~700 ms by crop-before-show, timed out, and was mapped UNCROPPED with a QGACROPLATE warning - while the real toasts in the same run cropped correctly (measurements landed 15 ms and 203 ms BEFORE their maps). A console window has no shadow strip to measure, so the UIA lookup can only ever time out: the cost is a 700 ms map delay on every such window plus a warning line that looks like a rendering defect and is not. The defer predicate should not admit it. NOT hidden in the test: crop-before-map.sh now scopes its VERDICT to toast/menu windows - counting console windows is what produced a retracted claim on 2026-09-09 - but reports the console occurrences as a NOTE, because filtering them away silently would be the bug. [also: graphics-performance] [verified 2026-09-10]
- P2 TOAST OVERCROP — ROOT CAUSE = the slide-in ANIMATION RACE (owner-reported "back up your PC" wrong geometry; PINNED 2026-09-06 from the agent log at `Q:\Qubes Logs`). Toasts slide in from the right; when the agent's UIA card measurement lands MID-SLIDE it sees only the card's right strip → `TcApplyResult` computes `insets l=<big> t=30 r=1 b=13` (huge left, ~1px right) → cropped short + shifted right. PROVEN: the SAME complex 3-image toast measured `l=209 r=1` (→186×173) / `l=53 r=1` / `l=105 r=1` with animation ON, and `l=16 t=30 r=16 b=13` (correct card) THREE times in a row with `SPI_SETCLIENTAREAANIMATION=0`. Simple/hero toasts settle fast so they rarely race (measured correct); complex/taller toasts race often. Fable's race guard (toastcrop.c:623) only re-checks the WINDOW rect (stable during the slide), so it misses the card sliding INSIDE a static window (its own path-(f)). COMPLETE FIX SET (all committed together, agent 7c1439c, owner "I need ALL the fixes ... at once" 2026-09-06): (1) PROPER CROP — toasts use raw-largest (311b016) + asymmetry guard `|l-r|>32` rejects a mid-slide read (2117027); (2) ANIMATIONS OFF — SetShadowsMain disables SPI_SETCLIENTAREAANIMATION on the user token (2117027, log-confirmed applied); (3) FAST FOR SIMPLE TOASTS — the 50ms settle re-walk is CONDITIONAL on the asymmetry check (eda7dd2), so a clean symmetric read accepts immediately with zero added latency; (4) CROP-BEFORE-SHOW — geometry hold (MapDeferred/CROP_BEFORE_SHOW_TIMEOUT_MS=400) was always on; the CONTENT hold (SliceMapHold, main.c:199) was DEFAULT-OFF and is the piece I deferred and never turned on — a slice-fed window maps with a zeroed slab → "black borders flash before it settles" (owner-reported) → flipped DEFAULT ON (7c1439c, registry SliceMapHold=0 restores old behavior, chrome scope 1 = slice path; de-slice/broker path has its own first-frame fix). Nothing else was reverted/discarded — audited 2026-09-06. **CROP FIXES (1,2,3 + the geometry half of 4) are GOOD** (overcrop fixed, pixel-confirmed content toast on the eda7dd2 build). **BUT the SliceMapHold CONTENT-hold (7c1439c) is a BLACK REGRESSION, not shippable**: rig-confirmed BY PIXELS on win11 24H2 — a slice-fed toast (PwNoteSliceFedMap; de-slice guest but the toast is slice-fed) maps PERSISTENTLY BLACK with SliceMapHold=1, because SliceContentReady (main.c:2697) releases on `PwSliceContentTick!=0` and PwNoteSliceContent (main.c:5547) sets that on the FIRST composite copy — which is the zeroed/black slab; the real content lands seconds later. So the readiness predicate means "a copy happened", not "painted". SliceMapHold=0 avoids black but maps-then-paints (the appear-delay = map-before-render). FIX IN PROGRESS: make the readiness predicate mean ACTUALLY painted (non-black), bounded by CROP_BEFORE_SHOW_TIMEOUT_MS fail-open. Validate BY PIXELS (an earlier log-based PASS was false — the log said content-ready while the toast was black; judge-output-not-logs). FIXED + PIXEL-PROVEN (agent 58de415, 2026-09-06): the readiness predicate now means PAINTED (slicepaint.h SlicePainted: 8x8 grid, painted iff >=2% samples have B/G/R>16), set on the window's own buffer, re-sampled every copy until non-black; SliceContentReady = PwSliceContentTick only; SliceChromeContentReady level 1 now content-holds slice-fed TOASTS on the de-slice path too (de-slice MENUS unchanged). Offline suite slicepaint_test.c 15/15, DEFECT_ANYCOPY 5-fail. RIG PIXEL result (win11 24H2, judged by cropping the toast rect + PIL mean, NOT the log): the complex bitmap toast renders NON-BLACK (mean 163, 100% non-black, hero+text+buttons visible - image sent to owner), map after paint (QGASLICEMAP painted=1). SliceMapHold stays default ON. RESIDUALS (polish, NOT black): (a) the map releases at the ~400ms CROP_BEFORE_SHOW_TIMEOUT rather than instantly on the paint signal (held_ms=422; a bounded delay, far better than SliceMapHold=0's "few seconds", but wire release-on-paint for snappiness); (b) BROKERDIMS feed-loss fires once during the toast's size transition (broker keeps a window-sized WGC channel across the card-size PwResizeWindow re-registration; frame 396x415 != slab 396x579 rejected) - transient, the toast recovers non-black within the hold, but fix the broker to size for the full window / re-open the channel on re-registration; (c) rapid-fire History.Clear can clear a toast before it paints (painted=0) - test artifact, not user-facing. NOTE: the earlier "control-view union overcrops" theory is WRONG — the union INCLUDES the FlexibleToastView card, so it does not overcrop; with animation off BOTH the union and raw-largest rules measure the card correctly. The shipped `raw-largest` change (agent 311b016) is therefore a correctness tidy-up, not the fix; kept as belt-and-suspenders (tested to pick the card) but the animation-disable is the real fix. Old (superseded) framing follows for the record: [diagnosed 2026-09-06, code-confirmed, live-corroborated). The 2026-09-03 card-finder rewrite (agent 9399acd control-view-first + 78a602e union, BOTH ancestors of HEAD 96330e5 = shipped) makes toastcrop.c:583-599 prefer the UIA ControlViewWalker (raw view only as fallback), so a toast is cropped to the UNION of its content controls (text/buttons/logo) INSIDE the card's padding+rounded border instead of the drawn card — a SILENT OVERCROP, the exact failure the module's own header (toastcrop.c:35-38) says it must never have. Passes every guard (a ~85%x75% content box clears the 40% floor) and logs as success ("toast card in WxH window, insets ..."). BackupReminder is the toast the module was TUNED on (toastcrop.h: 396x332 window carried a 377x287 card, insets ~2/31/17/14, measured 2026-08-11 under the OLD raw-view finder); no toast re-measured after 09-03 (windowing.md:25 asserts union==card by reasoning, only measured a menu). Because entry->X/Y/W/H also drives the per-window CAPTURE crop (capture.md:36), the pixels are cut to match. LIVE corroboration (win11-a0tb 24H2, 2026-09-06): a realchoice reminder toast rendered at 214x157 vs the 377x287 baseline — consistent with overcrop (raw size not captured; the insets line needs the agent log path, elusive this session). FIX (ranked, NOT implemented): (1) per-surface view — menus keep control-view-first (the 09-03 fix that killed menu edge strips), TOASTS use raw view (the pre-09-03 rule that produced the 08-11 numbers); (2) never crop a toast tighter than its largest single qualifying element; (3) ship a log line dumping both views' unions so the field can classify without a rig. Picking (1) vs (2) needs one live measurement: dump BOTH RawViewWalker and ControlViewWalker subtrees for a real toast (is the card element in the control view on 24H2?). REPRO: bridge off / service.legacy-toasts, fire a reminder-scenario toast as the interactive user, fullshot geometry.txt + winenum + the insets log; PASS iff dom0 rect == raw-view card ±2px with the rounded border intact. NOTE: the misc.md:17 "no w/h sanity guard" theory is STALE — SanitizeWireGeometry (send.c:105) refuses non-positive w/h; a negative rect gives an invisible toast, not a wrong-geometry one. [also: user-facing] [verified 2026-09-06]
- P2 guest title-bar hiding parked opt-in OFF: dom0 WM sends WINDOW_FLAG_MINIMIZE to caption-stripped windows after a raise — dom0-side mechanism unexplained. [verified 2026-09-01]
- P2 drag distortion when an override-redirect modal appears mid-drag — never reproduced; needs the exact modal + winwatch (shots are o-r-blind); plain overlap exonerated by measurement. (2026-08-16 record; history erased.) UNVERIFIED
- P2 menu-overhang clipping: synthesized menu painted into the owner's buffer would truncate an overhanging menu in dom0 while looking right in-guest — never measured either way. UNVERIFIED
- P3 DWM_CLOAKED_SHELL fullscreen ApplicationFrameWindow once mapped as a blank fullscreen rectangle — last recorded unfixed; check the current predicate before re-deriving (blanket cloak filters stay banned). [verified 2026-08-27]
- P3 work area under-margined for first ~3.5 min of boot until dom0's feed arrives; Explorer fights the work-area value forever (converges, churns 2 s). [verified 2026-09-01]
- P3 field "exit status 46" on seamless switch: localized to the qrexec launch path, exact Win32 error unknown — needs the reporter's qrexec-wrapper log. [verified 2026-08-29]

### test-framework
- P2 [FIXED 2026-09-09, fail-proofed] THE RESCUE GATE WAS DEFEATED BY MISSING DATA - prime-run's cpu reader printed the sentinel `9999` ("very busy") whenever libxl stats were UNREADABLE, so an unreadable stat and a busy guest were the same value, and it was the one value that disables recovery. Measured: win11-clean, 4.3.22 campaign, `admin.vm.Stats` went unreadable at the instant the install stuck (`cpu=` empty from t+414s to the end), every poll reset the quiet streak, `quiet >= 8` was never reached, the one-shot rescue never fired, and the run polled to the 3600 s DEADLINE. The floor condition held throughout - ONLY the streak was defeated. Fix: `NA` instead of `9999`, an unreadable cpu counts toward quiescence, plus a second INDEPENDENT trigger (`RESCUE_NOSHOW`, four consecutive no-window screen probes) because that is the direct observable of this stall and needs no stats. Fail-proof `mgmt/harness/failproof-prime-rescue.sh` replays the real recorded telemetry: defect re-introduced -> NEVER rescues (reproduces the deadline), fixed -> rescues at t+647s, healthy warm-reboot trace -> does not fire; 12/12. THE UNDERLYING TRIGGER IS NOT OURS AND IS NOT FIXED: a guest-initiated stage-1 reboot on a Xen HVM sometimes HALTS the domain and sometimes warm-resets it in place; on a warm reset the PV bus usually rebinds but occasionally does not, and stage 2's IDD activation then disables the emulated VGA so dom0 sees no window at all. That nondeterminism predates 4.3.22 (4.3.21 passed with it) and is why the harness needs a rescue that works. [verified 2026-09-09]
- P2 THE EVIDENCE LOCK DOES NOT GUARD THE PATH THAT ACTUALLY DESTROYS SUBJECTS. **CONFIRMED IN THE FIELD 2026-09-09, and it cost real evidence**: cell 4 of the 4.3.22 campaign re-primed `win11-acc` at 11:50 and destroyed the guest that had just failed win11-clean, before it could be interrogated - exactly the mechanism described below, six hours after it was filed and left unfixed. The diagnosis survived only because the telemetry was already on disk. NOTE THE TENSION before "fixing" this by simply refusing: had prime-run honoured the lock, cell 4 would have refused, and cell 4 is precisely what produced the decisive datum (the SAME package passing win11-clean forty minutes after it failed). The right shape is therefore NOT a bare refusal that cascade-kills the campaign, but archive-then-release: the campaign runner takes what it needs from the failed guest and releases the lock deliberately, and prime-run refuses only on a lock nobody has released (`EV_FORCE=1` to override). ORIGINAL REPORT: `evidence-lock.sh` opens with "make destroying a failed guest IMPOSSIBLE, not discouraged", and that is true only of `ev_reap`. `mgmt/harness/prime-run.sh:131` does a bare `qvm-remove -f "$CHURN"` with no `ev_locked` consultation, and prime-run is what every cell calls to create its subject - so a guest locked as evidence is removed by the next cell that primes that name, silently, having never consulted the lock. PROVEN 2026-09-09, not inferred: `mgmt/fixtures/win11-acc.evidence-lock` (locked 2026-09-08T21:27:57Z, "FAILED cell c-win11-reinstall, 20 passed 2 failed") was still on disk while the 4.3.21 final campaign's cell 1 re-primed `win11-acc` from `win11-base` and passed 12/0. The lock survived; the evidence it named did not. This is the same class as the failure `ev_lock_survivors` was written to stop (win11-upgrade and win11-appvm destroyed by the next cell's reap) - the mechanism was added to the reap and not to the destroyer. FIX: `ev_locked` check inside prime-run before the `qvm-remove -f`, refusing with the lock's reason unless `EV_FORCE=1`. SECOND, SMALLER DEFECT in the same area: nothing ever calls `ev_release`, so locks accumulate across campaigns and a stale one makes every subsequent reap refuse and leave a guest running. Harmless here (the previous 6/6-clean run passed with this same lock present, and the guest was Halted), but it means "this guest is evidence" degrades to noise exactly as it becomes routine. [verified 2026-09-09]
- P2 harness lifecycle redesign (2026-09-06) awaits LIVE validation. The failure it fixes, observed live: killing a harness (p3a-etw-gate.sh) left prime-run + sleeps orphaned 40+ min HOLDING the guest vm-lock (the old vmlock.sh `exec {FD}` flock fd was inherited by every child — and could never be made CLOEXEC, since `flock -n $FD` depends on inheritance), no stale-holder takeover, no teardown, and a lock-bounced run (rc=2) looked identical to "still priming" in a tailed log. FIXED in code: vmlock.sh holder-process redesign (lock fd lives ONLY in a `flock -n -o` waiter whose `tail --pid=<leader>` sentinel dies with the job — children cannot inherit it, lock self-frees ~2s after leader death even on SIGKILL; dead-holder takeover + last-resort file rotation; `vm_unlock`; `VMLOCK-ACQUIRED/RELEASED` banners), new run-lib.sh (`job_init` EXIT/TERM/INT/HUP traps: explicit-pid `kill_tree` — never pkill -f — then vm_unlock LAST, one "TORE DOWN" line; `rl_fg` interruptible long-foreground wrapper — bash defers traps during foreground children, a TERM mid-prime-run would otherwise defer teardown up to 1h; `run_verified` launcher distinguishing STARTED/BOUNCED/NOT-CONFIRMED; deliberately NO chaining helper — auto-chaining onto a dead job corrupted a live run), wired into a0-toast-bridge/p3a-etw-gate/a0-p3-toast-split/prime-run (prime-run also writes `mgmt/fixtures/<vm>.aborted` CONTAMINATED marker + best-effort shutdown on signal abort, cleared on next successful clone), protocol/run.py dead-holder retry + banner (its python fd was already non-inheritable). Local proofs all pass (TERM→rc143 in 1s, 0 orphans, lock freed, nested child never releases parent's lock; SIGKILL→lock self-frees ~2s; refusal rc=2 instant+loud; takeover 2s; run_verified both paths; lint gate clean). OPUS MUST VALIDATE LIVE: kill a real run mid-prime → zero orphans, lock frees, aborted-marker written, next run starts clean and is NOT auto-chained. Residual: SIGKILL of a leader skips traps — bounded orphans possible (lock still self-frees); stop jobs with SIGTERM to the leader pid. [also: architecture] UNVERIFIED
- P2 first LIVE campaign through protocol/run.py is the single forward item (owner, 2026-09-01: the Fable-rewritten, Sonnet-eval-verified runner IS the framework; the legacy bash-harness lineage, its meltdown-era campaign bookkeeping, and its lint backlog are PURGED). The runner's own gates subsume the old debt: falsifiability at load time, PASS refused without a recorded fail-proof, live-grade proofs required in live mode. Whatever matters gets re-established by that campaign, not by re-running old cells. [verified 2026-09-01]
- P3 verdict-grading defect FIXED + p3a-live-validated (2026-09-06, commit d22ec3c): the p3a/a0 wraps summed `grep -cE 'FAIL|INSTRUMENT'`, folding ungraded probes and un-stageable drills (a drill that can't break a reliable primary) into the same blocking integer as a product FAIL — so a proven ETW mechanism kept rendering PICK-ETW-BLOCKED (this was the whole "clean PICK-ETW is elusive" saga; it was harness gradability, not the bridge). New `mgmt/harness/verdict-lib.sh` three-class aggregator (FAIL gates / INVALID = ungraded own-channel "re-run" / PASS-DATUM = un-stageable-because-reliable, a pass); p3a GATE de-conjuncted (BLOCKED only on a real FAIL), T6 re-graded to grade push+precise+no-regression (median parity is correct — both tiers do the same wpndb read), a0/a0-p3-split same fix, matrix additive counter. The correct framework both should have used (protocol/run.py + campaign-verdict.sh) already forbids the fold. BOTH harnesses live-confirmed CLEAN on the rig (pkg 99a425b): p3a → PICK-ETW STATUS=CLEAN 0 FAIL + dom0 render witness T7w read (bubble in pixels); a0 full-package → STATUS=CLEAN 0 FAIL, all 9 phases PASS. Offline verdict-lib-selftest 30/30 (injected FAIL still BLOCKS). a0 now also carries the dom0 witness: P4a was already capturing p4a-dom0.tar at the forward ack but hardcoded `dom0=n/a` — added P4w (grade the tar → ATTENDED-PENDING / INSTRUMENT, mirrors p3a T7w); the last a0 run's capture read as a real 'win10-a0tb: A0T bridged' dom0 bubble. RESIDUALS: T7w/P4w capture-flake → run non-green (UNDECIDED on p3a, EXECUTED-WITH-GAPS on a0) — deliberate safe direction, watch; T6 thresholds (25ms/1.5x) held on first live medians, tune if noisy. [also: architecture] [verified 2026-09-06]
- P3 legacy lint findings (40) are BASELINED in tools/lint-baseline.txt — not open work; editing a legacy harness re-exposes its findings (line-anchored), so touched code gets fixed and NEW findings still fail the gate. Never add entries to the baseline. [verified 2026-09-01]

### network
- P2 live netvm handling: raw admin.vm.property.Set on a running guest strands the vif without changing the property (qubesd aborts replyless) — dom0-side question, candidate upstream report. [also: upstream] [verified 2026-08-23]
- P3 Windows PV TCP connect tail ~5x vs Linux qube (p99 65 vs 49 ms) + ~8 ms DNS overhead; IPv6 hypothesis disproven. [verified 2026-08-24]
- P3 goldens/checkpoint restores may carry another lineage's stale `Q:\qwtng-netcfg.txt` L3 cache — never measured. UNVERIFIED
- P3 mirage upstream PRs (mirage-net-xen#121, qubes-mirage-firewall#232) status unknown since 2026-08-24 — re-check before relying. [verified 2026-08-24]

### debug-introspection
- DONE: EMS armed (both templates, AppVM-inherited) and the serial channel proven end-to-end — SERIALMARK x3 land in guest-<vm>-dm.log. EMS/SAC silence on a healthy client boot is expected (SAC is Server-only); the bugcheck-time payoff is unprovable from userspace and EMS won't diagnose the IPI wedge regardless — insurance only. [verified 2026-09-02]
- P3 does `/var/log/xen/console/guest-<vm>.log` survive a domain restart — one cheap check with the existing HELPER VALIDATION marker. [verified 2026-09-01]
- P3 kernel heartbeat writer (would timestamp wedge onset in a dom0 log) — not built. [verified 2026-09-01]
- P3 OWNER GATES: gui-agent log sink onto the console writer; unauthenticated SYSTEM console shell. Both security tradeoffs, not technical ones. [verified 2026-09-01]

### updates
- P3 U1 (update availability to dom0) is UNMEASURED post-void — the 08-30 "scan failure" was ruled a meltdown hallucination by the owner; one clean live scan verifies the cell when convenient. [verified 2026-09-01]
- P2 open deployment items: win11-fresh relay/updater deploy returned empty (cause unknown); `-Scheduled` missing from scan tasks on 3 guests; live large-download acceptance for the relay truncation fix never re-run. UNVERIFIED
- P2 WU dialog defects: cannot be moved (jumps back — not root-caused) and its modality is invisible to dom0 (transient_for=0) — three options pending with the owner. [also: rendering, user-facing] [verified 2026-08-30]
- P3 plain-HTTP qrexec/vchan transport loss (~1/3 of responses) — not our code, not yet established as an upstream defect; needs a repro from a Linux qube or after a proxy restart. [also: upstream] [verified 2026-08-14]
- P3 Win10 ESU: real 2025-11 CU resolves and is installable; the gate is CBS ESU entitlement — owner MAK/licensing decision. [verified 2026-08-20]
- P3 U3 dom0-driven install drain has never run on a TemplateVM proper (close with UBR + CBS state=112 on win10-tpl). [verified 2026-08-20]

### installer
- P2 the media insertion path (release ISO attached, autorun launches installer) has never been tested live; the primer-task-in-goldens claim also needs re-verification. UNVERIFIED
- P2 most user-facing switches (`/nonet` `/nodisk` `/noapptweaks` `/reboot` `/noupdates`, `/updatesonly` success path) have never been executed by an automated gate; `/iddonly` `/iddoff` `/updatesonly` bypass Test-Payload (no SHA verification of the medium). [verified 2026-09-01]
- P3 non-`/auto` runs keep the console open at the end — a qrexec-driven `/iddoff`//`/iddonly` without `/auto` hangs on a display-less guest; recorded 2026-08-16, never investigated. [verified 2026-09-01]
- P3 the %~f0/shift elevation fix has no regression check that can fail here (all rigs run EnableLUA=0, the relaunch path is structurally unreachable). [verified 2026-08-16]

### architecture
- P2 leak-free agent exit needs an ack-gated revoke handshake (guest cannot safely revoke while dom0 maps) — Phase 3: design writeup + owner review before code. [verified 2026-08-05]
- P2 the public/internal repo split (findings/, mgmt/, internal docs/ out of the public repo) — awaiting the owner's boundary definition; until then internal-material commits are not pushed. [verified 2026-09-01]
- P3 per-window WGC revisit (would kill the PrintWindow limitations class structurally) — must survive total WGC absence in SYSTEM/session-1 context. [verified 2026-08-09]

### upstream
- P1 reports drafted and BLOCKED ON OWNER TEXT APPROVAL: xen winpv gnttab revoke-spin (docs/upstream-xen-pv-grant-revoke-spin.md — driver versions TODO), xl console overflow (docs/upstream-xl-console-overflow.md), gui-daemon vchan-EOF + restart_guid UAF (DESIGN-gui-daemon-restart-survival.md §3), xenvif ctrl-ring (patches/xenvif-ctrl-ring-fix.patch — last recorded unsent), qubesdb-cmd optind write bug. The IPI-wedge class qualifies but has NO draft yet. [verified 2026-09-01]
