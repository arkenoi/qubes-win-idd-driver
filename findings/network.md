# network — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- Netvm roster: `core-net` = conventional Linux netback; `fw-net` = qubes-mirage-firewall, the owner's production firewall (~28 Linux qubes), carrying our patched unikernel since 2026-08-24 — both carry Windows PV traffic at full rate. Installs happen offline (`netvm ''`); attach afterwards. Templates carry `netvm=''` FOREVER (a template with a netvm = STOP); AppVMs/StandaloneVMs MUST have one to test PV networking. [verified 2026-08-29]
- PV networking ships NETVM-FREE via the unplug latch (`PRIME_NETVM=latch`, `mgmt/clone-to-template.sh` + `guest/pvnic-selfprime.ps1`): `xen.sys` reads `Services\XEN\Unplug\NICS` each boot (delete-on-read), vetoing it unless some `Enum\XENBUS` subkey name contains "VIF" (the latch seeds that veto-bypass key); an armed latch lets xenvif's NET-child start complete in ONE boot at problem 0 instead of demanding a restart (pdo.c PdoStartDevice). Mechanism verified in pvdrivers source. [verified 2026-08-19]
- Delete-on-read makes re-arm load-bearing: ANY template boot consumes NICS — an innocent inspection boot ships an un-latched template and reset-loops every AppVM. Re-arm layers: QubesPvNic boot task + QubesPvNicRearm (event-1074) task, QwtngNetSetup arming the latch as its FIRST act (~8–12 s), the installer arming as its last act before the install shutdown (with readback), and `scrub_net_identity` re-arming and FAILING the build on no readback. Defect-reintroduction proof: both tasks disabled + one template boot → AppVM died at 20 s. [verified 2026-08-23]
- The 4.3.4 shipped installer left every user template un-latched (only the internal pipeline re-armed) — the field AppVM-dies-seconds-after-start reports (forum post 89; GWeck 56/70); reproduced on demand (NICS=0 → Dying at ~20 s) and fixed by the installer-time + service-time arms above. [verified 2026-08-25]
- The latch is UNCONDITIONAL for every qube class (owner decision D1, commit `cace671`): StandaloneVMs included; a standalone reading `skipped-non-template` is today a REGRESSION (docs/ACCEPTANCE-PROTOCOL.md C11, corrected 2026-08-30, measured on win10-c1 + win10-tpl). [verified 2026-08-30]
- Why the latch exists: an AppVM's volatile root reinstalls xenvif/xennet fresh from the (already staged) DriverStore EVERY networked boot, minting a new NetCfgInstanceId each time — so no GUID-keyed config can ship, and one-boot completion of that per-boot install IS the acceptance property (protocol ST4: "transient by design, never parked"). [verified 2026-08-19]
- Offline seeding alternatives are all measured dead: copying primed registry/device state creates a one-way-door devnode (PnP problem 31, NIC silently dead forever — tested twice); synthesising the devnode is refused by SetupAPI (0xE0000209 on a foreign bus enumerator); GUID pinning has zero consumers. Priming through a real netvm (`PRIME_NETVM=<netvm>`) still works and coexists with the latch, but is never used on templates. [verified 2026-08-19]
- Applier (`guest/pvnic-selfprime.ps1`) is PURE qubesdb: stock detection/value sources dropped; `VifDevicePresent` must be `-PresentOnly` (ghost devnodes from a detached netvm otherwise hang the no-netvm quiet-exit); DNS read from `/qubes-primary-dns`/`-secondary-dns` with 10.139.1.1/.2 as fallback only; gateway ping is NOT part of the verdict (a Qubes/mirage gateway never echoes — the "network configuration FAILED" alert on a healthy 12 MB/s link was our own ICMP check). qubesdb-cmd CLI is broken both directions; the client DLL via P/Invoke is what works. [verified 2026-08-23]
- Early L3 config: stock `network-setup.exe` is DELETED (its second per-boot run tore down a correct address = the old mid-boot outage); replaced by `QwtngNetSetup` (native C# auto-start service, `bin\qwtng-netsetup.exe`, embedded in pvnic-selfprime.ps1). Design constraints all measured: waits for adapter `OperationalStatus.Up` (applying mid-PV-install makes the AppVM halt itself), netsh not WMI (`EnableStatic` returns 66 on the Qubes /32), address and route set SEPARATELY (the combined netsh form pings the gateway, +10 s on mirage), L3 cache on `Q:\qwtng-netcfg.txt` (qubesdb is not open at ~12 s), then a resident 2 s reconciler that re-reads qubesdb and never touches a correct config. First traffic 13–21 s, 0 failures/21 samples per cold boot (~22–28 s from qvm-start; NLA is not a bottleneck). [verified 2026-08-23]
- QwtngNetSetup review fixes shipped in 4.3.7: qdb connection/buffer leak fixed (one connection for the service's life + qdb_free, verified live), sc-create fail-closed with the stock applier deleted only AFTER the replacement registers, csc stale-exe mask removed, log rotation moved to `Q:\Qubes Logs`. [verified 2026-08-25]
- DHCP is not part of the design and is OFF in the image: `scrub_net_identity` (both prime paths, own offline boot) clears leases/DUID/NetworkList, sets `EnableDHCP=0` on every interface, WRITES `NameServer=10.139.1.1,10.139.1.2`, re-arms the latch, and fails the build on residue readback. Root cause of the contamination it prevents: `qvm-firewall` drop rules are FORWARD-only and never stopped DHCP to the gateway itself, so vif-priming leaked leases into shipped templates. A Linux qube runs no DHCP client at all; Windows now matches. [verified 2026-08-23]
- Retraction guard: do NOT disable the DHCP Client SERVICE (`Services\Dhcp\Start=4`) — measured worse (first traffic 51–58 s vs 25–33 s) because it drives NLA and the NetworkProfile event the applier task triggers on; the scrub now ENFORCES `Start=2` with the measurement in a comment. [verified 2026-08-23]
- Live netvm switch via `qvm-prefs` on a RUNNING guest works: address never lost, default route back ~1.5 s after the vif, total outage ~4 s of vif re-establishment (NET-5, evidence in `Q:\qwtng-netsetup.log`). The earlier "reconciler cut 13 s→6 s" claim was RETRACTED — that binary had never compiled; the ~4 s figure is from a pipeline-built pair with the service confirmed RUNNING. [verified 2026-08-23]
- Stranding trap: raw `admin.vm.property.Set+netvm` against a RUNNING guest from this dev qube returns an EMPTY reply, tears the vif down WITHOUT changing the property — guest stranded until restart (4/4; qubesd aborts without replying, cause unreachable from here; open dom0-side question). Halted Set is clean both ways. `qvm-prefs` works live only when the CURRENT netvm value resolves in this qube's `admin.vm.List` (fw-net does not), and even a failed rc=1 qvm-prefs tears the vif. [verified 2026-08-23]
- Acceptance headline met (NET-2/NET-7): immediate live netvm attach with ZERO reboots — AppVM `win11-app` PV NIC bound in 26 s, StandaloneVM `win10-u10` in 25 s (`LastBootUpTime` byte-identical, 0 dialogs, emulated NIC unplugged, health-check ok). A SECOND BOOT IS A FAILURE — earlier "first-vif needs two boots" and "hotplug fails / standalone does not meet acceptance" are RETRACTED: every failing subject simply had no applier (pre-`cace671`). Verify the applier is present (`TASK QubesPvNic` + script + `UNPLUG_NICS=1`) BEFORE grading; that omission cost three runs. [verified 2026-08-29]
- Traffic is asserted by FILE TRANSFER cross-checked against the XENVIF adapter's own rx_bytes delta (ref: 16,192,808 B, delta 21,966,263) — never by pinging the gateway (netvms don't echo; the ping criterion inverted verdicts on guests moving megabytes). DNS/TCP = smoke only. Allow ~90 s after qrexec before grading (instant: dns=False/rx=153,487; +90 s: dns=True/rx=9,463,443). Throughput benchmarks go against a fast CDN (NET-8 ref: 258.2 Mbit/s via 25 MB Cloudflare `__down`; 100 MB returns 403) — a mirror download (0.2–0.7 MB/s, 3× session spread) characterises the upstream, never the PV link. [verified 2026-08-29]
- 4.3.16 campaign P2: 28 network checks, 0 FAIL (ran on win10-app/win11-app/win10-c1, subjects untouched by that campaign's contamination): zero-reboot live attach on both OSes, 25 MB proven by rx_bytes, 3/3 boot soak with real qubesdb IP and no APIPA, zero premature-reboot dialogs across 289 samples on genuinely never-vif guests (docs/ACCEPTANCE-4.3.16.md). Standing consequence: `win10-c1`/`win11-c1` have now had a vif and are no longer NET-6-eligible; a future first-vif cell needs a fresh clean-install exit. [verified 2026-08-30]
- Premature reboot dialog, final form: OUR package raises none (0/69 with the watcher armed BEFORE the first-ever vif on a never-vif guest); STOCK QWT's first-logon install DOES raise it (csrss `#32770`, title "Xen", "Xen PV Network Class needs to restart the system…"). It is a NETWORK-path event and cannot appear on `netvm=''` — any no-dialog result without a vif is vacuous (the old "0/310 samples" was). Our suppressor clears the pending Request and disables xenbus_monitor but cannot dismiss an already-displayed csrss window. [verified 2026-08-29]
- NET-6 eligibility discriminator: XENVIF enum absent/empty INCLUDING ghosts (`-IncludeHidden`) + zero `XENVIF\*` PnP devices. `Enum\XENBUS\VEN_XP0001&DEV_VIF` is the latch's seeded veto-bypass key and is NOT evidence a vif was ever seen (docs/ACCEPTANCE-PROTOCOL.md NET-6, corrected 2026-08-30). [verified 2026-08-30]
- QWT 4.2.2 payload defect (closed): its own xenvif enumerates the NET child at REV_09000004 max while its own xennet claims only REV_09000005 — no HWID intersects, PnP code 28, guest silently falls back to the emulated Realtek. Proven on a clean never-touched guest; fixed by our xenvif built from xenbits master (rev 5 / UNPLUG v3, pv-xenvif CI pipeline; everything else in the MSI except gui-agent/IDD is staged bit-for-bit from the GPG-verified stock 4.2.2 MSI). `install_patched_xenvif()` must run BEFORE priming — a PV INF AddReg rewrites NICS with no NOCLOBBER — and the pipeline fails the build if the driver isn't confirmed (a template shipping stock xenvif reproduces cmErr 43 on mirage). [verified 2026-08-23]
- xenvif upstream defect (ours to report): with no backend `feature-ctrl-ring` the control ring is never created yet ControllerEnable sets Enabled → RING_FULL trivially true → FrontendEnable fails → cmErr 43, whole NIC dead. Fixed unconditionally in our fork (`patches/xenvif-ctrl-ring-fix.patch`, deliberately split from the opt-in diagnostic). win-pv-devel report drafted but UNSENT as last recorded — needs owner approval of the text; re-check status before assuming sent. [verified 2026-08-22]
- Mirage arc, final: a Windows HVM on UNPATCHED qubes-mirage-firewall wedges the guest (xenvif busy-waits at DISPATCH_LEVEL under Frontend->Lock — ~2 cores, no qrexec, no ACPI; unbounded on the Qubes-vendored xenvif). Five defects fixed: mirage-net-xen close/reconnect state machine, `disconnect_backend` deleting the backend dir (read as hot-unplug → permanent NIC eject), InitWait-to-closing livelock + unguarded `Lwt.async` (crashed the live firewall), and the throughput collapse — mirage never set `NETRXF_data_validated` on aggregated GSO frames, so xenvif's re-segmented copies failed checksum verify and TCP equilibrated at ~0.05 MB/s (one-line fix `12fc0d4`). Submitted build measured on the rig 2026-08-24: Windows 15–16 MB/s, Linux 15.5–17.7 MB/s on the same backend, no Linux-pair regression (`rx_gso_checksum_fixup` falsifier held). [verified 2026-08-24]
- Mirage upstream status: mirage/mirage-net-xen#121 and mirage/qubes-mirage-firewall#232 published 2026-08-24 (owner-approved), with the library-first ordering ("#232 cannot go green until #121 releases 2.2.0") and the placeholder `qubes-firewall.sha256` disclosed in the PR bodies; #230 answered. Status after 2026-08-24 unknown — re-check before acting. [verified 2026-08-24]
- Mirage triage notes kept: qubes-mirage-firewall <0.9.5 deadlocked an HVM's TWO vifs on one thread (fixed upstream PR #219 — why only HVMs were hit); an HVM's two vifs share one IP (the guest's is `vif_ioemu`, the stubdom's is plain `vif`); with an unpatched mirage netvm attached, `qvm-start` HANGS ≥5 min with the domain Transient — rc=124 from a timeout wrapper is a hang, never a "clean failure". [verified 2026-08-22]
- Instrument traps fixed in `guest/health-check.ps1` (grading only, no build behaviour): KM-TEST loopback adapters counted as physical NICs (broke the offline-N/A branch and made identical guests grade differently), `-First 1` adapter selection graded the loopback's APIPA, and ping-gateway as the traffic criterion. Traffic is now DNS/TCP + adapter counters, ping recorded as corroboration only. [verified 2026-08-29]
- fw-net "invisible" trap: `qvm-check fw-net` answers "non-existent" from this dev qube, but that is a POLICY-FILTERED view (`admin.vm.CurrentState` → "Request refused"); fw-net exists and serves traffic. Never read Admin API filtering as absence. A local-endpoint throughput server is genuinely blocked by the netvm's inter-VM drop — and no longer needed (CDN benchmark). [verified 2026-08-29]
- A networked Windows guest is never quiescent: a WU servicing pass mid-run once masqueraded as a "PerWindowCapture logon hang" release blocker (retracted) and silently moved the OS build under benchmarks. Before blaming our code for a guest-wide symptom, check netvm, WU state and the System event log. The template seeds `NoAutoUpdate=1` + `ExcludeWUDriversInQualityUpdate=1`; the driver exclusion also guards the latch against WU-delivered Xen PV packages whose INF AddReg would rewrite NICS. [verified 2026-08-19]
- Q: volume side effects: gui-agent LogDir follows the private volume (`Q:\Qubes Logs`, C: fallback) so AppVM post-mortems survive the volatile root, and rotation prunes Q: since 4.3.7; `Q:\qwtng-netcfg.txt` (L3 cache) and `Q:\qwtng-netsetup.log` persist across AppVM reboots and travel with private-volume clones. Whether a golden/checkpoint-restored subject can carry another lineage's stale Q: cache (which the service would apply early-boot until qubesdb refreshes it) has never been measured. UNVERIFIED
- Residual sporadic first-boot reset (~1 in 5 fresh-build first AppVM boots, noted 2026-08-19): best-supported attribution is the xenvbd per-boot reboot-request mechanism (the template ships one settle boot short, so xenvbd re-files `xenbus_monitor\Request\xenvbd\Reboot=1` every AppVM boot); shipping xenbus_monitor stopped+disabled for every qube class (4.3.7) removes the prompt/restart path, but the resetter was never caught in the act. UNVERIFIED
- Open latency-tail question (reliability is clean): the Windows PV guest shows a ~5x heavier TCP connect tail than a Linux qube on the same link (p99 65 vs 49 ms, max 525 vs 95 ms) and ~8 ms extra DNS-client overhead; the IPv6-binding hypothesis was tested and disproven. Samplers need ≥5 s connect timeouts or the tail reads as an outage. [verified 2026-08-24]
