# updates — findings

## CURRENT STATE

AUTHORITATIVE — and the ONLY content of this file. The dated history log was amputated
2026-09-01 (owner call): the chronological format itself caused stale-first reads and
contaminated sessions. Maintain by editing bullets IN PLACE (add, correct, delete);
never append dated sections (the pre-commit hook refuses them). Every bullet carries
`[verified <date>]` or `UNVERIFIED`. Git history retains the old log for deliberate
forensics only — do not load it into context.

- Sections dated 2026-08-30/31 were ERASED on 2026-09-01 (owner call: those sessions were contaminated and their output is void). Do not cite them and do not reconstruct them from git history. Claims RETRACTED in that window STAY retracted; claims MADE in that window are void — re-verify live before relying on anything that traces there. [verified 2026-09-01]
- VOID (owner ruling 2026-09-01): the 2026-08-30 "U1 scan failure (0x8024402C x4)" was never a verifiable failure — a meltdown-period artifact like the rest of that window. There is NO open U1 defect and NO proxy-plane design decision pending; the "bitsadmin setieproxy LOCALSYSTEM is also required" claim from the same period is equally void. [verified 2026-09-01]
- ARCHITECTURE: updates are dom0-owned on the Linux vmupdate model — the guest NEVER auto-installs (deploy sets NoAutoUpdate=1; wuauserv/USO stay enabled for the on-demand path). Guest side is preinstalled (dom0 cannot inject an agent into Windows): guest/vmupdate-shim.ps1 + our fixed VMExec.ps1 answer dom0's injected-agent sequence; guest/wu-update.ps1 (rpc qubes.WindowsUpdate) speaks the exact contract — bare floats 0..100 on stderr, exit 0=ok / 100=no-updates / 1=error (+75=result unknown); every stderr message MUST end in a non-numeric word (dom0 falls back to float(line.split()[-1])). qvm-windows-update in the dom0 RPM is a fallback, not the required path. [verified 2026-08-13]
- The `vmexec` feature is REQUIRED (earlier "works without it" claim RETRACTED — both fallback shapes fail before the shim) and must be set FROM DOM0: the guest cannot advertise it because Windows qubesdb-cmd cannot write (upstream optind bug in qubes-core-qubesdb; candidate upstream report still awaiting owner-approved text). [verified 2026-08-13]
- Stock-QWT defects fixed in our shipped copies: VMExec.ps1 returned 0 for EVERY call (nonzero exit propagation since proven end-to-end on a real failure); VMExec-Decode mangled non-ASCII arguments (byte-accumulating UTF-8 decoder fix). Unrecognized verbs naming the updater workdir (e.g. chmod from newer dom0s) are logged NO-OPs returning 0 — dom0's transfer_agent takes max(codes), so one failing prep step otherwise turns a successful install into an ERROR verdict. [verified 2026-08-13]
- PROXY IS POSITIONAL, NOT TEMPORAL: relay + proxy planes exist only during a pass (Remove-Proxy in a finally restores the offline baseline — the 147-calls/afternoon leak class is closed), and the relay gates every connection on the OWNING PROCESS (update services via SCM + our agent; additive AllowedImages/AllowedServices REG_MULTI_SZ under HKLM\SOFTWARE\Qubes\UpdatesProxy; denial = RST at connect so a denied caller sees "no network"; QUBES_UPDATES_PEER_ALLOWLIST=off for diagnostics). The U5 TCP-table size/fetch race that RST-denied legitimate WU connections at random is fixed (retry with slack; fail-closed with distinct pid-lookup-failed logging). [verified 2026-08-20]
- Proxy planes as shipped: Ensure-Proxy sets netsh winhttp + HKLM WinINET + DODownloadMode=0 — this set STANDS; change it only on a live, verifiable failure from a clean subject. Instrument note kept: a ~2 s 0x8024402C fail would mean WU never dialed the proxy at all. [verified 2026-08-20]
- 0x80072F8F ROOT CAUSE + FIX (fleet-wide scan blocker): expired auto-update CTL/trust-list state — CDP CRLs were NEVER the mechanism. Sync-Revocation (commit 9216667, in qubes-windows-update.ps1) fetches authroot/disallowedcert/pinrules CTL cabs through the relay at every pass start, mirrors them in C:\ProgramData\QubesCTL, points AuthRoot RootDirURL at the mirror, flushes the chain cache. Proven by defect-reintroduction + self-heal on pristine images of both lineages; the delivery-CDN leg proven too (729.7 MB .msu, one attempt). U4 hardening: RootDirURL is set only when BOTH core CTLs are present — an empty/half mirror can no longer poison validation. DigiCert-cross-chain CDPs remain the first suspect if a future DOWNLOAD-phase 0x80072F8F appears (add via a QUBES_UPDATES_ALLOW_EXTRA, never by replacing the builtin allowlist). [verified 2026-08-19]
- RELAY (guest/qubes-updates-relay.cs, compiled on-guest with the in-box C#5 csc, connect-back per-connection qrexec spawn, full-path qrexec-client-vm): plain-HTTP rides a verified/retried path — complete-or-502, framing-aware (Content-Length, chunked terminator, clean EOF), >16 MB spills as committed Streamed; dead warm channels have their own bounded allowance so a scan can no longer silently report 0 updates off dead channels. The 16 MB-truncation-sold-as-200 defect is FIXED with `--selftest` 8/8 (seen to fail 6/8 on a defect-reintroduced build); the redesigned pool drains when idle and the updater client keeps connections alive. OPEN: no live large plain-HTTP download has re-run through the fixed build (acceptance = one attempt, no "stream ended early"). [verified 2026-08-20]
- TRANSPORT LOSS (task #14-transport, open): the qrexec/vchan hop loses part of a plain-HTTP response ~1/3 of the time — reproduced with our relay REMOVED (proxy-probe.cs), HTTP 200 with short body and orderly EOF; CONNECT is unaffected (4.8 GB at 12.8 MB/s through the same transport). Not our code, but NOT established as an upstream defect (would need repro after a proxy-qube restart or from a Linux qube); the relay's retry makes it non-fatal for everything WU uses. The "it worked this morning" mystery was luck on this lossy path caching one complete CTL, not configuration. [verified 2026-08-14]
- RETRACTION GUARD: "relay vchan churn is the wedge TRIGGER" is RETRACTED — churn was a provocation only (the wedge is the Xen HVM IPI/TLB-shootdown deadlock, tracked in its own finding). The scan debounce stays as hygiene: `-Scheduled` passes only, from last COMPLETED pass, default 30 min, QUBES_UPDATES_DEBOUNCE_MIN=0 disables; dom0-requested passes are never skipped. [verified 2026-08-20]
- CONTENT-CLASS ROUTER (netvm-free guests): scan-time classification by DownloadContents shape — SELF-CONTAINED (one static download.windowsupdate.com URL) → proxy-fetch + install NLA-free, verified BY EFFECT (MSRT proven installed); EXPRESS/PSF (DO filestreamingservice streams) → terminal informational 'wu-only-express'; Defender delta patches excluded (full mpam-fe is DO-only — Defender definitions genuinely have NO routeless path); Install-ViaWU (DO/BITS) runs only behind a real default route or QUBES_UPDATES_ALLOW_WU_NATIVE=1. Defect pair on record: the old WU-native path installs NOTHING routeless (ResultCode=4). Every offered update is deterministic: installs, or informational — never intermittent, never a phantom failure. [verified 2026-08-20]
- RETRACTED APPROACHES (do not revive): NLA/NCSI flipping and the KM-TEST loopback adapter (non-deterministic + security-weakening deception, forbidden); "checkpoint-cumulative transition" as the Win10 catalog-gap cause (WRONG — Win10 22H2 LCUs are monolithic; checkpoints are Win11 24H2+ only); harvesting express URLs (8496 expiring hash-matched deltas — doubly dead). Also RETRACTED: the "tunnel throughput ~120-150 KB/s" problem (measured 12.8-14.4 MB/s; old figures were the WU-native path) and the "805 KB/s Defender download bottleneck" (exclusion A/B showed no effect). [verified 2026-08-20]
- ESU (Win10 22H2, PARKED as informational): KB5071959 is a WU-only express phantom OOB with no security content — terminally classified, never chased; the REAL 2025-11 security CU KB5068781 is catalog-published monolithic and resolves today (776 MB .msu named by `-Action resolve`); its only gate is CBS ESU entitlement at install (owner MAK/licensing decision). Get-ServicingNotice reports the state as severity=info, excluded from the actionable/remaining count and from dom0's updates-available marker. [verified 2026-08-20]
- INSTALL PROVEN via catalog .msu + DISM (incl. msu→cab): win10-clean drained 19045.2965 → 19045.6456 (StandaloneVM rig in template-mode); Win11 25H2 UBR 8875 → 9168; the 24H2 cumulative 26100.8875 → 26100.9168 landed on win11-tpl once the KB filter dropped its poisoned sibling. Per docs/ACCEPTANCE-4.3.16.md the one genuine U3 gap: a dom0-driven install drain has NEVER run on a TemplateVM proper (close with UBR + CBS state=112 acceptance on win10-tpl). [verified 2026-08-20]
- ONE PACKAGE PER SERVICING SESSION: staging a second reboot-requiring package silently discards it at commit boot (proven n=2 with settle controls; DISM says 3010 for both) — serialization is the CORRECT fix, QUBES_UPDATES_ALLOW_MULTISTAGE stays OFF. The pre-download KB filter (exact filename match, superseded siblings dropped at zero bytes) is correctness first — the rejected kb5043080 sibling had poisoned the CBS transaction and rolled the CU back — and 9.5% transfer second. DISM 3010 = STAGED, not installed: the summary says "staged (completes at restart)". [verified 2026-08-14]
- CLASS GATING: the proxy updater runs IFF the deploy-time stamp HKLM\SOFTWARE\Qubes!VmClass == 'TemplateVM' (commit 3d74db7; clone-to-template stamps its outputs; to exercise the template path on the Standalone rig, temporarily stamp it). Anything else NEVER proxies: direct internet → Windows self-updates (NoAutoUpdate removed); offline → nothing. The direct-internet-only discriminator is RETRACTED (offline ≠ template). AppVM guard: RootIdentity xenstore-UUID mismatch → 'skipped-appvm' BEFORE Ensure-Proxy, proven both directions with zero-relay-byte witnesses on both lineages. The QdbDaemon startup race (boot pass reading empty VmClass and skipping a real template) is fixed with a ~14 s retry, exercised by U2's cold-boot clause (qdb_retry_evidence=true). [verified 2026-08-20]
- AVAILABILITY + SCAN HONESTY: scheduled task QubesWindowsUpdateScan (SYSTEM, boot + 6-hourly) is scan-ONLY — it never installs. Relay give-ups make a 0-result UNKNOWN (one rescan, then exit 75, never a fabricated count); a non-empty scan with give-ups reports SCAN PARTIAL (lower bound); the guard returns -1 for unknown, derives its path from -WorkDir, counts PLAIN REFUSED. updates-available is cleared optimistically at pass end (uninstalled-KB count when a reboot is pending) — the boot scan self-corrects a wrong guess, backstop proven live. [verified 2026-08-20]
- REBOOT POLICY: the pass commits the reboot itself whenever one is staged — template AND standalone (owner call; user-guided action, no safeguard). A guest-initiated reboot ends HALTED (<on_reboot>destroy</on_reboot> — a qube can never restart itself); Windows completes servicing at the next boot, which for a template is the boot that commits to the root. shutdown.exe is called directly with $LASTEXITCODE checked (the Start-Process version silently scheduled nothing). [verified 2026-08-13]
- AUTOLOGON IS PART OF THE UPDATE MACHINERY: a cumulative that consumes DefaultPassword makes the qube UNMANAGEABLE (rc=117 on qrexec service calls; the agent cannot inject into Winlogon). Prevention runs at EVERY pass end that staged a reboot regardless of who reboots (commit 6084c12) + boot-time QubesAutologonGuard + assert-at-deploy; the pass REFUSES to reboot when autologon cannot be guaranteed (staged update outranks an unreachable qube); guard proven to fail on a simulated-consumed state. Recovery for a locked guest: admin.vm.volume.Revert (volume-level, needs no session) — but check snapshot timestamps against the servicing first. [verified 2026-08-19]
- LOCALE-HARDENED, MEASURED NOT ASSUMED: progress floats and status timestamps are culture-invariant (proven end-to-end on the shipped handler under forced de-DE; 'yyyy' custom patterns take the culture's CALENDAR — only 's'/'o' are safe); catalog resolution is language-invariant by construction — the title only narrows/ranks on untranslated tokens, accept/reject runs on the .msu FILENAME, product family derives from InstallationType + the 22000 build boundary; the catalog's response language is NONDETERMINISTIC (an fr-FR request has returned Italian) so no logic may key on title text. The [ref]$null TryParse bug that silently disabled the status freshness guard is fixed (MinValue + TryParseExact). Testing trap: wu-update.ps1 writes progress via [Console]::Error.WriteLine — PowerShell 2>&1 captures NOTHING; use a separate process with cmd-level 2>. [verified 2026-08-14]
- RIG/BACKEND: qubes.UpdatesProxy for @tag:win-idd-testbed routes to win-idd-mgmt's tinyproxy on 127.0.0.1:8082 (PERMANENT standing infrastructure — never kill it), ahead of the core-update (Tor) fallback; the Tor backend is proven unreliable for large WU payloads. qrexec-client-vm always exits 0 on trigger and pipe-args must be UNQUOTED (a wrapped quote becomes `_@default` in dom0's log); only bytes the handler receives are evidence. [verified 2026-08-19]
- ACCEPTANCE STANDING (docs/ACCEPTANCE-4.3.16.md): U0 + U2 pass on 4.3.16; every U1 claim in that campaign — the "failure" AND the later "3/3 scans" closure — traces to the voided window, so U1 (availability to dom0) is simply UNMEASURED post-void; one clean live scan settles it when needed. U3 was not run in that campaign; the prior end-to-end proofs above stand. UNVERIFIED
- OPEN ITEMS (statuses last verified 2026-08-20; the erased window may have touched them — re-verify live): (a) win11-fresh's relay/updater deploy returned an EMPTY result — not deployed, not verified, cause unknown; (b) scan tasks on win10-clean / win11-tpl / win11-fresh lack `-Scheduled`, so the debounce is inert there until wu-task-add-scheduled.ps1 or a reinstall; (c) the live large-download acceptance for the relay truncation fix (see relay bullet); (d) the TemplateVM install drain (see install bullet); (e) plain-HTTP vchan loss (see transport bullet); (f) video-modes-during-update (hide or shrink+decorate the guest desktop during servicing, restore on every exit path) is planned, NOT started. UNVERIFIED
- SCOPE NOTE: xenvif attach/mirage, the 4.3.8 WDDM DriverVer regression, and MSI driver-catalog signing belong to the network/idd/install topics, not here; the catalog-signing defect is closed by the G0 gate per docs/ACCEPTANCE-4.3.16.md (8 catalogs, 0 unsigned, two-sided negative control). [verified 2026-08-30]
